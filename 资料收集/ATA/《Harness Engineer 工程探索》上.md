---
tags:
  - ata
  - 资料收集
articleId: 11020621211
url: https://ata.atatech.org/articles/11020621211?utm_source=open&utm_medium=hsf&utm_campaign=_OPEN_AONE
author: 张医博
nickname: 韩笠
workId: 139717
department: 云智能集团
created: 2026-04-17T18:21:17+08:00
source: ATA
---

# 一、文章信息

## 1. 基本信息

* 标题：《Harness Engineer 工程探索》上
* 作者：张医博（花名：韩笠，工号：139717）
* 部门：云智能集团
* 发布时间：2026-04-17T18:21:17+08:00
* 文章链接：https://ata.atatech.org/articles/11020621211?utm_source=open&utm_medium=hsf&utm_campaign=_OPEN_AONE
* 文章 ID：11020621211

## 2. 摘要

* Harness Engineering 是 AI 工程化新范式，通过规划、任务编排、记忆管理、工具调用、人机协同等能力，为大模型构建可验证、可回滚、可观测的“操作系统级”管控层。

# 二、正文原文

## 1. 正文

* 以下为 ATA 原文抓取内容。

前言 本文是一次关于 Harness Engineering 的深度探索，从 Prompt Engineering 的起源，到 Context Engineering 的演进，最终抵达 Harness Engineering 的工程范式。本文章，部分插图来自 Skill 调用wan2.7 插图。 一、what harness engineer ？ 要正本溯源先从 prompt 说起，因为 harness 是人类工程师自我不断反思，在模型之外靠工程化思维的锦上添花之笔，并非平底跃起的高楼大厦，而是经历了三个阶段： 第一阶段： prompt engineer（prompt工程） 特点：靠提示词输入，来约束模型内容的输出，尽可能让模型理解人话，局部问题的概率生成，通过 prompt 引导模型； 缺点：windows size 有限，距离较远的记忆发生腐化，经常容易老年痴呆； 第二阶段：context engineer（上下文工程） 特点：解决第一阶段 prompt 遇到的问题，通过记忆的方式，解决长上下文窗口限制，典型的 RAG 就是对 context engineer 的补充，检索内容注入到上下文，包括 文档切块 -> 结果排序 -> 长文压缩 -> 内容摘要 -> agent 间的信息传递。 缺点：随着任务的不断进化和改进，角色交替，人工干预的信息持续叠加，依然会出现 context 爆炸问题； 中间态： 而本身 skill 也是对 context 的一种渐进式的高级优化。解决了将一堆工具参数、使用说明扔给模型后的大量信息冗余和上下文占用，解决注意力集中的问题（按需给、分层给），但仍然解决不了 第三阶段：harness engineer（工程围栏） 特征：上述的 PE 和 context 都是在解决输入的问题。但在 agent 连续的任务执行时，谁来约束和纠偏，不能只喂信息，还要能驾驭过程。harness 为模型套上一层围栏，以工程思维解决记忆、执行、思考、观察 多环节的问题，让 agent 形成自闭 举个 harness 的例子 案例一：比如 TAM 胡先生，去陪和客户打球，Prompt 就是告诉小胡 对方的相关资料，适合什么位置，擅长大球，还是小球。conext 就是应对打不同球的战术，大球要位怎么做，小球发力怎么做。Harness engineer ，就是到了真刀真枪干的时候，要能随机应变的走位，并且要会打商务球，到最后结束比赛还能让客户玩的高兴，事后还要做个复盘。 案例二：还是 TAM 小胡，老板派他去客户现场做个护航技术交流，小胡用 harness 思维拆解。交流期间：小胡要准备护航的 checklist（POC 、压测、巡检） 资料给客户展示；介绍护航在关键阶段要有数据播报、应急预案、盯屏、告警订阅；交流完成后：要核对会议纪要和听记，进行为你纠偏，发现哪里描述不对，麻烦同步给客户，并形成一个最终的护航技术文档交付给客户； 所以我是怎么看 harness 的？ 一张图说明，Harness Engineer 和 Prompt Engineer 、Context Engineer 是包含关系，是在最外层的套壳； 业界翘楚是怎么看 Harness Engineer 的？ From LangChain 认知介绍：如果你不是模型，就是 Harness ，Agent = Harness + LLM；下面是来自 LangChain 关于 Harness 的能力拆解，借用官文； An agent harness is a combination of several different capabilities that make building long-running agents easier: Planning capabilities Virtual filesystem Filesystem permissions Task delegation (subagents) Context and token management Code execution Human-in-the-loopAlongside these capabilities, Deep Agents use Skills and Memory for additional context and instructions. Planning capabilitiesThe harness provides a write_todos tool that agents can use to maintain a structured task list.Features: Track multiple tasks with statuses ('pending', 'in_progress', 'completed') Persisted in agent state Helps agent organize complex multi-step work Useful for long-running tasks and planning Virtual filesystem accessThe harness provides a configurable virtual filesystem which can be backed by different pluggable backends. The backends support the following file system operations: Tool Description ls List files in a directory with metadata (size, modified time) read_file Read file contents with line numbers, supports offset/limit for large files. Also supports returning multimodal content blocks for non-text files (images, video, audio, and documents). See supported extensions below. write_file Create new files edit_file Perform exact string replacements in files (with global replace mode) glob Find files matching patterns (e.g., **/*.py) grep Search file contents with multiple output modes (files only, content with context, or counts) execute Run shell commands in the environment (available with sandbox backends only) Supported multimodal file extensions The virtual filesystem is used by several other harness capabilities such as skills, memory, code execution, and context management. You can also use the file system when building custom tools and middleware for Deep Agents.For more information, see backends. Filesystem permissionsThe harness supports declarative permission rules that control which files and directories the agent can read or write. Permissions apply to the built-in filesystem tools listed above and are evaluated in declaration order with first-match-wins semantics.How it works: Pass a list of rules to permissions= when creating the agent Each rule specifies operations (\"read\", \"write\"), paths (glob patterns), and mode (\"allow\" or \"deny\") The first matching rule wins. If no rule matches, the operation is allowed.Why it’s useful: Restrict agents to specific directories (e.g., /workspace/) Protect sensitive files (e.g., .env, credentials) Give subagents narrower access than the parent agentPermissions do not apply to sandbox backends, which support arbitrary command execution via the execute tool. For custom validation logic, use backend policy hooks.For the full rule structure, examples, and subagent inheritance, see Permissions. Task delegation (subagents)The harness allows the main agent to create ephemeral “subagents” for isolated multi-step tasks.Why it’s useful: Context isolation - Subagent’s work doesn’t clutter main agent’s context Parallel execution - Multiple subagents can run concurrently Specialization - Subagents can have different tools/configurations Token efficiency - Large subtask context is compressed into a single resultHow it works: Main agent has a task tool When invoked, it creates a fresh agent instance with its own context Subagent executes autonomously until completion Returns a single final report to the main agent Subagents are stateless (can’t send multiple messages back)Default subagent: “general-purpose” subagent automatically available Has filesystem tools by default Can be customized with additional tools/middlewareCustom subagents: Define specialized subagents with specific tools Example: code-reviewer, web-researcher, test-runner Configure via subagents parameter Context managementThe harness manages context so deep agents can handle long-running tasks within token limits while retaining the information they need.How it works: Input context — System prompt, memory, skills, and tool prompts shape what the agent knows at startup Compression — Built-in offloading and summarization keep context within window limits as tasks progress Isolation — Subagents quarantine heavy work and return only results (see Task delegation) Long-term memory — Persistent storage across threads via the virtual filesystemWhy it’s useful: Enables multi-step tasks that exceed a single context window Keeps the most relevant information in scope without manual trimming Reduces token usage through automatic summarization and offloadingFor configuration details, see Context engineering. Code executionWhen you use a sandbox backend, the harness exposes an execute tool that lets the agent run shell commands in an isolated environment. This enables the agent to install dependencies, run scripts, and execute code as part of its task.How it works: Sandbox backends implement the SandboxBackendProtocolV2 — when detected, the harness adds the execute tool to the agent’s available tools Without a sandbox backend, the agent only has filesystem tools (read_file, write_file, etc.) and cannot run commands The execute tool returns combined stdout/stderr, exit code, and truncates large outputs (saving to a file for the agent to read incrementally)Why it’s useful: Security — Code runs in isolation, protecting your host system from the agent’s operations Clean environments — Use specific dependencies or OS configurations without local setup Reproducibility — Consistent execution environments across teamsFor setup, providers, and file transfer APIs, see Sandboxes. Human-in-the-loopThe harness can pause agent execution at specified tool calls to allow human approval or modification. This feature is opt-in via the interrupt_on parameter.Configuration: Pass interrupt_on to create_deep_agent with a mapping of tool names to interrupt configurations Example: interrupt_on={\"edit_file\": True} pauses before every edit You can provide approval messages or modify tool inputs when promptedWhy it’s useful: Safety gates for destructive operations User verification before expensive API calls Interactive debugging and guidance SkillsThe harness supports skills that provide specialized workflows and domain knowledge to your deep agent.How it works: Skills follow the Agent Skills standard Each skill is a directory containing a SKILL.md file with instructions and metadata Skills can include additional scripts, reference docs, templates, and other resources Skills use progressive disclosure—they are only loaded when the agent determines they’re useful for the current task Agent reads frontmatter from each SKILL.md file at startup, then reviews full skill content when neededWhy it’s useful: Reduces token usage by only loading relevant skills when needed Bundles capabilities together into larger actions with additional context Provides specialized expertise without cluttering the system prompt Enables modular, reusable agent capabilitiesFor more information, see Skills. MemoryThe harness supports persistent memory files that provide extra context to your deep agent across conversations. These files often contain general coding style, preferences, conventions, and guidelines that help the agent understand how to work with your codebase and follow your preferences.How it works: Uses AGENTS.md files to provide persistent context Memory files are always loaded (unlike skills, which use progressive disclosure) Pass one or more file paths to the memory parameter when creating your agent Files are stored in the agent’s backend (StateBackend, StoreBackend, or FilesystemBackend) The agent can update memory based on your interactions, feedback, and identified patternsWhy it’s useful: Provides persistent context that doesn’t need to be re-specified each conversation Useful for storing user preferences, project guidelines, or domain knowledge Always available to the agent, ensuring consistent behaviorFor configuration details and examples, see Memory. 综合业态的最终看法 LLM 无论在怎么优化，在达到某个阶段后，在一个持续的窗口期内，还是会有瓶颈，就像是一块高速的 CPU ，虽然能力很强大，但要分场景的去驾驭，并不能解决所有问题，而 harness 就是驾驭 CPU 的操作系统，提供与 CPU 进行上下文切换、任务调度、内存管理的一套操作系统定式。而跑在操作系统之上的应用就像你开发的 agent ，通过操作系统去和 CPU 进行交互。 二、Harness engineer 工程范式 工程分层 第一层：边界定义 角色定义&任务目标 上下文信息优化&裁剪 结构化：规则文件、当前任务、运行态存储 等； 第二层：工具调用 选择合适的调用工具，给定边界； 配置调用的最佳规则，该用的时候用； 结果回传，多个工具调用的提炼、总结、筛选； 第三层：任务编排 理解任务 信息判断（不够就补充） 任务分析 输出编排 检查结果（同步修正） 第四层：记忆 当前任务状态 任务中间状态 长期记忆&偏好 第五层：评估&观测 输入验收 环境验证 机械测试 日志&Prom 监控 错误归因分析 第六层：验收 结果校验 快恢机制（重试、回滚） 工程抽象 编排层，对任务进行全局管理、拆解 ；执行层 ，通过 MCP 脚本 沙箱 等完成执行外界调用；记忆层， context 工程；执行层，报错结果通过反馈层的校验，再次注入上下文，再传递到大模型层，最中形成完成的链条。 大厂现身说法的几个场景 3.1 Anthropic 上下文记忆压缩 问题：长链路动态信息的负担 方案：变成 Context Reset，交给新的 Agent 解决 比喻：等于内存泄露，重启进程 自评失真 问题：裁判 + 选手是一个人 方案：将二者分开，通过 Verify 脚本方式进行业务逻辑测试（机械测试） 比喻：就像考试不能自己给自己批改 3.2 OpenAI Agent 自主规划与执行 沙箱环境的安全隔离 人类在关键节点的介入 结果的可验证与可回滚 Harnees 核心 Agent.md 来自 Agent.md 标准的 demo ，建议描述关键核心、且重点内容 Agent.md 不建议超过 300-500 行 描述关键核心、且重点内容 包含开发环境提示、测试指令、PR 规范等 # Sample AGENTS.md file\n\n## Dev environment tips\n- Use `pnpm dlx turbo run where <project_name>` to jump to a package instead of scanning with `ls`.\n- Run `pnpm install --filter <project_name>` to add the package to your workspace so Vite, ESLint, and TypeScript can see it.\n- Use `pnpm create vite@latest <project_name> -- --template react-ts` to spin up a new React + Vite package with TypeScript checks ready.\n- Check the name field inside each package's package.json to confirm the right name—skip the top-level one.\n\n## Testing instructions\n- Find the CI plan in the .github/workflows folder.\n- Run `pnpm turbo run test --filter <project_name>` to run every check defined for that package.\n- From the package root you can just call `pnpm test`. The commit should pass all tests before you merge.\n- To focus on one step, add the Vitest pattern: `pnpm vitest run -t \"<test name>\"`.\n- Fix any test or type errors until the whole suite is green.\n- After moving files or changing imports, run `pnpm lint --filter <project_name>` to be sure ESLint and TypeScript rules still pass.\n- Add or update tests for the code you change, even if nobody asked.\n\n## PR instructions\n- Title format: [<project_name>] <Title>\n- Always run `pnpm lint` and `pnpm test` before committing. 三、harness & spec kit & vibe coding 2024 年，Vibe Coding 让人人都能\"用嘴写代码\"。你只需要描述你想要的效果，AI 就会帮你生成代码。 Vibe Coding 的优势： Vibe Coding 的问题： 门槛极低，不懂代码也能做产品 快速原型，想法秒变 Demo 创意驱动，适合探索性项目 依赖模糊的 AI 提示词，经常产生\"架构不一致\"的代码 容易出现\"幻觉需求\"，AI 理解偏差导致返工 缺乏规范，代码质量参差不齐 难以维护，\"能跑但看不懂\" 形象比喻：Vibe Coding 就像\"即兴爵士乐\"，灵感来了就能演奏，但很难写出交响乐。 Spec Coding：从\"感觉\"到\"规格\" Spec Coding（规格驱动开发）应运而生。核心理念：先写规格，再写代码。用结构化的文档定义\"做什么\"，再让 AI 解决\"怎么做\"。Spec Kit 的工作流（GitHub 开源 CLI 工具）： 定义规则：首先定义项目治理规则 转换需求：将功能需求转化为用户为中心的规格说明 技术设计： outline 具体的文件修改和数据结构 创建任务：生成有序的任务列表 AI 生成：最后 AI 才生成实际代码 形象比喻：Spec Coding 就像\"建筑蓝图\"，先画好设计图，再施工，而不是边建边改。 Harness Engineering：从\"规格\"到\"管控\" Harness Engineering 成为 AI 开发的终极形态。三者的关系： 维度 Vibe Coding Spec Coding Harness Engineering 核心 灵感驱动 规格驱动 工程管控 输入 模糊描述 规格文档 规格 + 规则 + 记忆 过程 即兴发挥 按图施工 规划-执行-观测-修正闭环 输出 能跑的代码 规范的代码 可验证、可回滚、可观测的系统 适合 原型探索 中型项目 企业级生产系统 比喻 即兴爵士 交响乐 交响乐团+指挥+录音棚 Harness 对 Vibe Coding 的增强： 边界约束：Vibe Coding 是\"野马\"，Harness 给它套上\"缰绳\" 自动验证：每次生成后自动运行测试、Lint、类型检查 任务编排：大任务拆解为子 Agent 并行处理 记忆管理：记住项目偏好、用户习惯、历史教训 人在回路：关键节点人类审批，防止 AI 自主破坏 可观测性：日志、监控、错误归因，问题可追溯 Spec Kit + Harness 的 Vibe Coding 新范式 传统 Vibe Coding： 用户描述 → AI 生成代码 → 人工检查 → 返工循环 Spec Kit + Harness 的 Vibe Coding： 用户描述\n    ↓\nSpec Kit 生成规格文档（需求边界、假设、验收标准）\n    ↓\nHarness 读取规格，创建任务计划\n    ↓\n子 Agent 并行执行（代码生成、测试编写、文档更新）\n    ↓\n自动验证（Lint、Test、Type Check）\n    ↓\n人类审批（可选，关键节点）\n    ↓\n合并代码，更新记忆\n    ↓\n生成可追溯报告 预告 下章节将以实操案例，介绍如果落地 Harness 工程
