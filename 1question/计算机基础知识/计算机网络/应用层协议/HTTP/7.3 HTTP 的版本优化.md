---
title: "7.3 HTTP 的版本优化"
tags:
  - "计算机基础知识"
  - "计算机基础知识/计算机网络"
  - "计算机基础知识/计算机网络/应用层协议"
  - "HTTP 的版本优化"
  - "HTTP"
  - "计算机基础"
updated: 2026-04-16
---
# 一、HTTP/1.1 对 HTTP/1.0 的优化⭐
> ❗ **目前使用的 HTTP 协议大部分都是 1.1**
- **连接方式：**
    - HTTP 1.0 为短连接，HTTP 1.1 支持长连接
    - HTTP/1.0 每发起一个请求，都要新建一次 TCP 连接（三次握手），而且是串行请求，做了无谓的 TCP 连接建立和断开，增加了通信开销
    - ==从 HTTP/1.1起，默认使用长连接，用以保持连接特性。每个====`HTTP`====连接完成后，其对应的====`TCP`====连接并不是每次都会关闭，减少了 TCP 连接的重复建立和断开所造成的额外开销==
    - 使用长连接的`HTTP`协议，会在响应头有加入这个头部字段：**`Connection:keep-alive`**

        ![[IMG-20260405035535175.png|709]]
- **状态响应码：**
    - ==HTTP/1.1中新加入了大量的状态码==，光是错误响应状态码就新增了 24 种
    - 比如说
        - `100 (Continue)`——在请求大资源前的预热请求
        - `206 (Partial Content)`——范围请求的标识码
        - `409 (Conflict)`——请求与当前资源的规定冲突
        - `410 (Gone)`——资源已被永久转移，而且没有任何已知的转发地址
- **缓存处理：**
    - ==HTTP1.1提供了更多可供选择的缓存头来控制缓存策略==
    - 在 HTTP1.0 中主要使用 header 里的 If-Modified-Since、Expires 来做为缓存判断的标准
    - HTTP1.1 则引入了更多的缓存控制策略例如 Entity tag、If-Unmodified-Since、If-Match、If-None-Match 等
- **带宽优化及网络连接的使用：**
    - HTTP1.0 中，存在一些浪费带宽的现象，例如客户端只是需要某个对象的一部分，而服务器却将整个对象送过来了，并且不支持断点续传功能
    - HTTP1.1 则==在请求头引入了== ==**`range`**== ==头域，它允许只请求资源的某个部分==，即返回码是 `206`（Partial Content），这样就方便了开发者自由的选择以便于充分利用带宽和连接
- **Host 头处理：**
    - HTTP/1.1在请求头中加入了**`Host`**字段
    - 域名系统（DNS）允许多个主机名绑定到同一个IP地址上，但是 HTTP/1.0 并没有考虑这个问题
    - 假设我们有一个资源URL是 http://example1.org/home.html，HTTP/1.0的请求报文中，将会请求的是`GET /home.html HTTP/1.0`，也就是不会加入主机名
    - 这样的报文送到服务器端，服务器是理解不了客户端想请求的真正网址
    - 因此，HTTP/1.1在请求头中加入了`Host`字段。加入`Host`字段的报文头部将会是

        ```Plain
        GET /home.html HTTP/1.1
        Host: example1.org
        ```
    - 这样，服务器端就可以确定客户端想要请求的真正的网址了

# 二、HTTP/2 对 HTTP/1.1 的优化
1. **头部压缩**
    1. **HTTP/1.1存在的问题：**==请求 / 响应头部（Header）未经压缩就发送，如果每次互相====**发送相同的首部**====造成的浪费较多==
    2. **HTTP/2 的改进：**会压缩头（Header）如果你同时发出多个请求，他们的头是一样的或是相似的，那么协议会帮你消除重复的部分
    3. 这就是所谓的 **HPACK 算法**：
        1. 在客户端和服务器同时维护一张**头信息表（**哈希表**）**，所有字段都会存入这个表，生成一个索引号
        2. 以后就不发送同样字段了，只发送索引号，这样就提高速度了
2. **多路复用**
    1. **HTTP/1.1存在的问题：**==按请求的====**顺序响应**====的，如果服务器响应慢，会后边的客户端一直请求不到数据，也就是====**队头阻塞**==

        ![[IMG-20260405035539366.png|472]]
    2. **HTTP/2 的改进：**
        1. 移除了 HTTP/1.1 中的串行请求
        2. HTTP/2 是可以在**一个连接中**==**并发**==**多个请求或回应**，而不用按照顺序排队
        3. 也就不会再出现「队头阻塞」问题，降低了延迟，大幅度提高了连接的利用率
    3. 举例来说，在一个 TCP 连接里，服务器收到了客户端 A 和 B 的两个请求
        1. **如果发现 A 处理过程非常耗时，于是就回应 A 请求已经处理好的部分**
        2. **接着回应 B 请求，完成后，再回应 A 请求剩下的部分**

    ![[IMG-20260405035539422.png|461]]
3. **二进制格式**
    1. ==HTTP/1.1 里的纯文本形式的报文，对人友好，但是对计算机非常不友好==
    2. 故将原来 Headers + Body 的报文格式如今被拆分成了一个个**二进制的帧**：分为 头信息帧和数据帧
    3. 这样虽然对人不友好，但是因为计算机只懂二进制，那么**收到报文后，无需再将明文的报文转成二进制，而是直接解析二进制报文，这增加了数据传输的效率**

        ![[IMG-20260405035544188.png|652]]
4. 指定**优先级**
    1. 客户端还可以指定数据流的**优先级**
    2. 优先级高的请求，服务器就先响应该请求

# 三、HTTP/3 对 HTTP/2 的优化
> HTTP/2 主要的问题在于，多个 HTTP 请求在复用一个 TCP 连接，下层的 TCP 协议是不知道有多少个HTTP 请求的
>
> 所以一旦发生了**丢包**现象，就会**触发 TCP 的重传机制**：这样在一个 TCP 连接中的所有的 HTTP 请求都必须等待这个丢了的包被重传回来
>
> 1. HTTP/1.1 中的管道（ pipeline）传输中如果有一个请求阻塞了，那么队列后请求也统统被阻塞住了（对头阻塞问题）
>
> 2. HTTP/2 虽然解决了对头阻塞问题，但是 HTTP/2 多个请求复用一个TCP连接，一旦发生丢包，就会阻塞住所有的 HTTP 请求
>
> 这都是基于 TCP 传输层的问题
1. HTTP/3 把 HTTP 下面传输层的 **TCP** 协议改成了 **UDP**
    1. **UDP 发生是不管顺序，也不管丢包的，所以不会出现 HTTP/1.1 的队头阻塞 和 HTTP/2 的一个丢包全部重传问题**
    2. 大家都知道 UDP 是不可靠传输的，但基于 UDP 的 QUIC 协议 **可以实现类似 TCP 的可靠性传输**

        ![[IMG-20260405035547331.png|757]]
        QUIC 有自己的一套机制可以保证传输的可靠性的
        1. 当某个流发生丢包时，只会阻塞这个流，其他流不会受到影响
        2. TLS3 升级成了最新的 1.3 版本
        3. 头部压缩算法也升级成了 QPack
        4. HTTPS 要建立一个连接，要花费 6 次交互，先是建立三次握手，然后是 TLS/1.3 的三次握手
        5. **QUIC 直接把以往的 TCP 和 TLS/1.3 的 6 次交互合并成了 3 次，减少了交互次数**

            ![[IMG-20260405035550158.png|764]]
2. 所以
    1. **QUIC 是一个在 UDP 之上的伪 TCP + TLS + HTTP/2 的多路复用的协议**
    2. QUIC 是新协议，对于很多网络设备，根本不知道什么是 QUIC，只会当做 UDP，这样会出现新的问题
    3. 所以 HTTP/3 现在普及的进度非常的缓慢，不知道未来 UDP 是否能够逆袭 TCP
