- [[#1. Spring AMQP 介绍]]
- [[#2. SpringAMQP 提供了三个功能⭐]]
# 1. Spring AMQP 介绍
1. SpringAMQP是基于RabbitMQ封装的一套模板，并且还利用SpringBoot对其实现了自动装配，使用起来非常方便
    
    ![[Attachment/1question/中间件/RabbitMQ/SpringAMQP/IMG-20260405035438328.png]]
    
1. SpringAMQP 的官方地址
    
    > [!info] Spring AMQP  
    > Level up your Java code and explore what Spring can do for you.  
    > [https://spring.io/projects/spring-amqp](https://spring.io/projects/spring-amqp)  
    
# 2. SpringAMQP 提供了三个功能⭐
- **自动声明队列、交换机及其绑定关系：`@Bean` + `FanoutExchange`、`Queue`、`Binding`**
    
    ```Java
    // 声明交换机
    @Bean
    public FanoutExchange fanoutExchange(){
        return new FanoutExchange("itcast.fanout");
    }
    
    // 声明队列：
    @Bean
    public Queue fanoutQueue1(){
        return new Queue("fanout.queue1");
    }
    
    // 绑定队列到交换机
    @Bean
    public Binding fanoutBinding1(Queue fanoutQueue1, FanoutExchange fanoutExchange){
        return BindingBuilder
                .bind(fanoutQueue1)
                .to(fanoutExchange);
    }
    ```
    
- **基于注解的监听器模式，异步接收消息：`@RabbitListener()`注解**
    
    ```Java
    @RabbitListener(queues = "simple.queue")
    public void listenWorkQueue1(String msg) throws InterruptedException {
        System.out.println("消费者1接收到消息：【" + msg + "】" + LocalTime.now());
        Thread.sleep(20);
    }
    ```
    
- **封装了**`**RabbitTemplate**`**工具，用于发送消息：`rabbitTemplate.convertAndSend(queueName, message);`**
    
    ```Java
    @Autowired
    private RabbitTemplate rabbitTemplate;
    
    @Test
    public void testSendMessage2SimpleQueue() {
        String queueName = "simple.queue";
        String message = "hello, spring amqp!";
        rabbitTemplate.convertAndSend(queueName, message);
    }
    ```