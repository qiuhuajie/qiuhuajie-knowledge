---
title: "8.1 普通模式集群"
tags:
  - "中间件"
  - "中间件/RabbitMQ"
  - "中间件/RabbitMQ/MQ 集群"
  - "普通模式集群"
  - "MQ 集群"
  - "RabbitMQ"
updated: 2026-04-16
---

# 一、简介🙋‍♂️
1. 普通集群，或者叫标准集群（classic cluster），具备下列特征：
    1. 会在集群的==各个节点间共享部分数据==，包括：
        1. ==交换机==
        2. ==队列元信息，但==**==不包含队列中的消息==**
    2. 当访问集群某节点时
        1. **==如果队列不在该节点，会根据队列的元数据，去找数据实际所在节点==**
        2. 再从数据所在节点传递到当前节点并返回

    ![[Attachment/1question/中间件/RabbitMQ/MQ 集群/IMG-20260405035438324.png|551]]

    3. 队列所在节点宕机，队列中的消息就会丢失

    ![[IMG-20260405035451756.png|791]]

# 二、实践
## 1. 集群分配

|   |   |   |
|---|---|---|
|**主机名**|**控制台端口**|**amqp通信端口**|
|mq1|8081 ---> 15672|8071 ---> 5672|
|mq2|8082 ---> 15672|8072 ---> 5672|
|mq3|8083 ---> 15672|8073 ---> 5672|

> ==注意：==
>
> 集群中的节点标示默认都是：`rabbit@[hostname]`，因此以上三个节点的名称分别为：
>
> - rabbit@mq1
>
> - rabbit@mq2
>
> - rabbit@mq3
## 2. 获取 Cookie
1. RabbitMQ底层依赖于Erlang，而Erlang虚拟机就是一个面向分布式的语言，默认就支持集群模式。集群模式中的每个RabbitMQ 节点使用 cookie 来确定它们是否被允许相互通信
2. 要使两个节点能够通信，它们必须具有相同的共享秘密，称为**Erlang cookie**
3. cookie 只是一串最多 255 个字符的字母数字字符
4. 每个集群节点必须具有**相同的 cookie**。实例之间也需要它来相互通信
5. 先在之前启动的mq容器中获取一个cookie值，作为集群的cookie。执行下面的命令：

    ```Shell
     docker exec -it mq cat /var/lib/rabbitmq/.erlang.cookie
    ```
6. 可以看到cookie值如下：

    ```Shell
     NMQOJCGYYMPFCWRYGNIS
    ```
7. 接下来，停止并删除当前的mq容器，重新搭建集群

    ```Shell
     docker rm -f mq
    ```
## 3. 准备集群配置
1. 在/tmp目录新建一个配置文件 rabbitmq.conf：

    ```Bash
     cd /tmp
     # 创建文件
     touch rabbitmq.conf
    ```

    文件内容如下：

```Bash
 loopback_users.guest = false
 listeners.tcp.default = 5672
 cluster_formation.peer_discovery_backend = rabbit_peer_discovery_classic_config
 cluster_formation.classic_config.nodes.1 = rabbit@mq1
 cluster_formation.classic_config.nodes.2 = rabbit@mq2
 cluster_formation.classic_config.nodes.3 = rabbit@mq3
```
2. 再创建一个文件，记录cookie

    ```Bash
     cd /tmp
     # 创建cookie文件
     touch .erlang.cookie
     # 写入cookie
     echo "FXZMCVGLBIXZCDEMMVZQ" > .erlang.cookie
     # 修改cookie文件的权限
     chmod 600 .erlang.cookie
    ```
3. 准备三个目录,mq1、mq2、mq3：

    ```Bash
     cd /tmp
     # 创建目录
     mkdir mq1 mq2 mq3
    ```
4. 然后拷贝rabbitmq.conf、cookie文件到mq1、mq2、mq3：

    ```Bash
     # 进入/tmp
     cd /tmp
     # 拷贝
     cp rabbitmq.conf mq1
     cp rabbitmq.conf mq2
     cp rabbitmq.conf mq3
     cp .erlang.cookie mq1
     cp .erlang.cookie mq2
     cp .erlang.cookie mq3
    ```
## 4. 启动集群
1. 创建一个网络：

    ```Bash
     docker network create mq-net
    ```
2. 启动三个容器

    ```Bash
     docker run -d --net mq-net \
     -v ${PWD}/mq1/rabbitmq.conf:/etc/rabbitmq/rabbitmq.conf \
     -v ${PWD}/.erlang.cookie:/var/lib/rabbitmq/.erlang.cookie \
     -e RABBITMQ_DEFAULT_USER=itcast \
     -e RABBITMQ_DEFAULT_PASS=123321 \
     --name mq1 \
     --hostname mq1 \
     -p 8071:5672 \
     -p 8081:15672 \
     rabbitmq:3.8-management
    ```
    ```Bash
     docker run -d --net mq-net \
     -v ${PWD}/mq2/rabbitmq.conf:/etc/rabbitmq/rabbitmq.conf \
     -v ${PWD}/.erlang.cookie:/var/lib/rabbitmq/.erlang.cookie \
     -e RABBITMQ_DEFAULT_USER=itcast \
     -e RABBITMQ_DEFAULT_PASS=123321 \
     --name mq2 \
     --hostname mq2 \
     -p 8072:5672 \
     -p 8082:15672 \
     rabbitmq:3.8-management
    ```
    ```Bash
     docker run -d --net mq-net \
     -v ${PWD}/mq3/rabbitmq.conf:/etc/rabbitmq/rabbitmq.conf \
     -v ${PWD}/.erlang.cookie:/var/lib/rabbitmq/.erlang.cookie \
     -e RABBITMQ_DEFAULT_USER=itcast \
     -e RABBITMQ_DEFAULT_PASS=123321 \
     --name mq3 \
     --hostname mq3 \
     -p 8073:5672 \
     -p 8083:15672 \
     rabbitmq:3.8-management
    ```
## 5. 测试
1. 访问
    - [http://192.168.10.120:8081](http://192.168.10.120:8081/)
    - [http://192.168.10.120:8082](http://192.168.10.120:8082/)
    - [http://192.168.10.120:8083](http://192.168.10.120:8083/)

    可以看到集群的构成：

![[IMG-20260405035507763.png|800]]

2. 在 mq1 节点上创建一个队列

    ![[IMG-20260405035515245.png|800]]

3. 在 mq2、 mq3 上都可以看到这个队列
    > Node 表示这个队列的主节点时 mq1

    ![[IMG-20260405035523448.png|800]]

4. 再向这个队列发送一个消息
    > 所有节点都可以收到这条消息

    ![[IMG-20260405035529574.png|800]]

5. 再 将 mq1 节点停掉，模拟宕机

    ```Plain
     docker stop mq1
    ```

    mq1 此时处于宕机状态

![[IMG-20260405035533607.png|800]]

    在mq2、mq3 节点上创建的队列状态变为了 `down`

![[IMG-20260405035536563.png|800]]
