---
title: "13.1 SQL 性能分析"
tags:
  - "中间件"
  - "中间件/MySQL"
  - "中间件/MySQL/MySQL 性能优化相关"
  - "SQL 性能分析"
  - "MySQL 性能优化相关"
  - "MySQL"
updated: 2026-04-16
---

# 一、SQL 执行频率（什么类型的语句频繁）
1. MySQL 客户端连接成功后，通过 **`show [session|global] status`** 命令可以提供**服务器状态信息**
2. 通过如下指令，可以查看当前数据库的**`INSERT、UPDATE、DELETE、SELECT`**的访问频次
    ```SQL
     -- session 是查看当前会话 ;
     -- global 是查询全局数据 ;
     SHOW GLOBAL STATUS LIKE 'Com_______';
    ```
    ![[IMG-20260620224137489.png|538]]
    - **`Com_delete`** **：**删除次数
    - **`Com_insert`** **：**插入次数
    - **`Com_select`****：**查询次数
    - **`Com_update`****：**更新次数
3. 通过上述指令，**==可以查看到当前数据库到底是以查询为主==**❓**==，还是以增删改为主==**❓**==，从而为数据库优化提供参考依据==**
    1. **如果是以增删改为主**，可以考虑不对其进行索引的优化
    2. **如果是以查询为主**，那么就要考虑对数据库的索引进行优化了
# 二、慢查询日志（针对哪里优化）
1. 那假如说是以查询为主，我们又该如何定位**==针对于哪些查询语句进行优化呢==**❓ 即需要**==找出执行速度慢的SQL语句==**
2. 慢查询日志记录了所有执行时间超过指定参数（**`long_query_time`**，单位：秒，默认10秒）的所有SQL语句的日志
3. **开启慢查询日志**
    1. MySQL的慢查询日志默认没有开启，我们可以查看一下系统变量 **`slow_query_log`**
    ```SQL
     show variables like 'slow_query_log';
    ```
    2. 如果要开启慢查询日志，需要在MySQL的配置文件（**`/etc/my.cnf`**）中配置如下信息
    ```SQL
     # linux下部署的MySQL
     vim /etc/my.cnf
    ```
    ```SQL
     # 添加
     # ⭐开启MySQL慢日志查询开关
     slow_query_log=1
     # ⭐设置慢日志的时间为2秒，SQL语句执行时间超过2秒，就会视为慢查询，记录慢查询日志
     long_query_time=2
    ```
    ```SQL
     systemctl restart mysqld
    ```
4. 配置完毕之后，通过以下指令重新启动MySQL服务器进行测试，查看慢日志文件中记录的信息：**`/var/lib/mysql/localhost-slow.log`**
    ```SQL
     # 再次查看
     systemctl restart mysqld
    ```
5. **示例：**
    1. 执行如下SQL语句
    ```SQL
     select * from tb_user; -- 这条SQL执行效率比较高, 执行耗时 0.00sec
     select count(*) from tb_sku; -- 由于tb_sku表中, 预先存入了1000w的记录, count一次,耗时13.35sec
    ```
    2. 检查慢查询日志：**==在慢查询日志中，只会记录执行时间超多我们预设时间（2s）的SQL，执行较快的SQL是不会记录的==**
    ```SQL
     cd /var/lib/mysql/
     cat localhost-slow.log
    ```
    ![[IMG-20260620224140673.png|800]]
# 三、explain 查看执行计划（分析慢 SQL 为什么慢）🙋‍♂️
![[IMG-20260620224146708.png|800]]
1. EXPLAIN 或者 DESC 命令**==获取 MySQL 如何执行==** **`SELECT`** **==语句的信息（执行计划）==**
2. ⭐**所谓执行计划就是**
    - ==**优化器会不会优化我们自己书写的SQL语句**==**（比如表的连接情况、表的连接顺序.）**
    - ==**优化器针对此条 SQL 的执行对哪些索引进行了成本估算，并最终决定采用哪个索引**==
    - ==**是否用到索引**==**（或者最终选择不用索引，而是全表扫描）**
    - ==**优化器对单表执行的策略是什么**==
3. 通过分析 **`EXPLAIN`** 输出的表中的信息，可以确定查询的瓶颈所在，从而优化查询语句的性能
4. 语法：
    ```SQL
     -- 直接在select语句之前加上关键字 explain / desc
     EXPLAIN SELECT 字段列表 FROM 表名 WHERE 条件 ;
    ```
    ![[IMG-20260620224156952.png|800]]
5. Explain 执行计划中各个字段的含义
    - 🟡**`id`**：**查询中每个操作的唯一标识符**，一般是一个数字，从上到下依次递增
    - 🟡**`select_type`**：查询的类型，常见的有 **`SIMPLE`**（简单查询）、**`PRIMARY`**（主查询）、**`SUBQUERY`**（子查询）等
    - 🟡**`table`**：显示此行数据是**关于哪个表的**
    - 🟡**`type`**：通过 type 列可以判断**是否使用了索引**，以及如何使用的
        - `system`、`const`：如果是对主键或唯一索引的等值查询
        - `eq_ref`：通常出现在多表查询中，关联的条件是某一张表的主键或唯一索引，type 的类型会为
        - `ref`：最常见的，对于非聚簇索引的单值查询一般都为 ref
        - `RANGE` ：范围扫描
        - `INDEX` ：索引扫描
        - `ALL` ：全表扫描
    - 🟡**`key`**：**实际使用的索引**，如果为 **`NULL`** 则没有使用索引
    - **`possible_keys`**：显示可能应用在这张表中的索引，一个或多个
    - **`key_len`**：表示索引中使用的字节数
    - **`ref`**：表示此表和前面表的连接条件
    - **`rows`**：估算的结果集行数，越小越好
    - **`Extra`**：包含 MySQL 解决查询的详细信息，常见的有 **`Using filesort`**（需要进行文件排序）、**`Using temporary`**（需要使用临时表）等
# 四、profile 查看详情
1. show profiles 能够在做SQL优化时**==帮助我们了解时间都耗费到哪里去了==**❓
2. 通过**`have_profiling`**参数，能够看到当前MySQL**是否支持profile操作**
    ```SQL
     SELECT @@have_profiling ;
    ```
    ![[IMG-20260620224203837.png|800]]
3. 开启 **profile 操作**
    1. 可以看到，当前MySQL是支持 profile操作的，但是开关是关闭的
    2. 可以通过set语句在**`session/global`**级别**开启profiling**
    ```SQL
     SET profiling = 1;
    ```
4. 开关已经打开了，接下来，我们所**==执行的SQL语句，都会被MySQL记录，并记录执行时间消耗到哪儿去了==**
    1. 直接执行如下的SQL语句
    ```SQL
     select * from tb_user;
     select * from tb_user where id = 1;
     select * from tb_user where name = '白起';
     select count(*) from tb_sku;
    ```
    2. 执行一系列的业务SQL的操作，然后通过如下指令查看指令的执行耗时
    ```SQL
     -- 查看每一条SQL的耗时基本情况
     show profiles;
     -- 查看指定query_id的SQL语句各个阶段的耗时情况
     show profile for query query_id;
     -- 查看指定query_id的SQL语句CPU的使用情况
     show profile cpu for query query_id;
    ```
    3. 查看每一条SQL的耗时情况
    ![[IMG-20260620224209666.png|800]]
    4. 查看指定SQL各个阶段的耗时情况
    ![[IMG-20260620224214410.png|800]]
