---
title: "1. Jedis"
tags:
  - "中间件"
  - "中间件/Redis"
  - "中间件/Redis/Redis Java API"
  - "Jedis"
  - "Redis Java API"
  - "Redis"
updated: 2026-04-16
---

# 一、Jedis 快速入门
1. 引入依赖

    ```XML
     <dependencies>
         <dependency>
             <groupId>redis.clients</groupId>
             <artifactId>jedis</artifactId>
             <version>3.7.0</version>
         </dependency>
         <dependency>
             <groupId>org.junit.jupiter</groupId>
             <artifactId>junit-jupiter</artifactId>
             <version>5.8.2</version>
             <scope>test</scope>
         </dependency>
     </dependencies>
    ```
2. 建立 Jedis 连接

    ```Java
     @BeforeEach
     void setUp(){
         jedis = new Jedis("192.168.10.151", 6379);
         jedis.auth("111111");
         jedis.select(0);
     }
    ```
3. 操作时要用的方法名称和命令中`Redis-cli`的操作方法名称一致

    ```Java
     @Test
     void testString(){
         String result = jedis.set("name", "cool");
         System.out.println("result = " + result);
         String name = jedis.get("name");
         System.out.println("name = " + name);
     }
     @Test
     void testHash(){
         jedis.hset("user:1","name","Jack");
         jedis.hset("user:1","age","29");
         Map<String, String> map = jedis.hgetAll("user:1");
         System.out.println(map);    //{name=Jack, age=29}
     }
    ```
4. 释放 Jedis 连接

    ```Java
     @AfterEach
     void tearDown(){
         if (jedis != null){
             jedis.close();
         }
     }
    ```
# 二、Jedis 连接池
1. Jedis本身是线程不安全的，并且频繁的创建和销毁连接会有性能损耗
2. 因此==**推荐使用 Jedis 连接池代替 Jedis 的直连方式**==
3. jedis 依赖

    ```XML
     <dependencies>
         <dependency>
             <groupId>redis.clients</groupId>
             <artifactId>jedis</artifactId>
             <version>3.7.0</version>
         </dependency>
         <dependency>
             <groupId>org.junit.jupiter</groupId>
             <artifactId>junit-jupiter</artifactId>
             <version>5.8.2</version>
             <scope>test</scope>
         </dependency>
     </dependencies>
    ```
4. ==**配置 jedis 连接池**==

    ```Java
     public class JedisConnectionFactory {
         private static final JedisPool JEDIS_POOL;
         static {
             //配置连接池
             JedisPoolConfig poolConfig = new JedisPoolConfig();
             poolConfig.setMaxTotal(8);  //最大连接数
             poolConfig.setMaxIdle(8);   //最大空闲连接
             poolConfig.setMinIdle(0);   //最小空闲连接
             poolConfig.setMaxWaitMillis(1000);  //最大等待时间
             //创建连接池对象
             JEDIS_POOL = new JedisPool(poolConfig,
                     "192.168.10.151",6379,1000,"111111");
         }
         public static Jedis getJedis(){
             return JEDIS_POOL.getResource();
         }
     }
    ```
5. 测试

    ```Java
     public class JedisTest {
         private Jedis jedis;
         @BeforeEach
         void setUp(){
     //        jedis = new Jedis("192.168.10.151", 6379);
             //⭐jedis对象不需要new了，而是从连接池里取
             jedis = JedisConnectionFactory.getJedis();
             jedis.auth("111111");
             jedis.select(0);
         }
         @Test
         void testString(){
             String result = jedis.set("name", "cool");
             System.out.println("result = " + result);
             String name = jedis.get("name");
             System.out.println("name = " + name);
         }
         @Test
         void testHash(){
             jedis.hset("user:1","name","Jack");
             jedis.hset("user:1","age","29");
             Map<String, String> map = jedis.hgetAll("user:1");
             System.out.println(map);    //{name=Jack, age=29}
         }
         @AfterEach
         void tearDown(){
             if (jedis != null){
                 jedis.close();
             }
         }
     }
    ```
