- [[#1. 代码示例]]
- [[#2. Jmeter 测试]]
# 1. 代码示例
- 修改根据 id 查询商铺的业务，基于**逻辑过期**方式来解决**缓存击穿**问题
    
    - ⭐**（针对热点 key）**由于**逻辑删除并不会对缓存设置TTL，故缓存是永远不会物理失效的，也就**==**不需要考虑缓存未命中的问题**==
        
        - 缓存一定会命中！
        
        - 真实环境中，需要在促销活动开始前，**将并发量高的参数数据，提前手动写入缓存**，活动过去后再手动删除
        
    
    - ⭐**且由于是逻辑失效，因此是可以拿到过期数据（**==**保证可用性**==**）的，**==**在 一致性 要求不高的场景下，直接将过期数据返回给用户**==
    
    - 缓存中的数据更新，由获得锁的那个线程所开启的异步线程，完成缓存重建后重新设置过期时间
    
![[IMG-20260405035438397.png|Untitled 3 179.png]]
1. RedisData
    
    ```Java
     @Data
     public class RedisData {
         private LocalDateTime expireTime;   //逻辑过期时间
         private Object data;                //redis存储的数据
     }
    ```
    
    - 用于封装数据，添加逻辑过期日期字段，如下：
        
        ![[IMG-20260405035457373.png|Untitled 543.png]]
        
    
1. ShopServiceImpl
    
    ```Java
     @Service
     public class ShopServiceImpl extends ServiceImpl<ShopMapper, Shop> implements IShopService {
     
         @Resource
         StringRedisTemplate stringRedisTemplate;
     
         /**
          *
          * @param: id 商铺id
          * @return: 返回给前端结果类
          */
         @Override
         public Result queryById(Long id) {
     
             //逻辑过期解决缓存击穿
             Shop shop = queryWithLogicalExpire(id);
     
             if (shop == null){
                 return Result.fail("店铺不存在！");
             }
             //返回数据
             return Result.ok(shop);
         }
     
         /**
          *
          * @param: key redis中锁的key
          * @Description: 获取锁
          */
         private boolean tryLock(String key){
             Boolean flag = stringRedisTemplate.opsForValue().setIfAbsent(key, "myLock", 10, TimeUnit.MINUTES);
             return BooleanUtil.isTrue(flag);
         }
     
         /**
          *
          * @param: key redis中锁的key
          * @Description: 释放锁
          */
         private void unLock(String key){
             stringRedisTemplate.delete(key);
         }
     
         /**
          * ⭐ 创建线程池
          */
         private static final ExecutorService CACHE_REBUILD_EXECUTOR = Executors.newFixedThreadPool(10);
     
         /**
          *⭐
          * @param: id 商铺id
          * @Description: 逻辑删除处理缓存单点击穿
          */
         private Shop queryWithLogicalExpire(long id){
             //1. 从Redis中查询商铺缓存
             String shopJson = stringRedisTemplate.opsForValue().get(RedisConstants.CACHE_SHOP_KEY + id);
     
             //2. 判断是否存在
             if (StrUtil.isBlank(shopJson)){
                 //2.1. 未命中，直接返回null
                 return null;
             }
             //2.2 缓存一定命中，需要先把json反序列化为对象
             RedisData redisData = JSONUtil.toBean(shopJson, RedisData.class);
             Shop shop = JSONUtil.toBean((JSONObject) redisData.getData(), Shop.class);
             LocalDateTime expireTime = redisData.getExpireTime();
    
             //3. 判断逻辑过期时间
             if (expireTime.isAfter(LocalDateTime.now())){
                 //3.1 未过期，直接返回店铺信息
                 return shop;
             }
             //3.2 过期，直接当前已经过期的店铺信息，并且需要缓存重建
    
             //4 缓存重建
             //4.1 获取互斥锁
             boolean isLock = tryLock(RedisConstants.LOCK_SHOP_KEY + id);
             //4.2 判断锁是否获取成功
             if (isLock){
                 //4.3 获取成功，开启独立线程，实现缓存重建
                 CACHE_REBUILD_EXECUTOR.submit(() -> {       //一个lamda表达式写的空参方法
                     try {
                         //重建缓存
                         this.saveShopRedis(id,30L);
                     } catch (Exception e) {
                         throw new RuntimeException(e);
                     }finally {
                         //finally 中完成锁的释放
                         unLock(RedisConstants.LOCK_SHOP_KEY + id);
                     }
                 });
             }
             //5. 执行到这里的都是过期，直接返回已经过期数据
             return shop;
         }
     
         /**
          * ⭐
          * @param: id  商铺id
          * @param: expireSeconds  预期的逻辑TTL
          * @throws: InterruptedException
          * @Description: 数据重建过程，即根据id查出shop，并封装成带有逻辑截至日期的redisData对象
          */
         public void saveShopRedis(Long id,Long expireSeconds) throws InterruptedException {
             //1. 查询店铺数据
             Shop shop = getById(id);
             Thread.sleep(200);
             //2. 封装逻辑过期时间
             RedisData redisData = new RedisData();
             redisData.setData(shop);
             // 逻辑过期时间 = 当前时间 + 期望的TTL
             redisData.setExpireTime(LocalDateTime.now().plusSeconds(expireSeconds));
             //3. 写入Redis
             stringRedisTemplate.opsForValue().set(RedisConstants.CACHE_SHOP_KEY + id,JSONUtil.toJsonStr(redisData));
         }
     }
    ```
    
# 2. Jmeter 测试
[http://localhost:8081/shop/1](http://localhost:8081/shop/1)
1. 设置线程组
1. 预先手动生成的缓存：
    
    1. 此时redis缓存中为空
    
    1. 先写测试方法测试一次 saveShopRedis 方法，生成一个缓存
        
        ![[IMG-20260405035457425.png|Untitled 1 403.png]]
        
    
1. 此时修改数据库中 shop 的名字为 我10001茶餐厅
1. 开启Jmeter测试
1. 查看此时redis中的缓存，缓存重建成功！
    
    ![[IMG-20260405035509329.png|Untitled 2 327.png]]
    
1. 查看Jmeter的监控树
    
    - 开始的请求中，返回的都是旧值 我的茶餐厅
        
        ![[IMG-20260405035509359.png|Untitled 3 246.png]]
        
    
    - ⭐**某一时刻，当缓存重建好后，返回的就是新值了 我10001茶餐厅；==符合重建缓存时先返回旧值，允许存在数据不一致的情况==**
        
        ![[IMG-20260405035517219.png|Untitled 4 188.png]]
        
    
1. 执行终端也显示，只执行了一次sql语句
    
    ![[IMG-20260405035524854.png|Untitled 5 153.png]]