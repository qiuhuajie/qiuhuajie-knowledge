---
title: "发送短信验证码"
tags:
  - "中间件"
  - "中间件/Redis"
  - "中间件/Redis/Redis"
  - "发送短信验证码"
  - "单系统登录"
  - "Redis"
updated: 2026-04-16
---

# 一、发送短信验证码
![[IMG-20260620224127733.png|800]]
1. UserController

    ```Java
     @Resource
     private IUserService userService;
     @Resource
     private IUserInfoService userInfoService;
     /**
      * 发送手机验证码
      */
     @PostMapping("code")
     public Result sendCode(@RequestParam("phone") String phone, HttpSession session) {
         // 发送短信验证码并保存验证码
         return userService.sendCode(phone,session);
     }
    ```
2. UserServiceImpl

    ```Java
     @Slf4j
     @Service
     public class UserServiceImpl extends ServiceImpl<UserMapper, User> implements IUserService {
         @Override
         public Result sendCode(String phone, HttpSession session) {
             //1. 校验手机号
             if (RegexUtils.isPhoneInvalid(phone)){
                 //2. 如果不符合，返回错误信息
                 return Result.fail("手机号格式错误！");
             }
             //3. 符合，生成验证码
             String code = RandomUtil.randomNumbers(6);
             //4. 保存验证码到 session
             session.setAttribute("code",code);
             //5. 发送验证码
             log.debug("发送验证码成功，验证码：{}", code);
             //返回 ok
             return Result.ok();
         }
     }
    ```
    - 这里的 `RandomUtil` 是hutool工具类提供的： `cn.hutool.core.util.RandomUtil` 生成一个6位随机数
3. 测试：
    1. 输入错误手机号

    ![[IMG-20260620224130810.png|512]]
    2. 输入正确手机号

    ![[IMG-20260620224136623.png|800]]
        成功生成验证码
![[IMG-20260620224140123.png|627]]
