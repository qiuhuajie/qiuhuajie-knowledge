---
title: "7. SortedSet 类型"
tags:
  - "中间件"
  - "中间件/Redis"
  - "中间件/Redis/Redis 数据结构及其相关命令"
  - "SortedSet 类型"
  - "Redis 数据结构及其相关命令"
  - "Redis"
updated: 2026-04-16
---

# 一、介绍
1. Redis的SortedSet是一个**可排序**的 set 集合
    1. 与Java中的 **==TreeSet==** 有些类似
    2. **但底层数据结构却差别很大**，TreeSet 底层是红黑树实现的[[Set 接口]]
2. **底层的实现是一个跳表（SkipList）+ hash 表**
3. SortedSet 具备下列特性：
    - **==可排序==**
        1. SortedSet 中的**每一个元素都带有一个`score`属性，可以**==**基于**==**`score`**==**属性对元素排序**==
        2. **因为 SortedSet 的可排序特性，经常被用来实现 排行榜 这样的功能**[[5.3 点赞排行榜]][[5.3 点赞排行榜]][[5.3 点赞排行榜]]
    - **==元素不重复==**
    - 查询速度快

    ![[IMG-20260620224127704.png|800]]

4. 示例：

    ```Shell
     127.0.0.1:6379> ZADD sorted.set.test 3 node01
     (integer) 1
     127.0.0.1:6379> ZADD sorted.set.test 4 node02
     (integer) 1
     127.0.0.1:6379> ZADD sorted.set.test 1 node03
     (integer) 1
    ```

    ![[IMG-20260620224129118.png|800]]

# 二、SortedSet 的常见命令
- **`ZADD`** key score member：**添加**一个或多个元素到sorted set ，如果已经存在则更新其score值
- **`ZREM`** key member：**删除**sorted set中的一个指定元素
- **`ZSCORE`** key member : 获取sorted set中的指定元素的 **score 值**
- **`ZRANK`** key member：获取sorted set 中的指定元素的**排名**
- **`ZRANGEBYSCORE`** key min max：**按照 score 排序后，获取指定score范围内的元素**[[基于 推模式 实现关注推送功能]]
- `ZCARD` key：获取sorted set中的元素个数
- `ZCOUNT` key min max：统计score值在给定范围内的所有元素的个数
- `ZINCRBY` key increment member：让sorted set中的指定元素自增，步长为指定的increment值
- `ZRANGE` key min max：按照score排序后，获取指定排名范围内的元素
- `ZDIFF`、`ZINTER`、`ZUNION`：求差集、交集、并集
# 三、案例练习
1. 将班级的下列学生得分存入Redis的SortedSet中：
    - Jack 85, Lucy 89, Rose 82, Tom 95, Jerry 78, Amy 92, Miles 76
2. 并实现下列功能：
    - 删除Tom同学
    - 获取Amy同学的分数
    - 获取Rose同学的排名
    - 查询80分以下有几个学生
    - 给Amy同学加2分
    - 查出成绩前3名的同学
    - 查出成绩80分以下的所有同学

    ```Shell
     127.0.0.1:6379> ZADD stus 85 Jack 89 Lucy 82 Rose 95 Tom 78 Jerry 92 Amy 76 Miles
     (integer) 7
     127.0.0.1:6379> ZREM stus Tom
     (integer) 1
     127.0.0.1:6379> ZSCORE stus Amy
     "92"
     127.0.0.1:6379> ZRANK stus Rose
     (integer) 2
     127.0.0.1:6379> ZREVRANK stus Rose
     (integer) 3
     127.0.0.1:6379> ZCARD stus
     (integer) 6
     127.0.0.1:6379> ZCOUNT stus 0 80
     (integer) 2
     127.0.0.1:6379> ZINCRBY stus 2 Amy
     "94"
     127.0.0.1:6379> ZINCRBY stus -2 Amy
     "92"
     127.0.0.1:6379> ZRANGE stus 0 2
     1) "Miles"
     2) "Jerry"
     3) "Rose"
     127.0.0.1:6379> ZREVRANGE stus 0 2
     4) "Amy"
     5) "Lucy"
     6) "Jack"
     127.0.0.1:6379> ZRANGEBYSCORE stus 0 80
     7) "Miles"
     8) "Jerry"
     127.0.0.1:6379>
    ```
