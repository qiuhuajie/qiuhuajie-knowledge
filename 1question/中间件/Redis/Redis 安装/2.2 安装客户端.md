1. 安装完成 Redis，我们就可以操作 Redis，实现数据的 CRUD 了
2. 需要用到 Redis 客户端，包括：

    - 命令行客户端

    - 图形化桌面客户端

    - 编程客户端

- [[#1. 命令行客户端]]
- [[#2. 图形化客户端]]
# 1. **命令行客户端**
1. redis 安装完成后就==**自带**====了命令行客户端：==**`redis-cli`**，使用方式如下：

    ```Shell
     redis-cli [options] [commonds]
    ```

    - 其中常见的 options 有：

        - `h 127.0.0.1`：指定要连接的redis节点的IP地址，默认是127.0.0.1

        - `p 6379`：指定要连接的redis节点的端口，默认是6379

        - `a 123321`：指定redis的访问密码


    - 其中的 commonds 就是Redis的操作命令，例如：

        - `ping`：与redis服务端做心跳测试，服务端正常会返回`pong`


2. 不指定commond时，会进入`redis-cli`的交互控制台：

    ```Shell
     [root@redis01 bin]# redis-cli
     127.0.0.1:6379> ping
     (error) NOAUTH Authentication required.     # 需要身份授权
     127.0.0.1:6379> AUTH 111111                 # 使用配置文件中设置的密码验证身份
     OK
     127.0.0.1:6379> ping                        # ping <---> PONG
     PONG
     127.0.0.1:6379> set name qhjqhj
     OK
     127.0.0.1:6379> get name
     "qhjqhj"
     127.0.0.1:6379> SELECT 2                    # redis 固定16个数据库不能修改，但可以切换使用哪个数据库
     OK
     127.0.0.1:6379[2]> set name coolcool
     OK
     127.0.0.1:6379[2]> get name
     "coolcool"
     127.0.0.1:6379[2]> SELECT 0
     OK
     127.0.0.1:6379> get name
     "qhjqhj"
    ```

# 2. **图形化客户端**
安装包：
https://github.com/lework/RedisDesktopManager-Windows/releases
![[Attachment/1question/中间件/Redis/Redis 安装/IMG-20260405035438337.png]]
---

![[%E5%9B%BE%E5%83%8F_2 2.png|%E5%9B%BE%E5%83%8F_2 2.png]]