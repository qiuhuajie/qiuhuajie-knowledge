全文检索查询，会对用户输入内容分词，常用于搜索框搜索：
![[IMG-20260405035438298.png|Untitled 190.png]]
全文检索查询有两种方式：
# 1. **单字段查询（推荐）**
1. `match`查询（==单字段查询==）：全文检索查询的一种，会对用户输入内容分词，然后去倒排索引库检索，语法：
![[IMG-20260405035450015.png|Untitled 1 128.png]]
1. **示例：在** `**FIELD**` **字段中查询** `**TEXT**`
    
    1. 会提示 hotel 索引库中的所有字段
        
        ![[IMG-20260405035507070.png|Untitled 2 107.png]]
        
    
    1. 由于之前添加的一个 `all` 字段，里面包含了所有内容，因此可以直接在 `all` 字段中查询
        
        ![[IMG-20260405035514209.png|Untitled 3 82.png]]
        
    
# 2. **多字段查询**
1. `multi_match`查询（==多字段查询==）：与match查询类似，只不过**允许同时查询多个字段**，语法：
    
    ![[IMG-20260405035522587.png|Untitled 4 68.png]]
    
1. **示例：在** `**FIELD1**`**、**`**FIELD12**` **字段中查询** `**TEXT**`
    
    ![[IMG-20260405035522608.png|Untitled 5 60.png]]
    

> [!important] ==**注意：**==
> 
> 参与搜索的字段越多，性能越差，因此推荐：将字段 `copy_to` 到一个字段 `all` 中，使用单字段 `match` 查询