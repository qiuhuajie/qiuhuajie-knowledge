# 1. 软件下载地址： [https://www.elastic.co/cn/downloads/past-releases/elasticsearch-7-8-0](https://www.elastic.co/cn/downloads/past-releases/elasticsearch-7-8-0)

![[IMG-20260405035438291.png|Untitled 178.png]]

# 2. Node-01 `192.168.10.191`
1. 解压软件
    
    ```Bash
     # 解压缩
     tar -zxvf elasticsearch-7.8.0-linux-x86_64.tar.gz -C /opt/module
     # 改名
     mv elasticsearch-7.8.0 es
    ```
    
1. 创建 es 用户
    
    ```Bash
     useradd es \#新增 es 用户
     passwd es \#为 es 用户设置密码
     userdel -r es \#如果错了，可以删除再加
     chown -R es:es /opt/module/es \#文件夹所有者
    ```
    
1. 修改配置文件
    
    > 使用ROOT用户
    
    1. 修改/opt/module/es/config/elasticsearch.yml 文件
        
        ```YAML
         # 加入如下配置
         \#集群名称
         cluster.name: my-es
         \#⭐节点名称， 每个节点的名称不能重复
         node.name: node-1
         \#⭐ip 地址， 每个节点的地址不能重复尚硅谷技术之 Elasticsearch
         network.host: 192.168.10.191
         \#是不是有资格主节点
         node.master: true
         node.data: true
         http.port: 9200
         # head 插件需要这打开这两个配置
         http.cors.allow-origin: "*"
         http.cors.enabled: true
         http.max_content_length: 200mb
         \#es7.x 之后新增的配置，⭐初始化一个新的集群时需要此配置来选举 master
         cluster.initial_master_nodes: ["node-1"]
         \#es7.x 之后新增的配置，节点发现 ⭐节点之间使用9300端口通信
         discovery.seed_hosts: ["192.168.10.191:9300","192.168.10.192:9300"]
         gateway.recover_after_nodes: 2
         network.tcp.keep_alive: true
         network.tcp.no_delay: true
         transport.tcp.compress: true
         \#集群内同时启动的数据任务个数，默认是 2 个
         cluster.routing.allocation.cluster_concurrent_rebalance: 16
         \#添加或删除节点及负载均衡时并发恢复的线程个数，默认 4 个
         cluster.routing.allocation.node_concurrent_recoveries: 16
         \#初始化数据恢复时，并发恢复线程的个数，默认 4 个
         cluster.routing.allocation.node_initial_primaries_recoveries: 16
        ```
        
    
    1. 修改/etc/security/limits.conf
        
        ```Bash
         # 在文件末尾中增加下面内容
         # 每个进程可以打开的文件数的限制
         es soft nofile 65536
         es hard nofile 65536
        ```
        
    
    1. 修改/etc/security/limits.d/20-nproc.conf
        
        ```Bash
         # 在文件末尾中增加下面内容
         # 每个进程可以打开的文件数的限制
         es soft nofile 65536
         es hard nofile 65536
        ```
        
    
    1. 修改/etc/sysctl.conf
        
        ```Bash
         # 操作系统级别对每个用户创建的进程数的限制
         * hard nproc 4096
         # 注： * 带表 Linux 所有用户名称
        ```
        
    
    1. 重新加载
        
        ```Bash
         sysctl -p
        ```
        
    
1. 启动 ES 服务
    
    > 使用 es 用户
    
    ```Bash
     cd /opt/module/es/
     \#启动
     bin/elasticsearch
     \#后台启动
     bin/elasticsearch -d
    ```
    
1. 测试：[http://192.168.10.191:9200/](http://192.168.10.191:9200/)
    

    ![[IMG-20260404031902680.png|Untitled 1 119.png]]

    
# 3. Node-02 `192.168.10.192`
1. 解压软件
    
    ```Bash
     # 解压缩
     tar -zxvf elasticsearch-7.8.0-linux-x86_64.tar.gz -C /opt/module
     # 改名
     mv elasticsearch-7.8.0 es
    ```
    
1. 创建 es 用户
    
    ```Bash
     useradd es \#新增 es 用户
     passwd es \#为 es 用户设置密码
     userdel -r es \#如果错了，可以删除再加
     chown -R es:es /opt/module/es \#文件夹所有者
    ```
    
1. 修改配置文件
    
    > 使用ROOT用户
    
    1. 修改/opt/module/es/config/elasticsearch.yml 文件
        
        ```YAML
         # 加入如下配置
         \#集群名称
         cluster.name: my-es
         \#⭐节点名称， 每个节点的名称不能重复
         node.name: node-2
         \#⭐ip 地址， 每个节点的地址不能重复尚硅谷技术之 Elasticsearch
         network.host: 192.168.10.192
         \#是不是有资格主节点
         node.master: true
         node.data: true
         http.port: 9200
         # head 插件需要这打开这两个配置
         http.cors.allow-origin: "*"
         http.cors.enabled: true
         http.max_content_length: 200mb
         \#es7.x 之后新增的配置，⭐初始化一个新的集群时需要此配置来选举 master
         cluster.initial_master_nodes: ["node-1"]
         \#es7.x 之后新增的配置，节点发现 ⭐节点之间使用9300端口通信
         discovery.seed_hosts: ["192.168.10.191:9300","192.168.10.192:9300"]
         gateway.recover_after_nodes: 2
         network.tcp.keep_alive: true
         network.tcp.no_delay: true
         transport.tcp.compress: true
         \#集群内同时启动的数据任务个数，默认是 2 个
         cluster.routing.allocation.cluster_concurrent_rebalance: 16
         \#添加或删除节点及负载均衡时并发恢复的线程个数，默认 4 个
         cluster.routing.allocation.node_concurrent_recoveries: 16
         \#初始化数据恢复时，并发恢复线程的个数，默认 4 个
         cluster.routing.allocation.node_initial_primaries_recoveries: 16
        ```
        
    
    1. 修改/etc/security/limits.conf
        
        ```Plain
         # 在文件末尾中增加下面内容
         # 每个进程可以打开的文件数的限制
         es soft nofile 65536
         es hard nofile 65536
        ```
        
    
    1. 修改/etc/security/limits.d/20-nproc.conf
        
        ```Plain
         # 在文件末尾中增加下面内容
         # 每个进程可以打开的文件数的限制
         es soft nofile 65536
         es hard nofile 65536
        ```
        
    
    1. 修改/etc/sysctl.conf
        
        ```Plain
         # 操作系统级别对每个用户创建的进程数的限制
         * hard nproc 4096
         # 注： * 带表 Linux 所有用户名称
        ```
        
    
    1. 重新加载
        
        ```Bash
         sysctl -p
        ```
        
    
1. 启动 ES 服务
    
    > 使用 es 用户
    
    ```Bash
     cd /opt/module/es/
     \#启动
     bin/elasticsearch
     \#后台启动
     bin/elasticsearch -d
    ```
    
# 4. 测试验证

[http://192.168.10.191:9200/_cat/nodes](http://192.168.10.191:9200/_cat/nodes)

![[IMG-20260405035449583.png|Untitled 2 100.png]]