# 1. **导入项目**
![[IMG-20260404031903873.png|Untitled 185.png]]
![[IMG-20260404031903967.png|Untitled 1 124.png]]
# 2. **导入数据库数据**
![[IMG-20260404031904018.png|Untitled 2 104.png]]
![[IMG-20260404031904053.png|Untitled 3 79.png]]
# 3. **mapping 分析**
1. 创建索引库，最关键的是mapping映射，而mapping映射要考虑的信息包括：
    
    - 字段名
    
    - 字段数据类型
    
    - 是否参与搜索
    
    - 是否需要分词
    
    - 如果分词，分词器是什么？
    
1. 其中：
    
    - **字段名、字段数据类型**，可以参考数据表结构的名称和类型
    
    - **是否参与搜索**要分析业务来判断，例如图片地址，就无需参与搜索
    
    - **是否分词**呢要看内容，内容如果是一个整体就无需分词，反之则要分词
    
    - **分词器**，我们可以统一使用ik_max_word
    
1. 因此我们创建的 `**hotel**` **索引库映射**如下
    
    ```JSON
     PUT /hotel
     {
       "mappings": {
         "properties": {
           "id": {
             "type": "keyword"
           },
           "name":{
             "type": "text",
             "analyzer": "ik_max_word",
             "copy_to": "all"
           },
           "address":{
             "type": "keyword",
             "index": false
           },
           "price":{
             "type": "integer"
           },
           "score":{
             "type": "integer"
           },
           "brand":{
             "type": "keyword",
             "copy_to": "all"
           },
           "city":{
             "type": "keyword",
             "copy_to": "all"
           },
           "starName":{
             "type": "keyword"
           },
           "business":{
             "type": "keyword"
           },
           "location":{
             "type": "geo_point"
           },
           "pic":{
             "type": "keyword",
             "index": false
           },
           "all":{
             "type": "text",
             "analyzer": "ik_max_word"
           }
         }
       }
     }
    ```
    
    > [!important] **location 地理坐标说明：**
    > 
    > - ES中支持两种地理坐标数据类型：
    >     
    >     1. `geo_point`：由纬度（latitude）和经度（longitude）确定的一个点。例如："32.8752345, 120.2981576"
    >     
    >     1. `geo_shape`：有多个geo_point组成的复杂几何图形。例如一条直线，"LINESTRING (-77.03653 38.897676, -77.009051 38.889939)"
    >     
    
    > [!important] **copy_to 说明：**
    > 
    > 1. 字段拷贝可以使用copy_to属性将当前字段拷贝到指定字段
    > 
    > 1. 示例：
    >     
    >     ![[IMG-20260404031904140.png|Untitled 4 66.png]]
    >     
    
# 4. **引入依赖**
1. 引入es的`RestHighLevelClient`依赖：
    
    ![[IMG-20260404031904221.png|Untitled 5 58.png]]
    
1. 因为SpringBoot默认的ES版本是`7.6.2`，所以我们需要覆盖默认的ES版本：
    
    ![[IMG-20260404031904250.png|Untitled 6 51.png]]
    
1. 初始化RestHighLevelClient：编写一个测试类 HotelIndexTest
# 5. **初始化 RestClient**
编写一个测试类 HotelIndexTest
```Java
 public class HotelIndexTest {
     private RestHighLevelClient client;
 
     @BeforeEach
     void setUp(){
         this.client = new RestHighLevelClient(RestClient.builder(HttpHost.create("http://192.168.10.123:9200")));
     }
 
     @AfterEach
     void tearDown() throws IOException {
         this.client.close();
     }
 
     @Test
     void testInit(){
         System.out.println(client);     //org.elasticsearch.client.RestHighLevelClient@2bec854f
     }
 }
```