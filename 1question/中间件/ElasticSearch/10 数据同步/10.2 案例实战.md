---
title: "10.2 案例实战"
tags:
  - "中间件"
  - "中间件/ElasticSearch"
  - "中间件/ElasticSearch/数据同步"
  - "案例"
  - "数据同步"
  - "Elasticsearch"
updated: 2026-04-16
---

# 一、⭐技术选择
1. 利用==**MQ**==实现 mysql 与 elasticsearch 数据同步
2. ==当酒店数据发生增、删、改时，要求对elasticsearch中数据也要完成相同操作==
3. rabbitmq 的设计：

    ![[IMG-20260620224143023.png|800]]

4. ⭐==数据同步的流程：==

    ![[IMG-20260620224148668.png|800]]

# 二、Rabbitmq 部署
# 三、项目导入
- `hotel-admin`项目作为酒店管理的微服务**（消息发送）**
- `hotel-demo`项目作为搜索的微服务**（消息监听）**
# 四、声明 交换机、队列、路由键
> hotel-demo项目
1. 依赖引入

    ```XML
     <dependency>
         <groupId>org.springframework.boot</groupId>
         <artifactId>spring-boot-starter-amqp</artifactId>
     </dependency>
    ```
2. spring配置mq

    ```YAML
     server:
       port: 8089
     spring:
       datasource:
         url: jdbc:mysql://localhost:3306/heima?useSSL=false
         username: root
         password: ZXT774276296qq..
         driver-class-name: com.mysql.cj.jdbc.Driver
       rabbitmq:
         host: 192.168.10.123
         port: 5672
         username: itcast
         password: 123321
         virtual-host: /
    ```
3. 命名常量类

    ```Java
     public class MqConstants {
         //交换机
         public final static String HOTEL_EXCHANGE = "hotel.topic";
         //监听新增和修改的队列
         public final static String HOTEL_INSERT_QUEUE = "hotel.insert.queue";
         //监听删除的队列
         public final static String HOTEL_DELETE_QUEUE = "hotel.delete.queue";
         //监听新增和修改的队列的路由键
         public final static String HOTEL_INSERT_KEY = "hotel.insert";
         //监听删除的队列的路由键
         public final static String HOTEL_DELETE_KEY = "hotel.delete";
     }
    ```
4. 声明 交换机、队列、路由键

    ```Java
     @Configuration
     public class MqConfig {
         @Bean
         public TopicExchange topicExchange(){
             return new TopicExchange(MqConstants.HOTEL_EXCHANGE, true, false);
         }
         @Bean
         public Queue insertQueue(){
             return new Queue(MqConstants.HOTEL_INSERT_QUEUE, true);
         }
         @Bean
         public Queue deleteQueue(){
             return new Queue(MqConstants.HOTEL_DELETE_QUEUE, true);
         }
         @Bean
         public Binding insertQueueBinding(){
             return BindingBuilder.bind(insertQueue()).to(topicExchange()).with(MqConstants.HOTEL_INSERT_KEY);
         }
         @Bean
         public Binding deleteQueueBinding(){
             return BindingBuilder.bind(deleteQueue()).to(topicExchange()).with(MqConstants.HOTEL_DELETE_KEY);
         }
     }
    ```
# 五、消息发送
- hotel-admin项目：在 controller 中当酒店数据发生增、删、改时，发送通知到 mq 中

    ```Java
     @RestController
     @RequestMapping("hotel")
     public class HotelController {
         @Autowired
         private RabbitTemplate rabbitTemplate;
         @Autowired
         private IHotelService hotelService;
         @GetMapping("/{id}")
         public Hotel queryById(@PathVariable("id") Long id){
             return hotelService.getById(id);
         }
         @GetMapping("/list")
         public PageResult hotelList(
                 @RequestParam(value = "page", defaultValue = "1") Integer page,
                 @RequestParam(value = "size", defaultValue = "1") Integer size
         ){
             Page<Hotel> result = hotelService.page(new Page<>(page, size));
             return new PageResult(result.getTotal(), result.getRecords());
         }
         @PostMapping
         public void saveHotel(@RequestBody Hotel hotel){
             hotelService.save(hotel);
             //⭐新建酒店后，给mq发送消息，通知更新ES数据
             rabbitTemplate.convertAndSend(MqConstants.HOTEL_EXCHANGE, MqConstants.HOTEL_INSERT_KEY, hotel.getId());
         }
         @PutMapping()
         public void updateById(@RequestBody Hotel hotel){
             if (hotel.getId() == null) {
                 throw new InvalidParameterException("id不能为空");
             }
             hotelService.updateById(hotel);
             //⭐修改酒店后，给mq发送消息，通知更新ES数据
             rabbitTemplate.convertAndSend(MqConstants.HOTEL_EXCHANGE, MqConstants.HOTEL_INSERT_KEY, hotel.getId());
         }
         @DeleteMapping("/{id}")
         public void deleteById(@PathVariable("id") Long id) {
             hotelService.removeById(id);
             //⭐删除酒店后，给mq发送消息，通知更新ES数据
             rabbitTemplate.convertAndSend(MqConstants.HOTEL_EXCHANGE, MqConstants.HOTEL_DELETE_KEY, id);
         }
     }
    ```
# 六、消息监听
- hotel-demo项目：使用 `@Bean` 加载 mq 的组件（也可以使用`@RabbitListener`加载组件）
1. mq 监听类：不同的队列调用不同的service方法

    ```Java
     @Component
     public class hotelListener {
         @Autowired
         private IHotelService hotelService;
         @RabbitListener(queues = MqConstants.HOTEL_INSERT_QUEUE)
         public void listenerHotelInsertOrUpdate(Long id){
             hotelService.InsertOrUpdateById(id);
         }
         @RabbitListener(queues = MqConstants.HOTEL_DELETE_QUEUE)
         public void listenerHotelDelete(Long id){
             hotelService.deleteById(id);
         }
     }
    ```
2. service

    ```Java
     @Service
     public class HotelService extends ServiceImpl<HotelMapper, Hotel> implements IHotelService {
         @Autowired
         private RestHighLevelClient client;
         @Override
         public void deleteById(Long id) {
             try {
                 //1. 创建 Request 对象
                 DeleteRequest request = new DeleteRequest("hotel", id.toString());
                 //2. 发送请求
                 client.delete(request, RequestOptions.DEFAULT);
             } catch (IOException e) {
                 throw new RuntimeException(e);
             }
         }
         @Override
         public void InsertOrUpdateById(Long id) {
             try {
                 //先从数据库中查出数据
                 Hotel hotel = getById(id);
                 //由于数据库里的字段与所需ES文档字段有差异，所以需要转换
                 HotelDoc hotelDoc = new HotelDoc(hotel);
                 //1. 创建 Request 对象
                 IndexRequest request = new IndexRequest("hotel").id(hotelDoc.getId().toString());
                 //2. 准备请求参数：JSON 文档内容
                 request.source(JSON.toJSONString(hotelDoc), XContentType.JSON);
                 //3. 发送请求
                 client.index(request, RequestOptions.DEFAULT);
             } catch (IOException e) {
                 throw new RuntimeException(e);
             }
         }
     }
    ```
# 七、测试
1. 启动两个项目
2. 查看 mq 中的交换机和队列的创建情况，以及绑定关系

    ![[IMG-20260620224153624.png|523]]

3. 在搜索服务`hotel_demo`中查看原先的数据

    ![[IMG-20260620224158586.png|800]]

4. 在酒店管理`hotel_adim`中做修改

    ![[IMG-20260620224204726.png|768]]

5. 再次在搜索服务`hotel_demo`中查看

    ![[IMG-20260620224210803.png|800]]

6. 查看 mq 消息是否由消息的转发

    ![[IMG-20260620224214936.png|534]]
