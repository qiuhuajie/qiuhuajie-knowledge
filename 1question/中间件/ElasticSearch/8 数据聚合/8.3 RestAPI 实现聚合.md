---
title: "8.3 RestAPI 实现聚合"
tags:
  - "中间件"
  - "中间件/ElasticSearch"
  - "中间件/ElasticSearch/数据聚合"
  - "RestAPI 实现聚合"
  - "数据聚合"
  - "Elasticsearch"
updated: 2026-04-16
---

> 💡 以品牌聚合为例，演示下
> **`Java`** 的 **`RestClient`** 使用
# 一、请求组装

语法解析：

![[IMG-20260620224143006.png|800]]

# 二、聚合结果解析

语法解析：

![[IMG-20260620224152808.png|800]]

# 三、案例实战
> 💡 需求：搜索页面的品牌、城市等信息不应该是在页面写死，而是通过聚合索引库中的酒店数据得来的
> ![[IMG-20260620224201181.png|800]]
> 1. 前端接口：
> ![[IMG-20260620224207339.png|571]]
> 2. 可以看到请求参数与之前search时的RequestParam完全一致，这是在限定聚合时的文档范围
>     1. 例如：用户搜索“外滩”，那聚合必须是在这个搜索条件基础上完成 **⭐（如果对所有数据做聚合，所有星钻级别都会显示，我搜上海，但上海没有二钻的酒店，这样显然不对）**
>     2. 因此我们需要：
>         1. 编写controller接口，接收该请求
>         2. 修改IUserService\#getFilters()方法，添加RequestParam参数
>         3. 修改getFilters方法的业务，聚合时添加query条件
1. HotelController

    ```Java
     @RestController
     @RequestMapping("/hotel")
     public class HotelController {
         @Autowired
         private HotelService hotelService;
         @PostMapping("/list")
         public PageResult search(@RequestBody RequestParams params){
             return hotelService.search(params);
         }
         //做聚合
         @PostMapping("/filters")
         public Map<String, List<String>> getFilters(@RequestBody RequestParams params){
             return hotelService.filter(params);
         }
     }
    ```
2. HotelService

    ```Java
     @Service
     public class HotelService extends ServiceImpl<HotelMapper, Hotel> implements IHotelService {
         @Autowired
         private RestHighLevelClient client;
         //根据品牌、城市、星级做聚合
         @Override
         public Map<String, List<String>> filter(RequestParams params) {
             try {
                 //1. 准备 request
                 SearchRequest request = new SearchRequest("hotel");
                 //2. 准备 DSL
                 //2.1 设置查询条件
                 buildBasicQuery(params,request);
                 //2.2 设置 size
                 request.source().size(0);
                 //2.3 聚合
                 buildAggregation(request);
                 //3. 发送请求
                 SearchResponse response = client.search(request, RequestOptions.DEFAULT);
                 //4. 解析响应
                 HashMap<String, List<String>> map = new HashMap<>();
                 Aggregations aggregations = response.getAggregations();
                 setMap(map, aggregations, "brand_agg", "brand");
                 setMap(map, aggregations, "city_agg", "city");
                 setMap(map, aggregations, "starName_agg", "starName");
                 return map;
             } catch (IOException e) {
                 throw new RuntimeException(e);
             }
         }
         //封装构建基本查询语句的代码
         private void buildBasicQuery(RequestParams params, SearchRequest request) {
             //2.1 query（条件过滤需要使用复合查询 boolQuery）
             //构建 boolQuery
             BoolQueryBuilder boolQuery = QueryBuilders.boolQuery();
             //关键字搜索（must）
             String key = params.getKey();
             if (key == null || key.equals("")) {
                 boolQuery.must(QueryBuilders.matchAllQuery());
             }else {
                 boolQuery.must(QueryBuilders.matchQuery("all", key));
             }
             //城市条件
             String city = params.getCity();
             if (city != null && !city.equals("")) {
                 boolQuery.filter(QueryBuilders.termQuery("city", city));
             }
             //品牌条件
             String brand = params.getBrand();
             if (brand != null && !brand.equals("")) {
                 boolQuery.filter(QueryBuilders.termQuery("brand", brand));
             }
             //星级条件
             String startName = params.getStartName();
             if (startName != null && !startName.equals("")) {
                 boolQuery.filter(QueryBuilders.termQuery("startName", startName));
             }
             //价格条件
             String maxPrice = params.getMaxPrice();
             String minPrice = params.getMinPrice();
             if (maxPrice != null && !maxPrice.equals("") && minPrice != null && !minPrice.equals("")) {
                 boolQuery.filter(QueryBuilders.rangeQuery("price").lte(maxPrice).gte(minPrice));
             }
             //算分控制
             FunctionScoreQueryBuilder functionScoreQuery = QueryBuilders.functionScoreQuery(
                     //参数1：原始查询，做相关性算法算分
                     boolQuery,
                     //参数2：functionScore，做算分控制
                     new FunctionScoreQueryBuilder.FilterFunctionBuilder[]{
                             new FunctionScoreQueryBuilder.FilterFunctionBuilder(
                                     //过滤条件
                                     QueryBuilders.termQuery("isAD", true),
                                     //算分函数
                                     ScoreFunctionBuilders.weightFactorFunction(10)
                             )
                     }
             );
             request.source().query(functionScoreQuery);
         }
         //封装设置聚合条件的代码
         private void buildAggregation(SearchRequest request) {
             request.source().aggregation(
                     AggregationBuilders.terms("brand_agg").field("brand").size(100)
             );
             request.source().aggregation(
                     AggregationBuilders.terms("city_agg").field("city").size(100)
             );
             request.source().aggregation(
                     AggregationBuilders.terms("starName_agg").field("starName").size(100)
             );
         }
         //封装设置map，用于返回数据
         private void setMap(HashMap<String, List<String>> map, Aggregations aggregations, String aggName, String key) {
             //4.1 根据名称获取聚合结果
             Terms name_agg = aggregations.get(aggName);
             //4.2 获取桶
             List<? extends Terms.Bucket> buckets = name_agg.getBuckets();
             //4.3 遍历桶
             ArrayList<String> list = new ArrayList<>();
             for (Terms.Bucket bucket : buckets) {
                 String s = bucket.getKeyAsString();
                 list.add(s);
             }
             map.put(key,list);
         }
     }
    ```
3. 测试：**搜索上海**
    1. 请求：

    ![[IMG-20260620224212959.png|652]]

    2. 响应：

    ![[IMG-20260620224216249.png|800]]

    3. 效果：

    ![[IMG-20260620224218628.png|800]]
