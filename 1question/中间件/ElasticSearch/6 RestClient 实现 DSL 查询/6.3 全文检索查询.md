---
title: "6.3 全文检索查询"
tags:
  - "中间件"
  - "中间件/ElasticSearch"
  - "中间件/ElasticSearch/RestClient 实现 DSL 查询"
  - "全文检索查询"
  - "RestClient 实现 DSL 查询"
  - "Elasticsearch"
updated: 2026-04-16
---

1. 全文检索的match和multi_match查询与match_all的API基本一致。差别是查询条件，也就是query的部分
2. 同样是利用QueryBuilders提供的方法：

    ![[IMG-20260620224142990.png|800]]
3. 代码示例：

    ```Java
     public class HotelSearchTest {
         private RestHighLevelClient client;
         @BeforeEach
         void setUp(){
             this.client = new RestHighLevelClient(RestClient.builder(HttpHost.create("http://192.168.10.123:9200")));
         }
         @AfterEach
         void tearDown() throws IOException {
             this.client.close();
         }
         @Test
         void testMatch() throws IOException {
             //1. 准备 request
             SearchRequest request = new SearchRequest("hotel");
             //2. 准备 DSL
             request.source().query(QueryBuilders.matchQuery("all","如家"));
     //        request.source().query(QueryBuilders.multiMatchQuery("如家","name","business"));
             //3. 发送请求
             SearchResponse response = client.search(request, RequestOptions.DEFAULT);
             //4. 解析响应
             handleResponse(response);
         }
         private void handleResponse(SearchResponse response) {
             //4.1 获取并打印总结果数
             SearchHits searchHits = response.getHits();
             long total = searchHits.getTotalHits().value;
             System.out.println("共搜索到" + total + "条记录");
             //4.2 获取数组文档
             SearchHit[] hits = searchHits.getHits();
             // 遍历
             for (SearchHit hit : hits) {
                 // 获取 json 文档
                 String source = hit.getSourceAsString();
                 // 反序列化
                 HotelDoc hotelDoc = JSON.parseObject(source, HotelDoc.class);
                 System.out.println("hotelDoc = " + hotelDoc);
             }
         }
     }
    ```
