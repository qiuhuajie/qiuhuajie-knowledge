---
title: "6.1 RestClient DSL API"
tags:
  - "中间件"
  - "中间件/ElasticSearch"
  - "中间件/ElasticSearch/RestClient 实现 DSL 查询"
  - "RestClient 实现 DSL 查询"
  - "Elasticsearch"
  - "RestClient"
updated: 2026-04-16
---

# 一、基本步骤
查询的基本步骤是：
1. 创建`SearchRequest`对象

    ```Java
     SearchRequest request = new SearchRequest("...");
    ```
2. 准备`Request.source()`（也就是DSL中 JSON 最外边的大括号）

    ![[Attachment/1question/中间件/ElasticSearch/6 RestClient 实现 DSL 查询/IMG-20260620224142985.png|800]]
    1. 构造查询条件：
        1. `QueryBuilders`来构建查询条件

    ```Java
     QueryBuilders.xxxQuery()
    ```
        2. 传入`Request.source()`的 `query()`方法

    ```Java
     request.source().query(QueryBuilders.xxxxQuery());
    ```
    2. 设置查询结果的处理：`highlighter()`、`sort()`等，与`query()`同级

    ```Java
     request.source().highlighter(...);
    ```
3. 发送请求，得到结果

    ```Java
     SearchResponse response = client.search(request, RequestOptions.DEFAULT);
    ```
4. 解析结果（参考JSON结果，从外到内，逐层解析）
# 二、构建 DSL
RestAPI中其中构建DSL是通过==**HighLevelRestClient中的**==`==**resource()**==`**来实现的**，其中包含了查询、排序、分页、高亮等所有功能：
![[IMG-20260620224151314.png|693]]
# 三、构建查询条件
RestAPI中其中构建查询条件的核心部分是由一个名为==**QueryBuilders**==**的工具类**提供的，其中包含了各种查询方法：
![[IMG-20260620224200432.png|610]]
