---
title: "2.4 堆"
tags:
  - "堆内存大小"
  - "堆内存诊断"
  - "垃圾回收"
  - "堆内存"
  - "堆_Dump"
  - "JVM"
updated: 2026-04-16
---
- [[#一、介绍]]
- [[#二、堆内存大小]]
- [[#三、堆内存诊断]]

# 一、介绍

![[IMG-20260620003924673.png|555]]

1. 堆（Heap）
    1. 供所有==**类实例对象**====和====**数组对象**==分配内存的区域
    2. 通过 `new` 关键字创建对象都会使用堆内存，几乎所有实例对象都在会这里进行内存的分配
2. 堆内存是==线程共享==的，堆中对象都需要考虑线程安全的问题
3. 堆内存涉及垃圾回收机制
# 二、堆内存大小
1. 可以在运行代码时，通过虚拟机指令来指定：
    - `Xmx`：最大堆大小 `Xmx8m`
    - `Xms`：初始堆大小
    - `Xmn`：年轻代大小
2. 堆内存溢出问题：
    1. 演示代码

        ```Java
         public class heapDemo1 {
                 public static void main(String[] args) {
                     int i = 0;
                     try {
                         List<String> list = new ArrayList<>();
                         String a = "hello";
                         while (true) {
                             list.add(a); // hello, hellohello, hellohellohellohello ...
                             a = a + a;  // hellohellohellohello
                             i++;
                         }
                     } catch (Throwable e) {
                         e.printStackTrace();
                         System.out.println(i);
                     }
                 }
         }
        ```
    2. 运行结果：堆空间内存溢出 `OutOfMemoryError: Java heap space`

        ![[IMG-20260620003924765.png|758]]

# 三、堆内存诊断
1. 诊断演示：
    1. 代码

        ```Java
         public class heapDemo2 {
             /**
              * 演示堆内存
              */
             public static void main(String[] args) throws InterruptedException {
                 System.out.println("1...");
                 Thread.sleep(30000);
                 //new 一个10M的数组
                 byte[] array = new byte[1024 * 1024 * 10]; // 10 Mb
                 System.out.println("2...");
                 Thread.sleep(20000);
                 //数组引用设为null，也就意味着内存区域可以被当作垃圾回收掉
                 array = null;
                 System.gc();        //垃圾回收
                 System.out.println("3...");
                 Thread.sleep(1000000L);
             }
         }
        ```
    2. 诊断过程：
        1. 终端输入 `jps` 找到代码运行的线程

            ![[IMG-20260620003924872.png|398]]

        2. 使用 `jmap` 查看堆内存使用情况：开始空闲大小为

            ![[IMG-20260620003924899.png|549]]

            ![[IMG-20260620003924995.png|372]]

        3. 等控制台打印 2... 后，表示**数组创建**成功，再次使用 `jmap` 查看堆内存使用情况

            ![[IMG-20260620003925162.png|374]]

        4. 等控制台打印 3... 后，表示**垃圾回收**成功，再次使用 `jmap` 查看堆内存使用情况

            ![[IMG-20260620003925196.png|374]]

        5. 也可使用 `jconsole` 来用图形界面进行连续监控
            1. 运行代码，终端输入 `jconsole`

                ![[IMG-20260620003925361.png|458]]

            2. 选择线程 ➡️ 不安全的连接

                ![[IMG-20260620003925505.png|372]]

                - 可以看到堆内存的使用情况，由new数组时的增加，到垃圾回收后的骤减
    3. **Java VisualVM**
        1. 也可以看到堆内存大小的起伏

            ![[IMG-20260620003925608.png|975]]

        2. 也可以点击 **堆 Dump** 查看堆内存中**所有实例的信息**
            ![[IMG-20260620003925637.png|975]]