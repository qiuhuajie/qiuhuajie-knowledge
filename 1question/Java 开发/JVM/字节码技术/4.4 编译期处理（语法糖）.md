---
title: "4.4 编译期处理（语法糖）"
tags:
  - "集合"
  - "可变参数"
  - "swich_字符串"
  - "switch_枚举"
  - "foreach_循环"
  - "泛型集合的存值"
updated: 2026-04-16
---
- [[#一、语法糖]]
- [[#二、默认构造器]]
- [[#三、自动拆装箱]]
- [[#四、泛型集合的存值、取值]]
- [[#五、可变参数]]
- [[#六、foreach 循环]]
    - [[#1. 数组]]
    - [[#2. 集合]]
- [[#七、swich 字符串]]
- [[#八、switch 枚举]]
- [[#九、枚举类]]
- [[#十、try-with-resources]]
- [[#十一、匿名内部类]]
    - [[#1. 普通匿名内部类]]
    - [[#2. 引入局部变量的匿名内部类]]

# 一、语法糖
1. 所谓的 语法糖 ，其实就是指 java 编译器把 `.java` 源码编译为 `.class` 字节码的过程中，==自动生成和转换的一些代码==
2. 主要是**为了减轻程序员的负担**，算是 java 编译器给我们的一个额外福利（给糖吃嘛）
3. 另外，编译器**转换的结果直接就是 class 字节码**，只是为了便于阅读，给出了 几乎等价 的 java 源码方式，并不是编译器还会转换出中间的 java 源码，切记
# 二、默认构造器
1. 简化写法：

    ```Java
     public class Candy1 {
     }
    ```
2. 编译成class 后的**等效代码**
    - 这个无参构造是编译器帮助我们加上的
    - 即调用父类 Object 的无参构造方法，即调用 `java/lang/Object."<init>":()V`

    ```Java
     public class Candy1 {
         public Candy1() {
             super();
         }
     }
    ```
# 三、自动拆装箱
1. 简化写法：

    ```Java
     public class Candy2 {
         public static void main(String[] args) {
             Integer x = 1;
             int y = x;
         }
     }
    ```
    - **装箱**

        ![[IMG-20260620013612841.png|531]]

    - **拆箱**

        ![[IMG-20260620013612863.png|436]]

2. 在基本类型和包装类型之间来回转换（尤其是集合类中操作的都是包装类型），因此这些转换的事情在 JDK 5 以后都由编译器在编译阶段完成
3. 编译成class 后的**等效代码**

    ```Java
     public class Candy2 {
         public static void main(String[] args) {
             //装箱
             Integer x = Integer.valueOf(1);
             //拆箱
             int y = x.intValue();
         }
     }
    ```
# 四、泛型集合的存值、取值
1. 泛型也是在 JDK 5 开始加入的特性，但 java 在编译泛型代码后会执行 **泛型擦除** 的动作，即**泛型信息在编译为字节码之后就丢失了，实际的类型都当做了 Object 类型来处理**
2. 简化写法：

    ```Java
     public class Candy3 {
         public static void main(String[] args) {
             List<Integer> list = new ArrayList<>();
             list.add(10); // 实际调用的是 List.add(Object e)
             Integer x = list.get(0); // 实际调用的是 Object obj = List.get(int index);
         }
     }
    ```
    - 在取值时，编译器真正生成的字节码中，还要额外做一个类型转换的操作：

        ```Java
         // 需要将 Object 转为 Integer
         Integer x = (Integer)list.get(0);
        ```
3. 生成的字节码：
    - `add` 前自动将 10 装箱
    - `add` 实际调用的是 List.add(Object e)
    - `get` 实际调用的是 Object obj = List.get(int index)
    - `get` 后自动拆箱，checkcast 会做类型转换

    ![[IMG-20260620013613134.png|625]]

# 五、可变参数
1. 简化写法：

    ```Java
     public class Candy4 {
         public static void foo(String... args) {
             String[] array = args; // 直接赋值
             System.out.println(array);
         }
         public static void main(String[] args) {
             foo("hello", "world");
         }
     }
    ```
2. 可变参数 `String... args` 其实是一个 `String[] args` ，从代码中的赋值语句中就可以看出来
3. 同样 java 编译器会在编译期间将上述代码变换为**等效代码**：

    ```Java
     public class Candy4 {
         public static void foo(String[] args) {
             String[] array = args; // 直接赋值
             System.out.println(array);
         }
         public static void main(String[] args) {
             foo(new String[]{"hello", "world"});
         }
     }
    ```
    > 注意：如果调用了 foo() 则等价代码为 foo(new String[]{}) ，创建了一个空的数组，而不会传递 null 进去

# 六、foreach 循环
## 1. 数组
1. 简化写法：

    ```Java
     public class Candy5_1 {
         public static void main(String[] args) {
             int[] array = {1, 2, 3, 4, 5}; // 数组赋初值的简化写法也是语法糖哦
             for (int e : array) {
                 System.out.println(e);
             }
         }
     }
    ```
2. 编译成class 后的**等效代码**

    ```Java
     public class Candy5_1 {
         public Candy5_1() {
         }
         public static void main(String[] args) {
             int[] array = new int[]{1, 2, 3, 4, 5};
             for(int i = 0; i < array.length; ++i) {
                 int e = array[i];
                 System.out.println(e);
             }
         }
     }
    ```
## 2. 集合
1. 简化写法：

    ```Java
     public class Candy5_2 {
         public static void main(String[] args) {
             List<Integer> list = Arrays.asList(1,2,3,4,5);
             for (Integer i : list) {
                 System.out.println(i);
             }
         }
     }
    ```
2. 编译成 class 后的**等效代码**

    ```Java
     public class Candy5_2 {
         public Candy5_2() {
         }
         public static void main(String[] args) {
             List<Integer> list = Arrays.asList(1, 2, 3, 4, 5);
             Iterator iter = list.iterator();
             while(iter.hasNext()) {
                 Integer e = (Integer)iter.next();
                 System.out.println(e);
             }
         }
     }
    ```
    > 注意：foreach 循环写法能够配合数组，以及所有实现了 Iterable 接口的集合类一起使用
    > 其中 Iterable 用来获取集合的迭代器 Iterator

# 七、swich 字符串
1. 简化写法：

    ```Java
     public class Candy6_1 {
         public static void choose(String str) {
             switch (str) {
                 case "hello": {
                     System.out.println("h");
                     break;
                 }
                 case "world": {
                     System.out.println("w");
                     break;
                 }
             }
         }
     }
    ```
2. 编译成class 后的**等效代码**

    ```Java
     public class Candy6_1 {
         public Candy6_1() {
         }
         public static void choose(String str) {
             byte x = -1;
             switch (str.hashCode()) {
                 case 99162322: // hello 的 hashCode
                     if (str.equals("hello")) {
                         x = 0;
                     }
                     break;
                 case 113318802: // world 的 hashCode
                     if (str.equals("world")) {
                         x = 1;
                     }
             }
             switch (x) {
                 case 0:
                     System.out.println("h");
                     break;
                 case 1:
                     System.out.println("w");
             }
         }
     }
    ```
    - 可以看到，执行了两遍 switch，第一遍是根据字符串的 hashCode 和 equals 将字符串的转换为相应 byte 类型，第二遍才是利用 byte 执行进行比较
    - 为什么第一遍时必须既比较 hashCode，又利用 equals 比较呢？hashCode 是为了提高效率，减少可能的比较；而 equals 是为了防止 hashCode 冲突，例如 **BM 和 C. 这两个字符串的hashCode值都是 2123** 如下代码

        ![[IMG-20260620013613268.png|617]]

        会被编译器转换为**等效代码**：

        ![[IMG-20260620013613285.png|583]]

# 八、switch 枚举
1. 简化写法：

    ![[IMG-20260620013613608.png|670]]

2. 编译成class 后的**等效代码**

    ![[IMG-20260620013613839.png|657]]

# 九、枚举类
1. 简化写法：

    ![[IMG-20260620013614082.png|725]]

2. 编译成 class 后的**等效代码**

    ![[IMG-20260620013614279.png|652]]

# 十、try-with-resources
1. 简化写法：

    ![[IMG-20260620013614429.png|645]]

2. 编译成class 后的**等效代码**

    ![[IMG-20260620013614587.png|625]]

# 十一、匿名内部类
## 1. 普通匿名内部类
1. 简化写法：

    ![[IMG-20260620013615187.png|653]]

2. 编译成class 后的**等效代码**

    ![[IMG-20260620013615289.png|654]]

    ![[IMG-20260620013614740.png|695]]

## 2. 引入局部变量的匿名内部类
1. 简化写法：

    ![[IMG-20260620013614767.png|722]]

2. 编译成class 后的**等效代码**

    ![[IMG-20260620013614884.png|713]]