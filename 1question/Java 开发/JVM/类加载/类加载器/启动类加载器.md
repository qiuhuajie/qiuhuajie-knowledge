1. 用 `-Xbootclasspath/a:.` 来模拟 Bootstrap 类加载器加载类
1. 加载的类：
    
    ```Java
    public class F {
    	static {
    		System.out.println("bootstrap F init");
    	}
    }
    ```
    
1. 执行的代码：
    
    ```Java
    public class Load5_1 {
    	public static void main(String[] args) throws ClassNotFoundException {
    		Class<?> aClass = Class.forName("cn.itcast.jvm.t3.load.F");
    		System.out.println(aClass.getClassLoader());
    	}
    }
    ```
    
1. 执行命令
    
    ```Bash
    E:\git\jvm\out\production\jvm>java -Xbootclasspath/a:. cn.itcast.jvm.t3.load.Load5
    ```
    
1. 输出
    
    ```Bash
    bootstrap F init
    null //如果类加载器打印为 null 则表示是启动类加载器
    ```
    
    > [!important] 1.
    > 
    > `Xbootclasspath` 表示设置 bootclasspath
    > 
    > 1. 其中 `/a:.` 表示将当前目录追加至 bootclasspath 之后
    > 
    > 1. 三种替换方式：
    >     
    >     1. `java -Xbootclasspath:<new bootclasspath>`
    >     
    >     1. `java -Xbootclasspath/a:`<后追加路径>
    >     
    >     1. `java -Xbootclasspath/p:`<前追加路径>
    >     
    > 
    > 1. ==用作底层的 JVM 核心类的替换，开发 JVM 的人才会去用==