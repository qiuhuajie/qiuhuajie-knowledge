> [!important] 要进行垃圾回收，最为重要的一个问题是：
> 
> **判断谁是垃圾？**
- [[#1. 引用计数法 ⛔ ]]
- [[#2. 可达性分析法 GC Root Tracing ✅]]
# 1. 引用计数法 ⛔
1. 思想：
    
    1. 日常生活中，如果一个东西经常没被使用，那么这个对象可以说就是垃圾
    
    1. 在 Java 中也是如此，如果一个对象不可能再被引用，那么这个对象就是垃圾，应该被回收
    
1. **引用计数法**
    
    - 在一个对象被引用时加一，被去除引用时减一
    
    - 这样就可以==通过判断==**==引用计数是否为零==**==来判断一个对象是否为垃圾==
    
1. 存在的问题
    
    1. 引用计数法虽然简单，但是其存在一个致命的问题，那就是**循环引用**
        
        ![[Attachment/1question/大数据/Java 开发/JVM/垃圾回收/IMG-20260405035413780.png|Untitled 287.png]]
        
    
    1. A 引用了 B，B 引用了 A，它们各自的引用计数都为 1
    
    1. 但是**它们两个对象却从未被其他对象引用，只有它们自身互相引用**
    
    1. 从垃圾的判断思想来看，它们两个确实是不被其他对象引用的，但是此时它们的引用计数却不为零
    
# 2. **可达性分析法** GC Root Tracing ✅
1. GC Root Tracing 算法：现今的 Java 虚拟机判断垃圾对象使用的算法
1. **算法思想**
    
    - 从 GC Root 出发，==扫描堆中所有对象==
        
        - 所有==可达==（有引用直接或简介指向）的对象都是存活的对象
        
        - 而所有==不可达==的对象都是垃圾
        
    
    ![[IMG-20260404031755399.jpeg|Untitled 14.jpeg]]
    
1. 🙋‍♂️**哪些对象可以作为 GC Root ？**
    
    1. **GC Root 就是经过精心挑选的**==**一组活跃引用**==**，**==**这些引用肯定是存活的**==**。那么**==**通过这些引用延伸到的对象，自然也一定是存活的**==
    
    1. 通常包括：
        
        1. `System Class`：**Java 的系统类对象**，在运行期间肯定会用到的，如 Object，String，HashMap...，以及**异常对象**和**系统类加载器**
        
        1. `Busy Monitor`：**被加了锁的对象**
        
        1. `Thread`：**运行中的线程使用的对象（**Java 类中定义的**引用类型的静态变量、在虚拟机栈中引用的对象）**
        
        1. `Native Stack`：Java 虚拟机调用的操作系统的方法所引用的对象
        
    
1. 当对象被标记为可回收后，可以人为地控制让其不会被回收
    
    1. 当对象不可达（可回收）并发生 GC 时，会先判断对象是否执行了 `finalize` 方法，如果未执行，则会先执行 `finalize` 方法！
    
    1. 所以在 `finalize` 方法中，可以将当前对象与 GC Root 相关联
    
1. 可以通过 MAT 工具查看 GC Root，下载页面：
    
    > [!info] Eclipse Memory Analyzer Open Source Project | The Eclipse Foundation  
    > The Eclipse Foundation - home to a global community, the Eclipse IDE, Jakarta EE and over 415 open source projects, including runtimes, tools and frameworks.  
    > [https://www.eclipse.org/mat/previousReleases.php](https://www.eclipse.org/mat/previousReleases.php)  
    
    1. 在代码运行时执行：对当前的 Java 堆内存空间打快照，并以二进制形式存在文件中
        
        ```Bash
         D:\Projects\ideaProjects\JVM_Demo\out\production\jvm>jps
         10592
         12048
         9168 Jps
         10520 Launcher
         2328 gcDemo1
         
         D:\Projects\ideaProjects\JVM_Demo\out\production\jvm>jmap -dump:format=b,live,file=1.bin 2328
         
         D:\Projects\ideaProjects\JVM_Demo\out\production\jvm>jmap -dump:format=b,live,file=2.bin 2328
        ```
        
    
    1. 使用MAT 工具打开上面存的快照文件，可以过滤查看打快照那一时刻的 GC Root 对象
        
        ![[IMG-20260404031755458.png|Untitled 1 216.png]]
        
        ![[Attachment/1question/Java 开发/JVM/垃圾回收/IMG-20260405035421956.png|Untitled 2 181.png]]
        
    
    > [!info] https://mp.weixin.qq.com/s?__biz=MzI0NDc3ODE5OQ==&mid=2247490838&idx=1&sn=285fe163f9cd776e982d0757c39a408e&chksm=e959c8aede2e41b82d8a194454fe0c01bd970a9d7267532a6ed059d5ce62b6c9d625cb30f695&scene=178&cur_album_id=2153190488940642309\#rd  
    >  
    > [https://mp.weixin.qq.com/s?__biz=MzI0NDc3ODE5OQ==&mid=2247490838&idx=1&sn=285fe163f9cd776e982d0757c39a408e&chksm=e959c8aede2e41b82d8a194454fe0c01bd970a9d7267532a6ed059d5ce62b6c9d625cb30f695&scene=178&cur_album_id=2153190488940642309#rd](https://mp.weixin.qq.com/s?__biz=MzI0NDc3ODE5OQ==&mid=2247490838&idx=1&sn=285fe163f9cd776e982d0757c39a408e&chksm=e959c8aede2e41b82d8a194454fe0c01bd970a9d7267532a6ed059d5ce62b6c9d625cb30f695&scene=178&cur_album_id=2153190488940642309#rd)