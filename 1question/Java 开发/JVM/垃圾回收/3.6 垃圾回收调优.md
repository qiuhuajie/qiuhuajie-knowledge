1. GC 只是应用程序调优的一个方向，但是是影响比较明显的一个调优方向，尽量减少 STW
1. 其他调优方向：内存、锁竞争、CPU 占用、IO，只有各个方面综合起来，才能对应用程序有一个大的提升
1. 垃圾回收的调优目标：
    
    1. 科学计算的应用程序：注重高吞吐量，适合使用 并行回收器 Parallel GC
    
    1. 网络应用程序：注重低延迟，适合使用 CMS、G1、ZGC
    
# 1. **先要优化代码**
![[IMG-20260405035413783.png|Untitled 291.png]]
# 2. **新生代 GC 调优**
1. 新生代的特点
    
    1. 所有的 `new` 操作，这个对象首先会在 `Eden` 区分配，这个分配速度是非常快的
        
        > 每个线程都会在 Eden 区给它分配一个私有的区域：TLAB 全称 thread-local allocation buffer，以解决线程内存分配时的线程安全问题
        
    
    1. 死亡对象的回收代价是零
    
    1. 新生代中的对象 **98% 是朝生夕死的**，因此大新生代中大部分的对象，大概率不会存活很久
    
    1. `Minor GC` 的时间远远低于 `Full GC`
    
1. 如何给新生代进行 GC 调优
    
    1. 将**新生代大小**上调，但不是越大越好 `Xmn`
        
        - 新生代能容纳所有【并发量 * (请求-响应)】的数据
        
        - 幸存区大到能保留【当前活跃对象+需要晋升对象】
        
    
    1. **晋升年龄阈值**配置得当，让长时间存活对象尽快晋升
        
        - `XX:MaxTenuringThreshold=threshold`
        
        - `XX:+PrintTenuringDistribution`
        
    
# 3. **老年代 GC 调优**

> 以 CMS 为例
1. CMS 如果在并发清理阶段，用户线程会产生的新的垃圾对象，如果此时又导致内存（老年代）不足，造成 CMS 并发失败，那么 CMS 就会**退化为老年代串行回收器**，效率会大大降低，因此 CMS 的老年代内存越大越好
1. 遇到问题，查看日志
    
    1. 如果没有 Full GC，那么就先不要去调试老年代，要先尝试调优新生代
    
    1. 观察如果发生 Full GC 时，老年代内存占满，可以将老年代内存预设调大 `1/4 ~ 1/3`
        
        - `XX:CMSInitiatingOccupancyFraction=percent`
        
    
# 4. **案例**

> [!important] 案例1：
> 
> **Full GC 和 Minor GC频繁**
> 
> 1. 分析：可能是由于新生代区空间太小，`Eden` 区容易占满，导致 Minor GC 频繁，`Survivor` 区小，由于放不下那么多的幸存对象，导致很多生命周期短的对象，也会由于内存不足晋升到老年代，老年代存放的对象数量会大大增加，从而导致 Full GC 也很频繁
> 
> 1. 解决方法：
>     
>     1. 上调新生代的内存大小
>     
>     1. 提高晋升年龄阈值，让短生命周期的对象，尽可能得留在新生代的`Survivor` 区
>     

> [!important] 案例2：
> 
> **请求高峰期发生 Full GC，单次暂停时间特别长 （CMS）**
> 
> 1. 分析：可以在 GC 日志中查看是 CMS 哪个阶段时间较长

> [!important] 案例3：
> 
> **老年代充裕情况下，发生 Full GC （CMS jdk1.7）**
> 
> 1. JDK 1.7 之前：在**永久代（方法区的实现 见 2.5）**分配空间但已经没有足够空间时，也会触发 Full GC
>     
>     > JDK 1.7 之后：方法区的实现是元空间，是一块系统内存，回收并不受 Java 控制
>     
> 
> 1. 因此，可以试着调高永久代的内存大小