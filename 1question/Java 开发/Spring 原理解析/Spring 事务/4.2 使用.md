# 4.2 使用

![Untitled](IMG-20260620230837818.png)

> 💡 在 Spring Boot 中操作事务有两种方式：
> - 编程式事务
> - 声明式事务

# **1. 编程式事务（复杂**🙅**）**

## 1.1 介绍

在 Spring Boot 中实现编程式事务又有两种实现方法：

1. 使用 TransactionTemplate 对象实现编程式事务
2. 使用更加底层的 TransactionManager 对象实现编程式事务

## **1.2 TransactionTemplate 使用**

1. 要使用 TransactionTemplate  对象需要先将 TransactionTemplate 注入到当前类中
2. 然后再使用它提供的 execute 方法执行事务并返回相应的执行结果
3. 如果程序在执行途中出现了异常，那么就可以使用代码手动回滚事务
4. 具体实现代码如下：

    ![640 (21).png](IMG-20260620230837867.png)

## **1.3 TransactionManager 使用**

1. TransactionManager 实现编程式事务相对麻烦一点，它需要使用两个对象：TransactionManager 的子类，加上 TransactionDefinition 事务定义对象
2. 再通过调用 TransactionManager 的 getTransaction 获取并开启事务
3. 然后调用 TransactionManager 提供的 commit 方法提交事务，或使用它的另一个方法 rollback 回滚事务
4. 它的具体实现代码如下：

    ![640 (22).png](IMG-20260620230837946.png)

# **2. 声明式事务** ☑️

1. 从上述代码可以看出，**使用编程式事务更加灵活，但写法比较麻烦**
2. 声明式事务的实现比较简单，**只需要在方法上或类上添加 `@Transactional` 注解即可**
    1. **当加入了 `@Transactional` 注解就可以实现在方法执行前，自动开启事务；在方法成功执行完，自动提交事务**
    2. **如果方法在执行期间，出现了异常，那么它会自动回滚事务**
3. 它的具体使用如下：

    ![640 (23).png](IMG-20260620230838000.png)

4. **`@Transactional`** 注解的参数

    ![640 (24).png](IMG-20260620230838060.png)

    1. 使用事务，不是简单地使用 `@Transaction` 注解就可以，**还需要根据业务场景，选择合适的传播类型**
    2. ⭐一般来说，**使用 Spring 事务的步骤为**：
        1. **根据业务场景，分析要达成的事务效果，确定使用的事务传播类型**
        2. **在 Service 层使用 `@Transaction` 注解，配置对应的 `propogation` 属性**