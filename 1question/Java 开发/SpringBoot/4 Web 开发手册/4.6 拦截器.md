- [[#1. 简介]]
- [[#2. 使用示例：实现校验登陆状态]]
- [[#3. 拦截路径的设置]]
- [[#4. 原理]]
# 1. **简介**
1. 拦截器类似于 Servlet 开发中的过滤器 Filter，用于对处理器进行**预处理**和**后处理**
1. 开发者可以自己定义一些拦截器来实现特定的功能
1. 使用的设计模式
    
    1. **拦截器是AOP思想的具体应用**，而 AOP 是动态代理的具体应用
    
    1. 职责链模式
    
1. ⭐==**过滤器与拦截器的区别：**==
    
    1. **出身不同**：过滤器来自于 Servlet，而拦截器来自于 Spring 框架
    
    1. **触发时机不同**
        
        1. **请求的执行顺序是：请求进入容器 > 进入过滤器 > 进入 Servlet > 进入拦截器 > 执行控制器（Controller）**，如下图所示：
            
            ![[IMG-20260405035413871.png|640_(1) 2.png]]
            
            从图中可以看出 HandlerInteceptor 可以配置多个，其中任何一个返回 false 的话，请求都将被拦截，直接返回
            
        
        1. 也可以理解为 HandlerInterceptor 提供了更细粒度的拦截：**Filter 拦截的对象是 Serlvet，而 HandlerInterceptor 拦截的则是 Handler(Controller)**
        
        1. 所以过滤器和拦截器的执行时机也是不同的，**过滤器会先执行，然后才会执行拦截器，最后才会进入真正的要调用的方法**
        
    
    1. **实现不同**
        
        1. **过滤器是基于方法回调实现的**，当我们要执行下一个过滤器或下一个流程时，需要调用 FilterChain 对象的 doFilter 方法进行回调执行，如下图所示：
            
            ![[IMG-20260404031833731.png|640_(2) 2.png]]
            
        
        1. 而**拦截器是基于动态代理（底层是反射）实现的**，它的实现如下图所示：
            
            ![[IMG-20260405035420269.png|640_(3) 2.png]]
            
        
    
    1. **支持的项目类型不同**
        
        1. 过滤器是 Servlet 规范中定义的，所以**过滤器要依赖 Servlet 容器，它只能用在 Web 项目中**
        
        1. 而**拦截器是 Spring 中的一个组件，因此拦截器既可以用在 Web 项目中，同时还可以用在 Application 或 Swing 程序中**
        
    
    1. **使用的场景不同**
        
        1. 因为拦截器更接近业务系统，所以**拦截器主要用来实现项目中的业务判断的。**比如：登录判断、权限判断、日志记录等业务
        
        1. 而**过滤器通常是用来实现通用功能过滤的**，比如：敏感词过滤、字符集编码设置、响应数据压缩等功能
        
    
# 2. **使用**示例：**实现校验登陆状态**
1. 之前 2.5.2 中的问题 2：
    
    1. 问题：不登陆，直接访问 [http://localhost:8080/main.html](http://localhost:8080/main.html) 就能到达页面
    
    1. 解决：在 @GetMapping("/main.html") 中写判断当前是否登录的逻辑**（判断 session 中的loginUser）**
    
1. 其实，**更好的解决办法是**==**添加一个拦截器**==**，拦截非法（未登录状态）的请求；当访问非法的请求时，拦截器中的拦截逻辑就会生效**
    
    [[校验登录状态]]
    
    > [!important] ⭐但是这样实现的登录是基于session的，如果在高可用的情况下，有多台tomcat它们之间的session是不共享的，故需要
    > 
    > [[基于 Redis 实现共享 session 登录]]
    
1. springBoot 提供的拦截器接口：HandlerInterceptor
    
    ```Java
     public interface HandlerInterceptor {
    
         //handler 处理之前操作
         default boolean preHandle(HttpServletRequest request, HttpServletResponse response, Object handler) throws Exception {
             return true;
         }
     
         //handler 处理之后操作
         default void postHandle(HttpServletRequest request, HttpServletResponse response, Object handler, @Nullable ModelAndView modelAndView) throws Exception {
         }
     
         //页面渲染完后的一些操作（如：清理操作）
         default void afterCompletion(HttpServletRequest request, HttpServletResponse response, Object handler, @Nullable Exception ex) throws Exception {
         }
     }
    ```
    
1. **如何实现：**
    
    1. 编写一个拦截器，实现 HandlerInterceptor 接口
        
        ```Java
         @Slf4j
         public class LoginIntercepter implements HandlerInterceptor {
        
             //在处理前写拦截功能：preHandle
             @Override
             public boolean preHandle(HttpServletRequest request, HttpServletResponse response, Object handler) throws Exception {
                 String requestURI = request.getRequestURI();
                 log.info("拦截的请求路径是{}",requestURI);
         
                 HttpSession session = request.getSession();
                 Object loginUser = session.getAttribute("loginUser");
                 if (loginUser != null){
                     return true;
                 }else {
                     request.setAttribute("msg","未登录，请先登录");
                     //转发 或 重定向 到登陆页面
                     request.getRequestDispatcher("/").forward(request,response);
                                 //response.sendRedirect("/");
                     return false;
                 }
             }
         
             @Override
             public void postHandle(HttpServletRequest request, HttpServletResponse response, Object handler, ModelAndView modelAndView) throws Exception {
                 HandlerInterceptor.super.postHandle(request, response, handler, modelAndView);
             }
         
             @Override
             public void afterCompletion(HttpServletRequest request, HttpServletResponse response, Object handler, Exception ex) throws Exception {
                 HandlerInterceptor.super.afterCompletion(request, response, handler, ex);
             }
         }
        ```
        
    
    1. 在配置类中，将上面的拦截器 注册到 WebMvcConfigurer 容器中
        
        ```Java
         @Configuration
         public class AdminWebConfig implements WebMvcConfigurer {
             @Override
             public void addInterceptors(InterceptorRegistry registry) {
                 registry.addInterceptor(new LoginIntercepter())
                         .addPathPatterns("/**")
                     .excludePathPatterns("/","/login","/css/**","/fonts/**","/images/**","/js/**");
             }
         }
        ```
        
        1. **重写 addInterceptors()**
        
        1. **registry.addPathPatterns(拦截的请求路径)**
        
        1. **registry.excludePathPatterns(放行的请求路径)**
        
    
    1. 原先的 controller 判断逻辑取消掉
        
        ```Java
         @GetMapping("/main.html")
             public String mainPage(HttpSession session,Model model){
                 return "main";
             }
        ```
        
    
    1. 访问：当没有登录的情况下，访问拦截页面是，指定转发到 /或 /login
        
        ![[IMG-20260405035421989.png|Untitled 329.png]]
        
    
# 3. **拦截路径的设置**
1. 在**registry.addPathPatterns(拦截的请求路径)** 中一般写拦截全部请求 `**/****`
    
    1. `/admin/*` ：拦截的是/admin/add等等这种 , 但是 /admin/add/user不会被拦截
    
    1. `/admin/**` ：拦截的是/admin/下的所有
    
1. 这会导致静态资源也会被拦截
    
    ![[IMG-20260405035432113.png|Untitled 1 247.png]]
    
1. 需要在**registry.excludePathPatterns(放行的请求路径)**中放行静态资源路径
    
    ```Java
     .excludePathPatterns("/","/login","/css/**","/fonts/**","/images/**","/js/**");
    ```
    
    ```HTML
     页面中的静态资源都是以static下的子文件夹开头的：
     <link th:href="@{/css/style.css}" rel="stylesheet">
    ```
    
    放行后，就不再拦截静态资源了：
    
    ![[IMG-20260405035444315.png|Untitled 2 209.png]]
    
# 4. **原理**
1. 根据当前请求，找到 HandlerExecutionChain
    
    其中包含了可以处理请求的 handler 以及该 handler 的所有拦截器
    
    ![[IMG-20260405035504685.png|Untitled 3 159.png]]
    
1. 先来 **顺序执行** 所有拦截器的 preHandle方法
    
    > ⭐拦截器的执行顺序可以在配置类中注册拦截器时，根据order()来设置，值越小越早执行
    > 
    > ```Java
    >  @Configuration
    >  public class MvcConfig implements WebMvcConfigurer {
    >   @Override
    >   public void addInterceptors(InterceptorRegistry registry) {
    >       registry.addInterceptor(new LoginInterceptor()).order(1);
    >  
    >       registry.addInterceptor(new RefreshTokenIntercepter(stringRedisTemplate)).addPathPatterns("/**").order(0);
    >   }
    >  }
    > ```
    
    1. 如果当前拦截器prehandler返回为true：则执行下一个拦截器的preHandle
    
    1. 如果当前拦截器返回为false：直接**倒序执行**所有已经执行了的拦截器的 afterCompletion
    
    1. **如果任何一个拦截器返回false：直接跳出不执行目标方法**
    
1. 所有拦截器都返回 True：执行目标方法
1. 倒序执行所有拦截器的postHandle方法
1. 前面的步骤有**任何异常**都会直接**倒序**触发 afterCompletion
1. **页面成功渲染完成**以后，也会**倒序**触发 afterCompletion
    
    ![[IMG-20260405035511519.png|Untitled 4 130.png]]
    
    ![[IMG-20260405035426821.png|Untitled 5 108.png]]
    
    ![[IMG-20260405035426848.png|Untitled 6 89.png]]