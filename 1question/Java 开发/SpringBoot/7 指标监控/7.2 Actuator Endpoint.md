- [[#1. 管理 Endpoint]]
    - [[#1.1 开启与禁用Endpoints]]
    - [[#1.2 ⭐暴露 Endpoints]]
- [[#2. 常使用的端点]]
- [[#3. Health Endpoint]]
- [[#4. Metrics Endpoint]]
# 1. **管理 Endpoint**
## 1.1 **开启与禁用Endpoints**
1. 默认所有的 Endpoint （除 shutdown）都是开启的
1. 开启或者禁用某个Endpoint，配置格式为 `***management.endpoint.<endpointName>.enabled = true**`
    
    ```Plain
     management:
       endpoint:
         beans:
           enabled: true
    ```
    
1. 也可以禁用所有端点，再开启指定的端点：
    
    ```Plain
     management:
       endpoints:
         enabled-by-default: false # 默认禁用所有端点
       endpoint:
         beans:
           enabled: true
         health:
           enabled: true
    ```
    
## 1.2 **⭐暴露 Endpoints**
1. 支持的暴露方式
    
    - HTTP：默认只暴露 health 和 info 端点
    
    - JMX（Java Management Extensions，即Java管理扩展）：默认暴露所有端点
    
    - 除了 health 和 info，剩下的Endpoint都应该进行保护访问（如果引入SpringSecurity，则会默认配置安全访问规则）
    
1. **以 JMX 的形式 暴露所有端点 监控信息**
    
    1. 配置：
        
        ```Plain
         management:
           endpoints:
             jmx:
               exposure:
                 include: '*'
        ```
        
    
    1. 使用 jconsole 打开
        
        ![[IMG-20260404031836831.png|Untitled 339.png]]
        
    
    1. 查看暴露的所有端点：
        
        ![[IMG-20260404031836913.png|Untitled 1 256.png]]
        
    
1. **以 http 的形式 暴露**==**指定端点**==**监控信息**
    
    ```Plain
     management:
       endpoints:
         web:
           exposure:
             include: 'health,info,bus-refresh'
    ```
    
    [http://localhost:3344/actuator](http://localhost:3344/actuator) **（这里 3344 是 springcloud 学习时配的 springcloud config 服务端）详见 SpringCloud笔记 11.3**
    
    ![[IMG-20260405035420282.png|Untitled 2 214.png]]
    
1. **以 http 的形式 暴露**==**所有端点**==**监控信息**
    
    ```Plain
     management:
       endpoints:
         web:
           exposure:
             include: '*'
    ```
    
    [http://localhost:3344/actuator](http://localhost:3344/actuator)
    
    ![[IMG-20260405035421997.png|Untitled 3 163.png]]
    
1. **查看以 http 形式暴露的端点的** ==**详细信息**==
    
    ```Plain
     management:
       endpoints:
         web:
           exposure:
             include: '*'
       endpoint:
         health:
           show-details: always \#总是显示详细信息。可显示每个模块的状态信息
    ```
    
    [http://localhost:3344/actuator/health](http://localhost:3344/actuator/health)
    
    ![[IMG-20260405035432257.png|Untitled 4 133.png]]
    
# 2. **常使用的端点**
|   |   |
|---|---|
|**ID**|**描述**|
|`**auditevents**`|暴露当前应用程序的审核事件信息。需要一个`**AuditEventRepository组件**`。|
|`**beans**`|显示应用程序中所有Spring Bean的完整列表。|
|`**caches**`|暴露可用的缓存。|
|`**conditions**`|显示自动配置的所有条件信息，包括匹配或不匹配的原因。|
|`**configprops**`|显示所有`**@ConfigurationProperties**`。|
|`**env**`|暴露Spring的属性`**ConfigurableEnvironment**`|
|`**flyway**`|显示已应用的所有Flyway数据库迁移。 需要一个或多个`**Flyway**`组件。|
|⭐`**health**`|显示应用程序运行状况信息。|
|`**httptrace**`|显示HTTP跟踪信息（默认情况下，最近100个HTTP请求-响应）。需要一个`**HttpTraceRepository**`组件。|
|⭐`**info**`|显示应用程序信息。|
|`**integrationgraph**`|显示Spring `**integrationgraph**` 。需要依赖`**spring-integration-core**`。|
|`**loggers**`|显示和修改应用程序中日志的配置。|
|`**liquibase**`|显示已应用的所有Liquibase数据库迁移。需要一个或多个`**Liquibase**`组件。|
|⭐`**metrics**`|显示当前应用程序的“指标”信息。|
|`**mappings**`|显示所有`**@RequestMapping**`路径列表。|
|`**scheduledtasks**`|显示应用程序中的计划任务。|
|`**sessions**`|允许从Spring Session支持的会话存储中检索和删除用户会话。需要使用Spring Session的基于Servlet的Web应用程序。|
|`**shutdown**`|使应用程序正常关闭。默认禁用。|
|`**startup**`|显示由`**ApplicationStartup**`收集的启动步骤数据。需要使用`**SpringApplication**`进行配置`**BufferingApplicationStartup**`。|
|`**threaddump**`|执行线程转储。|
# 3. **Health Endpoint**
1. 健康检查端点，我们一般用于在云平台，平台会**定时的检查应用的健康状况**，我们就需要Health Endpoint可以**为平台返回当前应用的一系列组件健康状况的集合**
    
    [http://localhost:3344/actuator/health](http://localhost:3344/actuator/health)
    
    ![[IMG-20260405035444489.png|Untitled 5 111.png]]
    
1. 重要的几点：
    
    - health endpoint 返回的结果，应该是一系列健康检查后的一个汇总报告
    
    - 很多的健康检查默认已经自动配置好了，比如：数据库、redis等
    
    - 可以很容易的添加自定义的健康检查机制
    
# 4. **Metrics Endpoint**
1. 提供详细的、层级的、空间指标信息，这些信息可以被 pull（主动推送）或者push（被动获取）方式得到
    
    - 通过Metrics对接多种监控系统
    
    - 简化核心Metrics开发
    
    - 添加自定义Metrics或者扩展已有Metrics
    
1. 示例：
    
    1. [http://localhost:3344/actuator/metrics](http://localhost:3344/actuator/metrics)
        
        ![[IMG-20260405035504836.png|Untitled 6 92.png]]
        
    
    1. 上面的只能 查看概况，如果像具体看哪个，如查看 JVM 内存的使用情况：[http://localhost:3344/actuator/metrics/jvm.memory.used](http://localhost:3344/actuator/metrics/jvm.memory.used)（把具体的监控项 再拼在请求中）
        
        ![[IMG-20260405035511619.png|Untitled 7 77.png]]