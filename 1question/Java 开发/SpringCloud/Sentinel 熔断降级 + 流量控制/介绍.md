---
title: "介绍"
tags:
  - "熔断降级"
  - "Spring_Cloud"
  - "Sentinel"
  - "让请求"
  - "请求发生堆积"
  - "堆内存"
updated: 2026-04-16
---

# 一、简介
1. Sentinel 以**流量为切入点**，从**流量控制、熔断降级、系统负载保护**等多个维度保护服务的稳定性
2. Sentinel 的主要特性

    ![[IMG-20260405035400236.png|800]]

3. Sentinel 分为两个部分：
    - **核心库（Java 客户端）**不依赖任何框架/库，能够运行于所有 Java 运行时环境，同时对 Dubbo / Spring Cloud 等框架也有较好的支持
    - **控制台（Dashboard）**基于 Spring Boot 开发，打包后可以直接运行，不需要额外的 Tomcat 等应用容器
4. 官网：
    > ℹ️ 介绍
    > A powerful flow control component enabling reliability, resilience and monitoring for microservices.
    > [https://github.com/alibaba/Sentinel/wiki/介绍](https://github.com/alibaba/Sentinel/wiki/介绍)

    https://github.com/alibaba/Sentinel

# 二、Sentinel 两大功能 ⭐
## 1. 流量控制
1. 解决的问题：
    - 任意时间到来的请求往往是随机不可控的，而系统的处理能力是有限的
    - 我们==需要==**==根据系统的处理能力对流量进行控制==**
2. Sentinel 作为一个调配器，它用于调整网络包的发送数据，可以根据需要**把随机的请求调整成合适的形状**

    ![[IMG-20260405035413899.gif|800]]

3. 流量控制有以下几个角度:
    - **资源的调用关系**，例如资源的调用链路，资源和资源之间的关系
    - **运行指标**，例如 QPS、线程池、系统负载等
    - **控制的效果**，例如直接限流、冷启动、排队等

## 2. 熔断降级
1. **Sentinel 还会**==**及时对调用链路中的不稳定因素进行熔断**==
2. 解决的问题
    1. 一个服务常常会调用别的模块，可能是另外的一个远程服务、数据库，或者第三方 API 等。例如，支付的时候，可能需要远程调用银联提供的 API
    2. 然而，这个==**被依赖服务的稳定性是不能保证的**==
        1. 如果依赖的服务出现了不稳定的情况，请求的响应时间变长，那么调用服务的方法的响应时间也会变长，线程会产生堆积
        2. 最终可能耗尽业务自身的线程池，服务本身也变得不可用
    3. 总结，==由于调用关系的复杂性，如果调用链路中的====**某个资源出现了不稳定**====，可能会导致====**请求发生堆积**====，进而导致====**级联错误**==

    ![[IMG-20260405035401484.png|800]]

3. ⭐ **Sentinel 和 Hystrix 的熔断降级原则是一致的**
    1. 当检测到调用链路中某个资源出现不稳定的表现，例如请求响应时间长或异常比例升高的时候，则对这个资源的调用进行限制
    2. **让请求**==**快速失败**==**，（拉闸断电），然后**==**调用服务降级的兜底方法**==**并**==**返回友好提示**==**，避免影响到其它的资源而导致**==**级联故障**==
    3. 服务熔断的流程：3步
        1. 服务的降级
        2. 熔断
        3. 恢复调用链路
