---
title: "熔断降级规则"
tags:
  - "熔断降级"
  - "Spring_Cloud"
  - "熔断降级规则"
  - "RT"
  - "断路器"
  - "Sentinel"
updated: 2026-04-16
---

# 一、基本介绍
1. Sentinel 熔断降级会在调用链路中**某个资源出现不稳定状态时**（例如调用超时或异常比例升高），**对这个资源的调用进行限制，让请求快速失败**，**避免影响到其它的资源而导致级联错误**
2. 当资源被降级后，在接下来的**降级时间窗口之内**，对该资源的调用都自动熔断（默认行为是抛出 DegradeException）
3. **Sentinel 的断路器是没有半开状态**的
    > [[Hystrix 断路器]]：系统自动去检测是否请求有异常，没有异常就关闭断路器恢复使用，有异常则继续打开断路器不可用

# 二、降级策略
> 所有的降级策略都只在 时间窗口 内有效，超过时间窗口后，降级关闭
## 1. RT（平均响应时间，秒级）
1. 触发降级的条件：==**平均响应时间（个/s）超出阈值 且在时间窗口内通过的请求 >= 5**==

    ![[IMG-20260405035414065.png|800]]

2. RT最大4900（更大的需要通过-Dcsp.sentinel.statistic.max.rt=XXXX才能生效）
3. 配置：

    ![[IMG-20260404031845944.png|752]]

## 2. 异常比列（秒级）
1. 触发降级的条件：**==每秒请求量 >= 5 且 每秒异常总数占通过量的比值超过阈值==**

    ![[IMG-20260405035420398.png|693]]

2. 配置：

    ![[IMG-20260405035422121.png|755]]

## 3. 异常数（分钟级）
1. 触发降级的条件：**==当资源近 1 分钟的异常数目超过阈值之后会进行熔断==**
2. 时间窗口一定要大于等于 60s，否则结束熔断状态后仍可能在进入熔断（异常数是按早分钟数统计的）

    ![[IMG-20260405035435179.png|698]]

3. 配置：

    ![[IMG-20260405035435199.png|752]]
