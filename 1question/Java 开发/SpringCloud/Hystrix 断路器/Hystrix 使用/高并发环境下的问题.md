---
title: "高并发环境下的问题"
tags:
  - "Spring_Cloud"
  - "高并发环境下的问题"
  - "熔断降级"
  - "不再秒响应"
  - "线程"
  - "流量控制"
updated: 2026-04-16
---

# 一、问题
1. 上述在非高并发情形下，还能勉强满足，但是在高并发的情况下。。。**使用 JMeter 模拟高压环境**
2. **服务提供者端压测**
    1. **让 2000 个线程同时访问 普通业务（秒响应）**

    ![[IMG-20260405035414021.png|800]]

    2. **测试结果：**
        1. **访问：**[**http://localhost:8001/payment/hystrix/ok/31**](http://localhost:8001/payment/hystrix/ok/31)
        2. **不再是立刻响应了，竟然也转起圈，转完才响应数据**
        3. **原因：tomcat的默认的工作线程数被打满 了，没有多余的线程来分解压力和处理**

    ![[IMG-20260404031839945.png|800]]

3. **服务消费端压测**
    1. **让 2000 个线程同时访问 普通业务（秒响应）**
    2. **测试结果：**
        1. **不再秒响应：**

    ![[IMG-20260405035420389.png|800]]

        2. **甚至访问超时：**

    ![[IMG-20260405035427578.png|800]]

# 二、问题原因
1. 高并发环境下，出现故障的原因：
    1. 8001 同一层次的其它接口服务被困死，因为 tomcat 线程池里面的工作线程已经被挤占完毕
    2. 80 此时调用 8001，客户端访问响应缓慢，转圈圈

# 三、如何解决
1. **正因为有上述故障或不佳表现才有 降级/容错/限流等技术诞生**
2. 情况1：
    - 超时导致服务器变慢（转圈）
    - 解决：对方服务**(8001)超时**了，调用者(80)不能一直卡死等待，必须有服务降级**（请求超时不再等待）**
3. 情况2：
    - 出错（宕机或程序运行出错）
    - 解决：对方服务**(8001)down机**了，调用者(80)不能一直卡死等待，必须有服务降级**（请求的服务出错要有兜底的处理逻辑）**
4. 情况3：
    - 对方服务(8001)OK（2s），但是调用者**(80) 自己出故障或有自我要求**（要求：1s），即调用者自己的等待时间小于服务提供者
    - 解决：自己处理降级
