- [[#1. 编码和解码的基本介绍]]
- [[#2. Netty 的编解码器]]
- [[#3. 解码器 ]]
    - [[#3.1 ByteToMessageDecoder]]
    - [[#3.2 ReplayingDecoder]]
    - [[#3.3 其它解码器]]
# 1. 编码和解码的基本介绍
1. 编写网络应用程序时，因为数据在网络中传输的都是二进制字节码数据，在发送数据时就需要编码，接收数据时就需要解码
    
    ![[Attachment/1question/大数据/Java 开发/Netty/Netty 编解码器/IMG-20260405035413815.png|Untitled 373.png]]
    
1. `codec`（编解码器）的组成部分有两个：
    
    - `decoder`(解码器)：负责把业务数据转换成字节码数据
    
    - `encoder`(编码器)：负责把字节码数据转换成业务数据
    
# 2. Netty 的编解码器
1. 当 Netty 发送或者接受一个消息的时候，就将会发生一次数据转换
    
    - 入站消息会被解码，从字节转换为另一种格式（比如java 对象）
    
    - 如果是出站消息，它会被编码成字节
    
1. Nety 提供一系列实用的编解码器，他们都实现了 `ChannelInboundHadnler` 或者 `ChannelOutboundHandler` 接口
1. ⭐**数据的流动过程：以入站为例**
    
    1. 数据在 `socket` 中是以二进制字节码的形式存在，接收方会用 `chanmelRead()` 方法将数据取出
    
    1. 数据接收方收到数据后，会由 `pipeline` 链中第一个 `ChannelInboundHadnler` 处理器处理，一般为解码处理器
    
    1. 在这些解码器类中，负责读取数据的 `chanmelRead()` 方法已经被重写了，而负责解码的方法 `decode()` 是一个抽象方法，需要具体解码器重写
        
        ![[IMG-20260404031816334.png|Untitled 1 281.png]]
        
        例如：
        
        ![[IMG-20260405035420205.png|Untitled 2 234.png]]
        
    
    1. 调用由解码器所提供的 `decode()` 方法进行解码后，将已经解码的字节转发给 `ChannelPipeline` 中的下一个 `ChannelInboundHandler`
    
# 3. 解码器
## 3.1 ByteToMessageDecoder
1. 继承关系图：**继承了 `ChannelInboundHandler` 接口，可见解码器 `ByteToMessageDecoder` 是入站执行链上的一环**
    
    ![[IMG-20260405035426539.png|Untitled 3 178.png]]
    
1. 一个关于 `ByteToMessageDecoder` 实例分析：
    
    自定义一个解码器 handler `ToIntegerDecoder`，并添加到 `ChannelPipeline`
    
    ```Java
    public class ToIntegerDecoder extends ByteToMessageDecoder {
    		@Override
    		protected void decode(ChannelHandlerContext ctx, ByteBuf in, List<Object> out) throws Exception {
    				// 在调用 readInt() 方法前必须验证所输入的 ByteBuf 是否具有足够的数据
    				if (in.readableBytes() >= 4) {
    						// int 在被添加到 List 中时，会被自动装箱为 Integer
    						out.add(in.readInt());
    				}
    		}
    }
    ```
    
    1. 这个例子中， 每次入站从 `ByteBuf` 中读取 `4` 字节， 将其解码为一个 `int`， 然后将它添加到一个 `List` 中
    
    1. 当没有更多元素可以被添加到该 `List` 中时， 它的内容将会被发送给下一个 `ChannelInboundHandler`
    
    1. `decode` 执行分析图
        
        ![[IMG-20260405035430980.png|Untitled 4 143.png]]
        
    
## 3.2 ReplayingDecoder
1. `public abstract class ReplayingDecoder<S> extends ByteToMessageDecoder`
1. ReplayingDecoder 扩展了ByteToMessageDecoder类，使用这个类，我们不必调用`readableBytes()`方法判断 `in` 里的数据情况
    
    ![[IMG-20260405035439775.png|Untitled 5 118.png]]
    
    - 参数 `S` 指定了用户状态管理的类型，示例中 `Void` 代表不需要状态管理
    
1. ReplayingDecoder使用方便，但它也有一些局限性
    
    1. 并不是所有的 ByteBuf 操作都被支持，如果调用了一个不被支持的方法，将会抛出一个`UnsupportedOperationException`
    
    1. ReplayingDecoder 在某些情况下可能稍慢于 ByteToMessageDecoder，例如网络缓慢并且消息格式复杂时，消息会被拆成了多个碎片，速度变慢
    
## 3.3 其它解码器
1. `LineBasedFrameDecoder`：这个类在Netty内部也有使用，它使用行尾控制字符 (n或者rn)作为分隔符来解析数据
1. `DelimiterBasedFrameDecoder`：使用自定义的特殊字符作为消息的分隔符
1. `HttpObjectDecoder`：一个HTTP数据的解码器
1. `LengthFieldBasedFrameDecoder`：通过指定长度来标识整包消息，这样就可以自动的处理黏包和半包消息
1. `MessageToMessageEncoder` ：[[9.3 群聊系统]]，使用的 netty 提供的解码器 `StringDecoder` 就是继承自 `MessageToMessageEncoder`
    
    ![[IMG-20260405035443399.png|Untitled 6 96.png]]