- [[#1. 方案思路]]
- [[#2. 协议包封装]]
- [[#3. 编解码器]]
- [[#4. 服务端]]
- [[#5. 客户端]]
- [[#6. 运行结果]]
# 1. 方案思路
1. 使用 ==**自定义协议 + 编解码器**== 来解决
    
    1. 关键就是**要解决服务器端每次读取数据长度的问题**
    
    1. 只要这个问题解决，就不会出现服务器多读或少读数据的问题，从而避免的 TCP 粘包、拆包
    
1. 如何实现
    
    1. 发送方在发送消息时，先将要发送的消息封装成一个 协议包对象，再通过 编码器将协议包对象转为字节码
    
    1. 接收方再接收消息时，先将字节码解码为协议包对象，再将数据从协议包中取出，接着进行业务处理
    
1. **⭐解决问题的关键步骤：**
    
    1. 解码器中，先取出编码器中以字节码发过来的协议包对象中的 **`len`** 值，根据这个 `len` 值，创建对应长度的 **`byte[]`**
    
    1. 然后就可以在读取数据时，**每次只读 `len` 长度的字节码，并将读出的数据封装成协议包对象**
        
        ![[IMG-20260405035413817.png|Untitled 375.png]]
        
        ![[IMG-20260405035414853.png|Untitled 1 283.png]]
        
    
    1. 这么做就可以保证，每次能读取固定长度的数据，从而避免的 TCP 粘包、拆包
    
# 2. 协议包封装
`MessageProtocol`
```Java
public class MessageProtocol {
    private int len;
    private byte[] content;
    public int getLen() {
        return len;
    }
    public void setLen(int len) {
        this.len = len;
    }
    public byte[] getContent() {
        return content;
    }
    public void setContent(byte[] content) {
        this.content = content;
    }
}
```
# 3. 编解码器
1. `MyMessageEncoder`
    
    ```Java
    public class MyMessageEncoder extends MessageToByteEncoder<MessageProtocol> {
        @Override
        protected void encode(ChannelHandlerContext ctx, MessageProtocol msg, ByteBuf out) throws Exception {
            System.out.println("MyMessageEncoder encode 方法被调用");
    				// // ⭐编码器从协议包中分析出长度和内容，然后发送 channel 中
            out.writeInt(msg.getLen());
            out.writeBytes(msg.getContent());
        }
    }
    ```
    
1. `MyMessageDecoder`
    
    ```Java
    public class MyMessageDecoder extends ReplayingDecoder<Void> {
        @Override
        protected void decode(ChannelHandlerContext ctx, ByteBuf in, List<Object> out) throws Exception {
            System.out.println("MyMessageDecoder decode 被调用");
            // 需要将得到二进制字节码 -> MessageProtocol 数据包(对象)
            int length = in.readInt();
            // 按照字节流读取内容
            byte[] content = new byte[length];  // ⭐解码器中从 channel 读取数据，每次只取长度为 14 的数据
            in.readBytes(content);
    
            // 封装成 MessageProtocol 对象
            MessageProtocol messageProtocol = new MessageProtocol();
            messageProtocol.setLen(length);
            messageProtocol.setContent(content);
            // 放入 out，传递给下一个 handler 业务处理
            out.add(messageProtocol);
        }
    }
    ```
    
# 4. 服务端
1. `MyServer`
    
    ```Java
    public class MyServer {
        public static void main(String[] args) throws Exception{
    
            EventLoopGroup bossGroup = new NioEventLoopGroup(1);
            EventLoopGroup workerGroup = new NioEventLoopGroup();
            try {
                ServerBootstrap serverBootstrap = new ServerBootstrap();
                serverBootstrap.group(bossGroup,workerGroup)
                        .channel(NioServerSocketChannel.class)
                        .childHandler(new ChannelInitializer<SocketChannel>() {
                            @Override
                            protected void initChannel(SocketChannel ch) throws Exception {
                                ChannelPipeline pipeline = ch.pipeline();
                                pipeline.addLast(new MyMessageEncoder());
                                pipeline.addLast(new MyMessageDecoder());
                                pipeline.addLast(new MyServerHandler());
                            }
                        }); //自定义一个初始化类
                ChannelFuture channelFuture = serverBootstrap.bind(7000).sync();
                channelFuture.channel().closeFuture().sync();
            }finally {
                bossGroup.shutdownGracefully();
                workerGroup.shutdownGracefully();
            }
        }
    }
    ```
    
1. `MyServerHandler`
    
    ```Java
    public class MyServerHandler extends SimpleChannelInboundHandler<MessageProtocol> {
        private int count;
    
        @Override
        protected void channelRead0(ChannelHandlerContext ctx, MessageProtocol msg) throws Exception {
            int len = msg.getLen();
            byte[] content = msg.getContent();
    
            String message = new String(content, StandardCharsets.UTF_8);
            System.out.println("服务器接收到数据长度：" + len);
            System.out.println("服务器接收到数据：" + message);
            System.out.println("服务器接收到消息量：" + (++this.count));
    
            // 服务器回送数据给客户端，回送一个随机 uuid
            String resMessage = UUID.randomUUID().toString();
            int resLen = resMessage.getBytes(Charset.forName("utf-8")).length;
            byte[] resContent = resMessage.getBytes(Charset.forName("utf-8"));
            // 构建一个协议包
            MessageProtocol messageProtocol = new MessageProtocol();
            messageProtocol.setLen(resLen);
            messageProtocol.setContent(resContent);
            ctx.writeAndFlush(messageProtocol);
        }
    
        @Override
        public void exceptionCaught(ChannelHandlerContext ctx, Throwable cause) throws Exception {
            //cause.printStackTrace();
            ctx.close();
        }
    }
    ```
    
# 5. 客户端
1. `MyClient`
    
    ```Java
    public class MyClient {
        public static void main(String[] args)  throws  Exception{
    
            EventLoopGroup group = new NioEventLoopGroup();
            try {
                Bootstrap bootstrap = new Bootstrap();
                bootstrap.group(group).channel(NioSocketChannel.class)
                        .handler(new ChannelInitializer<SocketChannel>() {
                            @Override
                            protected void initChannel(SocketChannel ch) throws Exception {
                                ChannelPipeline pipeline = ch.pipeline();
                                pipeline.addLast(new MyMessageEncoder());
                                pipeline.addLast(new MyMessageDecoder());
                                pipeline.addLast(new MyClientHandler());
                            }
                        });
    
                ChannelFuture channelFuture = bootstrap.connect("localhost", 7000).sync();
    
                channelFuture.channel().closeFuture().sync();
    
            }finally {
                group.shutdownGracefully();
            }
        }
    }
    ```
    
1. `MyClientHandler`
    
    ```Java
    public class MyClientHandler extends SimpleChannelInboundHandler<MessageProtocol> {
        private int count;
        @Override
        public void channelActive(ChannelHandlerContext ctx) throws Exception {
            // 使用客户端发送 10 条数据 hello,server 编号
            for(int i= 0; i< 10; ++i) {
                String message = "hello,server " + i;
                byte[] content = message.getBytes(Charset.forName("utf-8"));
                int length = message.getBytes(Charset.forName("utf-8")).length;
                // 创建协议包对象
                MessageProtocol messageProtocol = new MessageProtocol();
                messageProtocol.setLen(length);
                messageProtocol.setContent(content);
                ctx.writeAndFlush(messageProtocol);
            }
        }
    
        @Override
        protected void channelRead0(ChannelHandlerContext ctx, MessageProtocol msg) throws Exception {
            int len = msg.getLen();
            byte[] content = msg.getContent();
    
            String message = new String(content, StandardCharsets.UTF_8);
            System.out.println("客户端接收到数据长度：" + len);
            System.out.println("客户端接收到消息：" + message);
            System.out.println("客户端接收消息数量：" + (++this.count));
        }
    
        @Override
        public void exceptionCaught(ChannelHandlerContext ctx, Throwable cause) throws Exception {
            cause.printStackTrace();
            ctx.close();
        }
    }
    ```
    
# 6. 运行结果
1. 启动服务端、再启动两个客户端
1. 服务端输出：
    
    1. 可以看到与之前不同，现在**服务端每次只接收长度为** `**14**` **字节的数据**，不管是哪个客户端发来的都是如此形式接收
    
    1. 故这样处理，==避免了TCP 粘包和拆包问题==
        
        ![[IMG-20260405035420212.png|Untitled 2 236.png]]
        
    
1. 客户端 1 输出：
    
    ![[IMG-20260404031819027.png|Untitled 3 180.png]]
    
1. 客户端 2 输出：
    
    ![[IMG-20260404031819085.png|Untitled 4 144.png]]