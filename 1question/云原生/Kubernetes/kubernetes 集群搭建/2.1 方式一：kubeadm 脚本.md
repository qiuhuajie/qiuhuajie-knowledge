- [[#1. 环境规划]]
- [[#2. 系统初始化]]
- [[#3. 安装 Docker]]
- [[#4. 安装 Kubeadm 工具]]
- [[#5. 部署 master 节点]]
- [[#6. 添加 work node 节点]]
- [[#7. 安装网络插件]]
- [[#8. 测试集群]]
# 1. **环境规划**
|   |   |   |   |
|---|---|---|---|
|**主机名**|**IP**|**角色**|**系统**|
|master|192.168.10.171|master node|centos7.9|
|node01|192.168.10.172|woker node|centos7.9|
|node02|192.168.10.173|woker node|centos7.9|
# 2. **系统初始化**
1. 修改静态IP
    
    ```Bash
     vi /etc/sysconfig/network-scripts/ifcfg-ens33
    ```
    
1. 修改主机名
    
    ```Bash
     vi /etc/hostname
    ```
    
1. 添加域名映射
    
    ```Bash
     vi /etc/hosts
    ```
    
1. 重启生效
    
    ```Bash
     systemctl restart network
     reboot
    ```
    
1. 保证三个节点之间可以使用主机名 Ping 通
1. 关闭防火墙
    
    ```Bash
     systemctl stop firewalld
     systemctl disable firewalld
     systemctl status firewalld
    ```
    
1. 关闭 SE Linux
    
    > selinux 是 linux 系统下的一个安全服务，如果不关闭它，在安装集群中会产生各种各样的奇葩问题
    
    ```Bash
     # 永久关闭
     sed -i 's/enforcing/disabled/' /etc/selinux/config
     
     # 临时关闭
     setenforce 0
    ```
    
1. 关闭 swap
    
    > swap分区指的是虚拟内存分区，它的作用是物理内存使用完，之后将磁盘空间虚拟成内存来使用，启用swap设备会对系统的性能产生非常负面的影响
    
    ```Bash
     # 永久关闭
     sed -ri 's/.*swap.*/#&/' /etc/fstab         # 需要重启后才能生效
     
     # 临时关闭
     swapoff -a
     
     # 验证：
     [root@master ~]# free -m
                   total        used        free      shared  buff/cache   available
     Mem:           7802         359        7299          11         143        7232
     Swap:             0           0           0
    ```
    
1. 将桥接的 IPv4 流量传递到 iptables 的链
    
    ```Bash
     cat > /etc/sysctl.d/k8s.conf << EOF
     net.bridge.bridge-nf-call-ip6tables = 1net.bridge.bridge-nf-call-iptables = 1
     EOF
     
     sysctl --system
    ```
    
    ```Bash
     * Applying /usr/lib/sysctl.d/00-system.conf ...
     * Applying /usr/lib/sysctl.d/10-default-yama-scope.conf ...
     kernel.yama.ptrace_scope = 0
     * Applying /usr/lib/sysctl.d/50-default.conf ...
     kernel.sysrq = 16
     kernel.core_uses_pid = 1
     kernel.kptr_restrict = 1
     net.ipv4.conf.default.rp_filter = 1
     net.ipv4.conf.all.rp_filter = 1
     net.ipv4.conf.default.accept_source_route = 0
     net.ipv4.conf.all.accept_source_route = 0
     net.ipv4.conf.default.promote_secondaries = 1
     net.ipv4.conf.all.promote_secondaries = 1
     fs.protected_hardlinks = 1
     fs.protected_symlinks = 1
     * Applying /etc/sysctl.d/99-sysctl.conf ...
     * Applying /etc/sysctl.d/k8s.conf ...
     * Applying /etc/sysctl.conf ...
    ```
    
1. 时间同步
    
    ```Bash
     yum install ntpdate -y
     
     ntpdate time.windows.com
    ```
    
1. 配置阿里云 yum 源
    
    ```Bash
     curl -o /etc/yum.repos.d/CentOS-Base.repo https://mirrors.aliyun.com/repo/Centos-7.repo
    ```
    
1. yum系统升级
    
    ```Bash
     yum -y update
     
     reboot
    ```
    
    ```Bash
     yum clean
    
     yum makecache
    ```
    
# 3. **安装 Docker**
1. 安装 wget
    
    ```Bash
     yum install wget
    ```
    
1. 安装 Docker
    
    ```Bash
     wget https://mirrors.aliyun.com/docker-ce/linux/centos/docker-ce.repo -O /etc/yum.repos.d/docker-ce.repo
     
     yum -y install docker-ce-18.06.1.ce-3.el7
     
     systemctl enable docker && systemctl start docker
     
     docker --version
    ```
    
1. 给 Docker 配置阿里云加速
    
    ```Bash
     cat > /etc/docker/daemon.json << EOF
     {
     "registry-mirrors": ["https://b9pmyelo.mirror.aliyuncs.com"]
     }
     EOF
    ```
    
1. 重启docker 服务
    
    ```Bash
     systemctl daemon-reload
     
     systemctl restart docker
    ```
    
1. 验证阿里云仓库是否配置成功
    
    ```Bash
     docker info
     
     ...
     Registry Mirrors:
      https://b9pmyelo.mirror.aliyuncs.com/
     ...
    ```
    
# 4. **安装 Kubeadm 工具**
1. 安装 kubernetes 的源
    
    ```Bash
     cat > /etc/yum.repos.d/kubernetes.repo << EOF
     [kubernetes]
     name=Kubernetes
     baseurl=https://mirrors.aliyun.com/kubernetes/yum/repos/kubernetes-el7-x86_64
     enabled=1
     gpgcheck=0
     repo_gpgcheck=0
     gpgkey=https://mirrors.aliyun.com/kubernetes/yum/doc/yum-key.gpg
     https://mirrors.aliyun.com/kubernetes/yum/doc/rpm-package-key.gpg
     EOF
    ```
    
    ```Bash
     [root@master ~]# cd /etc/yum.repos.d/
     [root@master yum.repos.d]# ls
     CentOS-Base.repo  CentOS-CR.repo  CentOS-Debuginfo.repo  CentOS-fasttrack.repo  CentOS-Media.repo  CentOS-Sources.repo  CentOS-Vault.repo  CentOS-x86_64-kernel.repo  docker-ce.repo  kubernetes.repo
    ```
    
1. yum 二连
    
    ```Bash
     yum clean
     
     yum makecache
    ```
    
1. 升级
    
    ```Bash
     yum -y update
     
     reboot
    ```
    
1. 安装 kubeadm、kubelet 和 kubectl
    
    ```Bash
     yum install -y kubelet-1.20.9 kubeadm-1.20.9 kubectl-1.20.9 --disableexcludes=kubernetes
     
     systemctl enable --now kubelet
    ```
    
    > 注意需要安装指定版本的，否则拉取镜像会出现问题：
    > 
    > ```Bash
    >  this version of kubeadm only supports deploying clusters with the control plane version >= 1.22.0. Current version: v1.17.0
    >  To see the stack trace of this error execute with --v=5 or higher
    >  
    >  # 已安装的 kubeadm 与 当前要安装的kuberbetes的版本冲突
    >  # 之前的安装命令：yum install -y kubelet kubeadm kubectl
    > ```
    > 
    > 解决方法：将 kubelet kubeadm kubectl 版本降低
    > 
    > ```Bash
    >  yum remove -y kubelet kubeadm kubectl
    >  
    >  # 安装指定的低版本的
    >  yum install -y kubelet-1.20.9 kubeadm-1.20.9 kubectl-1.20.9 --disableexcludes=kubernetes
    >  
    >  systemctl enable --now kubelet
    > ```
    
# 5. **部署 master 节点**
1. 在 master 上安装 部署 kubernetes
    
    ```Bash
     kubeadm init --apiserver-advertise-address=192.168.10.171 --image-repository registry.aliyuncs.com/google_containers --kubernetes-version v1.12.9 --service-cidr=10.96.0.0/12 --pod-network-cidr=10.244.0.0/16
     
     ...
     Your Kubernetes control-plane has initialized successfully!
     ...
    ```
    
    ![[IMG-20260404031956814.png|Untitled 393.png]]
    
1. 查看拉取的镜像
    
    ```Bash
     [root@master ~]# docker images
     REPOSITORY                                                        TAG        IMAGE ID       CREATED         SIZE
     registry.aliyuncs.com/google_containers/kube-proxy                v1.20.9    8dbf9a6aa186   8 months ago    99.7MB
     registry.aliyuncs.com/google_containers/kube-apiserver            v1.20.9    0d0d57e4f64c   8 months ago    122MB
     registry.aliyuncs.com/google_containers/kube-controller-manager   v1.20.9    eb07fd4ad3b4   8 months ago    116MB
     registry.aliyuncs.com/google_containers/kube-scheduler            v1.20.9    295014c114b3   8 months ago    47.3MB
     registry.aliyuncs.com/google_containers/etcd                      3.4.13-0   0369cf4303ff   19 months ago   253MB
     registry.aliyuncs.com/google_containers/coredns                   1.7.0      bfe3a36ebd25   21 months ago   45.2MB
     registry.aliyuncs.com/google_containers/pause                     3.2        80d28bedfe5d   2 years ago     683kB
    ```
    
1. 在 master 上执行
    
    ```Bash
     mkdir -p $HOME/.kube
     sudo cp -i /etc/kubernetes/admin.conf $HOME/.kube/config
     sudo chown $(id -u):$(id -g) $HOME/.kube/config
    ```
    
1. 查看节点（当前只有 master 一个节点）
    
    ```Bash
     [root@master ~]# kubectl get nodes
     NAME     STATUS     ROLES                  AGE   VERSION
     master   NotReady   control-plane,master   10m   v1.20.9
    ```
    
# 6. **添加 work node 节点**
1. 在所有work node节点上执行，将所有node 加入master
    
    ```Bash
     # node 节点上
     kubeadm join 192.168.10.171:6443 --token pp8kwv.5svl8zq5oqlhbf5q \
         --discovery-token-ca-cert-hash sha256:d7e0342ece8a043b703074c1b9400d70a083684d37930fa9e0849b4b4f79f621
    ```
    
1. 再次查看节点（node节点虽然都加进来了，但都是NotReady状态）
    
    ```Bash
     [root@master ~]# kubectl get nodes
     NAME     STATUS     ROLES                  AGE   VERSION
     master   NotReady   control-plane,master   13m   v1.20.9
     node01   NotReady   <none>                 66s   v1.20.9
     node02   NotReady   <none>                 57s   v1.20.9
    ```
    
# 7. **安装网络插件**
1. 在master节点上执行，配置网络插件
    
    ```Bash
     # 将 yaml配置文件下载下来
     curl https://docs.projectcalico.org/manifests/calico.yaml -O
     
     # 使配置文件生效
     kubectl apply -f calico.yaml
    ```
    
1. 执行上面的命令后，会下载一些镜像，并运行为容器
    
    ```Bash
     [root@master ~]# kubectl get pods -n kube-system
     NAME                                       READY   STATUS              RESTARTS   AGE
     calico-kube-controllers-858c9597c8-96xwr   0/1     ContainerCreating   0          2m22s
     calico-node-22vjq                          0/1     PodInitializing     0          2m22s
     calico-node-rd2fk                          0/1     Init:2/3            0          2m22s
     calico-node-s25wb                          0/1     PodInitializing     0          2m22s
     coredns-7f89b7bc75-gmcgt                   0/1     ContainerCreating   0          24m
     coredns-7f89b7bc75-pczvx                   0/1     ContainerCreating   0          24m
     etcd-master                                1/1     Running             0          24m
     kube-apiserver-master                      1/1     Running             0          24m
     kube-controller-manager-master             1/1     Running             0          24m
     kube-proxy-285ns                           1/1     Running             0          24m
     kube-proxy-292mv                           1/1     Running             0          12m
     kube-proxy-smz4t                           1/1     Running             0          12m
     kube-scheduler-master                      1/1     Running             0          24m
    ```
    
1. 等待所有的pod 的STATUS 变为 running
    
    ```Bash
     # 等所有的变为 running
     [root@master ~]# kubectl get pods -n kube-system
     NAME                                       READY   STATUS             RESTARTS   AGE
     calico-kube-controllers-858c9597c8-96xwr   1/1     Running            0          9m38s
     calico-node-22vjq                          0/1     ImagePullBackOff   0          9m38s                  # 这里一直是ImagePullBackOff但是好像没影响
     calico-node-rd2fk                          1/1     Running            0          9m38s
     calico-node-s25wb                          1/1     Running            0          9m38s
     coredns-7f89b7bc75-gmcgt                   1/1     Running            0          31m
     coredns-7f89b7bc75-pczvx                   1/1     Running            0          31m
     etcd-master                                0/1     Running            2          32m
     kube-apiserver-master                      0/1     Running            2          32m
     kube-controller-manager-master             0/1     Running            2          32m
     kube-proxy-285ns                           1/1     Running            2          31m
     kube-proxy-292mv                           1/1     Running            0          20m
     kube-proxy-smz4t                           1/1     Running            0          20m
     kube-scheduler-master                      0/1     Running            2          32m
    ```
    
1. 再次查看节点（都变为Ready状态）
    
    ```Bash
     [root@master ~]# kubectl get nodes
     NAME     STATUS   ROLES                  AGE   VERSION
     master   Ready    control-plane,master   32m   v1.20.9
     node01   Ready    <none>                 20m   v1.20.9
     node02   Ready    <none>                 20m   v1.20.9
    ```
    
# 8. **测试集群**
1. 拉取 nginx 镜像，并创建为 pod
    
    ```Bash
     [root@master ~]# kubectl create deployment nginx --image=nginx
     deployment.apps/nginx created
    ```
    
1. 查看 pod 情况（nginx 已经running）
    
    ```Bash
     [root@master ~]# kubectl get pod
     NAME                     READY   STATUS    RESTARTS   AGE
     nginx-6799fc88d8-xpc28   1/1     Running   0          32s
    ```
    
1. 对外暴露端口
    
    ```Bash
     [root@master ~]# kubectl expose deployment nginx --port=80 --type=NodePort
     service/nginx exposed
    ```
    
1. 查看service情况
    
    ```Bash
     [root@master ~]# kubectl get svc
     NAME                 TYPE        CLUSTER-IP      EXTERNAL-IP   PORT(S)        AGE
     service/kubernetes   ClusterIP   10.96.0.1       <none>        443/TCP        34m
     service/nginx        NodePort    10.111.47.223   <none>        80:32401/TCP   23s   # 80是master暴露给node的端口，32401是node暴露给外部的端口
     
     [root@master ~]# kubectl get service
     NAME         TYPE        CLUSTER-IP      EXTERNAL-IP   PORT(S)        AGE
     kubernetes   ClusterIP   10.96.0.1       <none>        443/TCP        85m
     nginx        NodePort    10.111.47.223   <none>        80:32401/TCP   51m
    ```
    
1. 测试访问：【nodeIP:暴露的端口】
    
    [http://192.168.10.172:32401/](http://192.168.10.172:32401/)
    
    ![[IMG-20260404031956885.png|Untitled 1 296.png]]