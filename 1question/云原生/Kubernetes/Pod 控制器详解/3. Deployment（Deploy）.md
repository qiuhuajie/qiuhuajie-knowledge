- [[#1. 介绍]]
- [[#2. 操作]]
    - [[#2.1 创建 Deploy]]
    - [[#2.2 扩缩容]]
    - [[#2.3 镜像更新]]
    - [[#2.4 重建更新]]
    - [[#2.5 滚动更新]]
    - [[#2.6 更新后的 RS]]
    - [[#2.7 版本回退]]
    - [[#2.8 金丝雀发布⭐]]
# 1. 介绍
1. 主要作用：
    
    1. 为了更好的解决服务编排的问题，kubernetes在V1.2版本开始，引入了Deployment控制器
    
    1. **这种控制器并不直接管理 pod，而是⭐**==**通过管理 ReplicaSet 来间接管理 Pod**==
    
    1. 即 Deployment 管理 ReplicaSet，ReplicaSet 管理 Pod
    
    1. 故 Deployment 比 ReplicaSet 功能更加强大
    
    ![[IMG-20260404031955257.png|Untitled 400.png]]
    
1. **Deployment 主要功能：**
    
    - **支持 ReplicaSet 的所有功能**
    
    - **支持发布的停止、继续**
    
    - **支持滚动升级和回滚版本**
    
1. Deployment的资源清单文件：
    
    ```Plain
     apiVersion: apps/v1 # 版本号
     kind: Deployment # 类型
     metadata: # 元数据
       name: # rs名称
       namespace: # 所属命名空间
       labels: \#标签
         controller: deploy
     spec: # 详情描述
       replicas: 3 # 副本数量
       revisionHistoryLimit: 3 # 保留历史版本
       paused: false # 暂停部署，默认是false
       progressDeadlineSeconds: 600 # 部署超时时间（s），默认是600
       strategy: # 策略
         type: RollingUpdate # 滚动更新策略
         rollingUpdate: # 滚动更新
           maxSurge: 30% # 最大额外可以存在的副本数，可以为百分比，也可以为整数
           maxUnavailable: 30% # 最大不可用状态的 Pod 的最大值，可以为百分比，也可以为整数
       selector: # 选择器，通过它指定该控制器管理哪些pod
         matchLabels:      # Labels匹配规则
           app: nginx-pod
         matchExpressions: # Expressions匹配规则
           - {key: app, operator: In, values: [nginx-pod]}
       template: # 模板，当副本数量不足时，会根据下面的模板创建pod副本
         metadata:
           labels:
             app: nginx-pod
         spec:
           restartPolicy: [Always | Never | OnFailure]  \#Pod的重启策略
           nodeName: <string> \#设置NodeName表示将该Pod调度到指定到名称的node节点上
           nodeSelector: obeject \#设置NodeSelector表示将该Pod调度到包含这个label的node上
    
           containers:
           - name: nginx
             image: nginx:1.17.1
             ports:
             - containerPort: 80
    ```
    
# 2. 操作
### 2.1 **创建 Deploy**
1. 创建pc-deployment.yaml
    
    ```Plain
     apiVersion: apps/v1
     kind: Deployment
     metadata:
       name: pc-deployment
       namespace: dev
     spec:
       replicas: 3
       selector:
         matchLabels:
           app: nginx-pod
       template:
         metadata:
           labels:
             app: nginx-pod
         spec:
           containers:
           - name: nginx
             image: nginx:1.17.1
    ```
    
1. 创建 Deploy
    
    ```Plain
     [root@master ~]# kubectl create -f pc-deployment.yaml -n dev --record=true
     deployment.apps/pc-deployment created
    ```
    
1. 查看 Deploy
    
    ```Plain
     [root@master ~]# kubectl get deploy pc-deployment -n dev
     NAME            READY   UP-TO-DATE   AVAILABLE   AGE
     pc-deployment   3/3     3            3           16s
    ```
    
    - `UP-TO-DATE` 最新版本的pod的数量
    
    - `AVAILABLE` 当前可用的pod的数量
    
1. 查看 RS（在Deploy名字的基础上拼接了一段字符）
    
    ```Plain
     [root@master ~]# kubectl get rs -n dev
     NAME                       DESIRED   CURRENT   READY   AGE
     nginx-app-d6ff45774        3         3         3       2d
     pc-deployment-5d9c9b97bb   3         3         3       26s
    ```
    
1. 查看当前控制器创建出来的pod（在RS名字的基础上又拼接了一段字符）
    
    ```Plain
     [root@master ~]# kubectl get pods -n dev
     NAME                             READY   STATUS    RESTARTS   AGE
     pc-deployment-5d9c9b97bb-4c6ch   1/1     Running   0          67s
     pc-deployment-5d9c9b97bb-6rbxj   1/1     Running   0          67s
     pc-deployment-5d9c9b97bb-b2pg6   1/1     Running   0          67s
    ```
    
### 2.2 **扩缩容**
1. `**kubectl scale**`
1. 动态的修改 pod 副本的数量
    
    1. 扩容：修改`spec:replicas: 5`
        
        ```Plain
         [root@master ~]# kubectl scale deploy pc-deployment --replicas=5  -n dev
         deployment.apps/pc-deployment scaled
        ```
        
    
    1. 查看 Deploy
        
        ```Plain
         [root@master ~]# kubectl get deploy pc-deployment -n dev
         NAME            READY   UP-TO-DATE   AVAILABLE   AGE
         pc-deployment   5/5     5            5           101s
        ```
        
    
    1. 查看 Pod
        
        ```Plain
         [root@master ~]# kubectl get pods -n dev
         NAME                             READY   STATUS    RESTARTS   AGE
         pc-deployment-5d9c9b97bb-4c6ch   1/1     Running   0          109s
         pc-deployment-5d9c9b97bb-6rbxj   1/1     Running   0          109s
         pc-deployment-5d9c9b97bb-74d42   1/1     Running   0          16s
         pc-deployment-5d9c9b97bb-b2pg6   1/1     Running   0          109s
         pc-deployment-5d9c9b97bb-mld2c   1/1     Running   0          16s
        ```
        
    
    1. 缩容：修改`spec:replicas: 2`
        
        ```Plain
         [root@master ~]# kubectl scale deploy pc-deployment --replicas=2  -n dev
         deployment.apps/pc-deployment scaled
        ```
        
    
    1. 稍等片刻，就只剩下2个了
        
        ```Plain
         [root@master ~]# kubectl get pods -n dev
         NAME                             READY   STATUS    RESTARTS   AGE
         pc-deployment-5d9c9b97bb-4c6ch   1/1     Running   0          2m17s
         pc-deployment-5d9c9b97bb-6rbxj   1/1     Running   0          2m17s
        ```
        
    
### 2.3 **镜像更新**
1. `**kubectl set image**`
1. deployment支持两种更新策略：重建更新和滚动更新,可以通过 `strategy` 指定策略类型，支持两个属性
    
    - `type`：指定策略类型，支持两种策略
        
        - `Recreate`：在创建出新的Pod之前会先杀掉所有已存在的Pod
        
        - `RollingUpdate`：滚动更新，就是杀死一部分，就启动一部分，在更新过程中，存在两个版本Pod。RollingUpdate 有两个子选项：
            
            - `maxUnavailable`：用来指定在升级过程中不可用Pod的最大数量，默认为25%
            
            - `maxSurge`： 用来指定在升级过程中可以超过期望的Pod的最大数量，默认为25%
            
        
    
### 2.4 **重建更新**
1. **一次性删除所有老版本的Pod，再创建新版本的 Pod**
1. 编辑pc-deployment.yaml，在spec节点下添加更新策略
    
    ```Plain
     apiVersion: apps/v1
     kind: Deployment
     metadata:
       name: pc-deployment
       namespace: dev
     spec:
       strategy: # 策略
         type: Recreate # 重建更新
       replicas: 3
       selector:
         matchLabels:
           app: nginx-pod
       template:
         metadata:
           labels:
             app: nginx-pod
         spec:
           containers:
           - name: nginx
             image: nginx:1.17.1
    ```
    
1. 创建deploy进行验证
    
    ```Plain
     [root@master ~]# kubectl create -f pc-deployment.yaml
     deployment.apps/pc-deployment created
     
     [root@master ~]# kubectl get pod
     NAME                     READY   STATUS    RESTARTS   AGE
     nginx-6799fc88d8-wx7gq   1/1     Running   1          5h7m
     
     [root@master ~]# kubectl get pod -n dev
     NAME                             READY   STATUS    RESTARTS   AGE
     pc-deployment-5d9c9b97bb-ckf2w   1/1     Running   0          52s
     pc-deployment-5d9c9b97bb-hvl8v   1/1     Running   0          52s
     pc-deployment-5d9c9b97bb-zvvc7   1/1     Running   0          52s
    ```
    
1. 变更镜像
    
    ```Plain
     [root@master ~]# kubectl set image deployment pc-deployment nginx=nginx:1.17.2 -n dev
     deployment.apps/pc-deployment image updated
    ```
    
1. `w` 观察升级过程
    
    ```Plain
     [root@master ~]# kubectl get pods -n dev -w
     NAME                             READY   STATUS        RESTARTS   AGE
     pc-deployment-5d9c9b97bb-hvl8v   0/1     Terminating   0          70s
     
     pc-deployment-7c7477c7ff-gd42h   0/1     Pending       0          0s
     pc-deployment-7c7477c7ff-f8ml8   0/1     Pending       0          0s
     pc-deployment-7c7477c7ff-x2prv   0/1     Pending       0          0s
     
     pc-deployment-7c7477c7ff-gd42h   0/1     ContainerCreating   0          0s
     pc-deployment-7c7477c7ff-f8ml8   0/1     ContainerCreating   0          0s
     pc-deployment-7c7477c7ff-x2prv   0/1     ContainerCreating   0          0s
     
     pc-deployment-7c7477c7ff-f8ml8   1/1     Running             0          32s
     pc-deployment-7c7477c7ff-gd42h   1/1     Running             0          32s
     pc-deployment-7c7477c7ff-x2prv   1/1     Running             0          32s
    ```
    
### 2.5 **滚动更新**
1. **先删除一部分老版本的 Pod，再启动一部分新版本的 Pod；再删除剩余部分老版本的 Pod，再启动一部分新版本的 Pod**
1. **在整个更新的过程中，新老版本同时同在**
1. 编辑pc-deployment.yaml，在spec节点下添加更新策略
    
    ```Plain
     apiVersion: apps/v1
     kind: Deployment
     metadata:
       name: pc-deployment
       namespace: dev
     spec:
       strategy: # 策略
         type: RollingUpdate # 滚动更新策略
         rollingUpdate:
           maxSurge: 25%
           maxUnavailable: 25%
       replicas: 3
       selector:
         matchLabels:
           app: nginx-pod
       template:
         metadata:
           labels:
             app: nginx-pod
         spec:
           containers:
           - name: nginx
             image: nginx:1.17.1
    ```
    
1. 创建deploy进行验证
    
    ```Plain
     [root@master ~]# kubectl create -f pc-deployment.yaml
     deployment.apps/pc-deployment created
     
     [root@master ~]# kubectl get pods -n dev
     NAME                             READY   STATUS    RESTARTS   AGE
     pc-deployment-5d9c9b97bb-5bqb7   1/1     Running   0          11s
     pc-deployment-5d9c9b97bb-dcktd   1/1     Running   0          11s
     pc-deployment-5d9c9b97bb-dzlfw   1/1     Running   0          11s
    ```
    
1. 变更镜像
    
    ```Plain
     [root@master ~]# kubectl set image deployment pc-deployment nginx=nginx:1.17.3 -n dev
     deployment.apps/pc-deployment image updated
    ```
    
1. 观察升级过程
    
    ```Plain
     [root@master ~]# kubectl get pods -n dev -w
     NAME                             READY   STATUS              RESTARTS   AGE
     pc-deployment-5d9c9b97bb-5bqb7   1/1     Running             0          26s
     pc-deployment-5d9c9b97bb-dcktd   1/1     Running             0          26s
     pc-deployment-5d9c9b97bb-dzlfw   1/1     Running             0          26s
     # ⭐可以看到这个新创建的 Pod 属于新 RS 76fd8c7f84
     # 说明一个新的 RS 被创建了
     pc-deployment-76fd8c7f84-wvk9h   0/1     ContainerCreating   0          4s
     
     pc-deployment-76fd8c7f84-wvk9h   1/1     Running             0          29s
     pc-deployment-5d9c9b97bb-dzlfw   1/1     Terminating         0          51s
     pc-deployment-76fd8c7f84-qthvs   0/1     ContainerCreating   0          0s
     
     pc-deployment-76fd8c7f84-qthvs   1/1     Running             0          2s
     pc-deployment-5d9c9b97bb-dzlfw   0/1     Terminating         0          53s
     pc-deployment-5d9c9b97bb-5bqb7   1/1     Terminating         0          53s
     pc-deployment-5d9c9b97bb-dzlfw   0/1     Terminating         0          53s
     pc-deployment-76fd8c7f84-z78kc   0/1     Pending             0          0s
     pc-deployment-76fd8c7f84-z78kc   0/1     Pending             0          0s
     pc-deployment-76fd8c7f84-z78kc   0/1     ContainerCreating   0          0s
     
     pc-deployment-76fd8c7f84-qthvs   1/1     Running   0          6m6s
     pc-deployment-76fd8c7f84-wvk9h   1/1     Running   0          6m35s
     pc-deployment-76fd8c7f84-z78kc   1/1     Running   0          6m4s
    ```
    
### 2.6 **更新后的 RS**
1. **镜像更新中RS的变化**
    
    1. 发现原来的RS的依旧存在，只是pod数量变为了0，而后**又新产生了一个RS**，pod数量为4
    
    1. 那么**为什么还要把原来的RS保留着？**
    
1. 其实这就是deployment能够进行版本回退的奥妙所在（见下）
    
    ```Plain
     [root@master ~]# kubectl get rs -n dev
     NAME                       DESIRED   CURRENT   READY   AGE
     pc-deployment-5d9c9b97bb   0         0         0       17m
     pc-deployment-76fd8c7f84   3         3         3       17m
    ```
    
### 2.7 **版本回退**
1. `**kubectl rollout**`
1. deployment支持版本升级过程中的暂停、继续功能以及版本回退等诸多功能
1. `kubectl rollout`： 版本升级相关功能，支持下面的选项：
    
    - `status` 显示当前升级状态
    
    - `history` 显示 升级历史记录
    
    - `pause` 暂停版本升级过程
    
    - `resume` 继续已经暂停的版本升级过程
    
    - `restart` 重启版本升级过程
    
    - `undo` 回滚到上一级版本（可以使用--to-revision回滚到指定版本）
    
1. 示例：
    
    1. 先重新生成pc-deployment.yaml的Pod
        
        ```Plain
         [root@master ~]# kubectl delete -f pc-deployment.yaml
         deployment.apps "pc-deployment" deleted
         
         [root@master ~]# kubectl create -f pc-deployment.yaml --record
         deployment.apps/pc-deployment created
         
         [root@master ~]# kubectl get pods -n dev
         NAME                             READY   STATUS    RESTARTS   AGE
         pc-deployment-5d9c9b97bb-8rhxd   1/1     Running   0          26s
         pc-deployment-5d9c9b97bb-mq5d5   1/1     Running   0          26s
         pc-deployment-5d9c9b97bb-x8lcg   1/1     Running   0          26s
         
         [root@master ~]# kubectl get rs -n dev
         NAME                       DESIRED   CURRENT   READY   AGE
         pc-deployment-5d9c9b97bb   3         3         3       33s
        ```
        
    
    1. 升级版本
        
        ```Plain
         [root@master ~]# kubectl set image deployment pc-deployment nginx=nginx:1.17.2 -n dev
         deployment.apps/pc-deployment image updated
         
         [root@master ~]# kubectl get rs -n dev
         NAME                       DESIRED   CURRENT   READY   AGE
         pc-deployment-5d9c9b97bb   0         0         0       51s
         pc-deployment-7c7477c7ff   3         3         3       10s      # ⭐版本升级时，新创建的
        ```
        
    
    1. 查看当前升级版本的状态
        
        ```Plain
         [root@master ~]# kubectl rollout status deploy pc-deployment -n dev
         deployment "pc-deployment" successfully rolled out
        ```
        
    
    1. 查看升级历史记录
        
        ```Plain
         [root@master ~]# kubectl rollout history deploy pc-deployment -n dev
         deployment.apps/pc-deployment
         REVISION  CHANGE-CAUSE
         1         kubectl create --filename=pc-deployment.yaml --record=true
         2         kubectl create --filename=pc-deployment.yaml --record=true
         # 升级了一次，所有两个版本
        ```
        
    
    1. 再升级一个版本
        
        ```Plain
         [root@master ~]# kubectl set image deployment pc-deployment nginx=nginx:1.17.3 -n dev
         deployment.apps/pc-deployment image updated
         
         [root@master ~]# kubectl rollout history deploy pc-deployment -n dev
         deployment.apps/pc-deployment
         REVISION  CHANGE-CAUSE
         1         kubectl create --filename=pc-deployment.yaml --record=true
         2         kubectl create --filename=pc-deployment.yaml --record=true
         3         kubectl create --filename=pc-deployment.yaml --record=true
         # 升级了两次，所有三个版本
        ```
        
    
    1. 版本回滚
        
        ```Plain
         [root@master ~]# kubectl rollout undo deployment pc-deployment --to-revision=1 -n dev
         deployment.apps/pc-deployment rolled back
        ```
        
    
    1. 查看升级记录**（版本1已经没了，因为现在版本4就是原来的版本1）**
        
        ```Plain
         [root@master ~]# kubectl rollout history deploy pc-deployment -n dev
         deployment.apps/pc-deployment
         REVISION  CHANGE-CAUSE
         2         kubectl create --filename=pc-deployment.yaml --record=true
         3         kubectl create --filename=pc-deployment.yaml --record=true
         4         kubectl create --filename=pc-deployment.yaml --record=true
        ```
        
    
    1. RS 也回到了第一个版本
        
        ```Plain
         [root@master ~]# kubectl get rs -n dev
         NAME                       DESIRED   CURRENT   READY   AGE
         nginx-app-d6ff45774        3         3         3       2d1h
         pc-deployment-5d9c9b97bb   3         3         3       4m13s
         pc-deployment-76fd8c7f84   0         0         0       24s
         pc-deployment-7c7477c7ff   0         0         0       3m32s
        ```
        
    
### 2.8 **金丝雀发布⭐**
1. Deployment控制器支持控制更新过程中的控制，如“暂停(pause)”或“继续(resume)”更新操作
1. 金丝雀发布
    
    1. ==**在更新版本时，使用**== **`pause`**==**先让一部分新版本的 Pod 运行起来，主体部分还是旧版本**==
    
    1. **然后让一部分 用户请求打到新的 Pod 上，观察新的 Pod 是否能提供正常稳定的服务**
        
        1. 如果正常，则使用 `resume`继续完成余下的 Pod 资源滚动更新
        
        1. 否则，执行回滚更新操作
        
    
1. 示例：
    
    1. 对 pc-deployment 执行更新操作，同时 使用 `pause` 将更新操作暂停
        
        ```Plain
         [root@master ~]# kubectl set image deploy pc-deployment nginx=nginx:1.17.4 -n dev && kubectl rollout pause deployment pc-deployment  -n dev
         deployment.apps/pc-deployment image updated
         deployment.apps/pc-deployment paused
        ```
        
    
    1. 查看此时的 RS
        
        ```Plain
         [root@master ~]# kubectl get rs -n dev
         NAME                       DESIRED   CURRENT   READY   AGE
         pc-deployment-5d9c9b97bb   3         3         3       15h
         pc-deployment-6b575b9c45   1         1         0       23s
         pc-deployment-76fd8c7f84   0         0         0       15h
         pc-deployment-7c7477c7ff   0         0         0       15h
        ```
        
    
    1. 查看此时的 Pod（可以看到此时，由于使用的时滚动更新策略，这里新拉起一个 新版本的 Pod）
        
        ```Plain
         [root@master ~]#  kubectl get pods -n dev
         NAME                             READY   STATUS    RESTARTS   AGE
         pc-deployment-5d9c9b97bb-85zvg   1/1     Running   1          15h
         pc-deployment-5d9c9b97bb-m9fnk   1/1     Running   1          15h
         pc-deployment-5d9c9b97bb-s8c5k   1/1     Running   1          15h
         pc-deployment-6b575b9c45-p4w66   1/1     Running   0          48s
        ```
        
    
    1. 查看此时的更新状态
        
        ```Plain
         [root@master ~]# kubectl rollout status deploy pc-deployment -n dev
         Waiting for deployment "pc-deployment" rollout to finish: 1 out of 3 new replicas have been updated...
         Waiting for deployment "pc-deployment" rollout to finish: 1 out of 3 new replicas have been updated...
        ```
        
    
    1. 新开一个 shell 窗口，执行继续更新的命令
        
        ```Plain
         [root@master ~]#  kubectl rollout resume deploy pc-deployment -n dev
         deployment.apps/pc-deployment resumed
        ```
        
    
    1. 这时，观察更新状态的动态更新
        
        ```Plain
         [root@master ~]# kubectl rollout status deploy pc-deployment -n dev
         Waiting for deployment "pc-deployment" rollout to finish: 1 out of 3 new replicas have been updated...
         Waiting for deployment "pc-deployment" rollout to finish: 1 out of 3 new replicas have been updated...
         Waiting for deployment spec update to be observed...
         Waiting for deployment spec update to be observed...
         Waiting for deployment "pc-deployment" rollout to finish: 1 out of 3 new replicas have been updated...
         Waiting for deployment "pc-deployment" rollout to finish: 1 out of 3 new replicas have been updated...
         Waiting for deployment "pc-deployment" rollout to finish: 2 out of 3 new replicas have been updated...
         Waiting for deployment "pc-deployment" rollout to finish: 2 out of 3 new replicas have been updated...
         Waiting for deployment "pc-deployment" rollout to finish: 2 out of 3 new replicas have been updated...
         Waiting for deployment "pc-deployment" rollout to finish: 1 old replicas are pending termination...
         Waiting for deployment "pc-deployment" rollout to finish: 1 old replicas are pending termination...
         deployment "pc-deployment" successfully rolled out
        ```