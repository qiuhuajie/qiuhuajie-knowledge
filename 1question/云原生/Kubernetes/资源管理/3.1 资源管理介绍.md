---
title: "3.1 资源管理介绍"
tags:
  - "云原生"
  - "云原生/Kubernetes"
  - "云原生/Kubernetes/资源管理"
  - "资源管理"
  - "Kubernetes"
  - "K8s"
updated: 2026-04-16
---
# 一、资源管理介绍
1. 在 kubernetes 中==所有的内容都抽象为资源，====**用户需要通过操作资源来管理 kubernetes**==
2. 学习 kubernetes 的核心，就是**学习如何对集群上的**==**Pod、PodContrller、Service、Volume**==**存储等各种资源进行操作**

    ![[IMG-20260404031957925.png|800]]

    1. kubernetes的本质上就是一个集群系统，用户可以在集群中部署各种服务，所谓的部署服务，其实就是在kubernetes集群中运行一个个的容器，并将指定的程序跑在容器中
    2. kubernetes的最小管理单元是pod而不是容器，所以只能将容器放在`Pod`中，而kubernetes一般也不会直接管理Pod，而是通过`Pod`控制器来管理Pod的
    3. Pod可以提供服务之后，就要考虑如何访问Pod中服务，kubernetes提供了`Service`资源实现这个功能
    4. 当然，如果Pod中程序的数据需要持久化，kubernetes 还提供了各种存储系统
3. ==可以看到==**==几乎所有资源都是围绕 Pod 设计的==**
    - 控制器对象是用来管控 Pod 对象的
    - Service 或者 Ingress 资源对象是用来暴露 Pod 的
    - PersistentVolume 资源对象是用来为 Pod 提供存储的
