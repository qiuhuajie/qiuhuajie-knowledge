- [[#1. EmptyDir]]
- [[#2. HostPath]]
- [[#3. NFS]]
# 1. **EmptyDir**
1. EmptyDir是最基础的Volume类型
    
    1. **一个EmptyDir就是Host上的一个空目录**
    
    1. EmptyDir是**在Pod被分配到Node时创建**的，它的初始内容为空，并且无须指定宿主机上对应的目录文件，因为**kubernetes会自动分配**一个目录
    
    1. ==**当 Pod 销毁时， EmptyDir 中的数据也会被永久删除**==
    
1. ==**EmptyDir用途：**==
    
    - ==**临时空间**==**：**例如用于某些应用程序运行时所需的临时目录，且无须永久保留
    
    - ==**多容器共享目录**==：一个容器需要从另一个容器中获取数据的目录
    
    ![[IMG-20260404031957722.png|Untitled 409.png]]
    
1. 示例：
    
    1. 在一个Pod中准备两个容器nginx和busybox，然后声明一个Volume分别挂在到两个容器的目录中
    
    1. nginx容器负责向Volume中写日志，busybox中通过命令将日志内容读到控制台
    
    1. 创建一个volume-emptydir.yaml
        
        ```Plain
         apiVersion: v1
         kind: Pod
         metadata:
           name: volume-emptydir
           namespace: dev
         spec:
           containers:
           - name: nginx
             image: nginx:1.17.1
             ports:
             - containerPort: 80
             volumeMounts:  # ⭐nginx容器中的挂载点：将logs-volume挂在到nginx容器中，对应的目录为 /var/log/nginx
             - name: logs-volume
               mountPath: /var/log/nginx  # nginx 会向自己容器下的这个目录里写日志，但其实是写在了挂载的 volume中
           - name: busybox
             image: busybox:1.30
             command: ["/bin/sh","-c","tail -f /logs/access.log"] # 初始命令，动态读取指定文件中内容
             volumeMounts:  # ⭐busybox容器中的挂载点：将logs-volume 挂在到busybox容器中，对应的目录为 /logs
             - name: logs-volume
               mountPath: /logs
           volumes: # 声明volume， name为logs-volume，类型为emptyDir
           - name: logs-volume
             emptyDir: {}
        ```
        
        ```Plain
         # 创建Pod
         [root@k8s-master01 ~]# kubectl create -f volume-emptydir.yaml
         pod/volume-emptydir created
         
         # 查看pod
         [root@k8s-master01 ~]# kubectl get pods volume-emptydir -n dev -o wide
         NAME                  READY   STATUS    RESTARTS   AGE      IP       NODE   ......
         volume-emptydir       2/2     Running   0          97s   10.42.2.9   node1  ......
         
         # 通过podIp访问nginx
         [root@k8s-master01 ~]# curl 10.42.2.9
         ......
         
         # 通过kubectl logs命令查看指定容器的标准输出
         [root@k8s-master01 ~]# kubectl logs -f volume-emptydir -n dev -c busybox
         10.42.1.0 - - [27/Jun/2021:15:08:54 +0000] "GET / HTTP/1.1" 200 612 "-" "curl/7.29.0" "-"
        ```
        
    
# 2. **HostPath**
1. EmptyDir中数据不会被持久化，它会随着Pod的结束而销毁
1. 如果想**简单的将数据持久化到主机中**，可以选择HostPath
    
    1. **HostPath就是**==**将Node主机中一个实际目录挂在到Pod中**==**，以供容器使用**
    
    1. 这样的设计就**可以保证Pod销毁了，但是数据依据可以存在于Node主机上**
    
1. 示例：
    
    1. 创建一个volume-hostpath.yaml：
        
        ```Plain
         apiVersion: v1
         kind: Pod
         metadata:
           name: volume-hostpath
           namespace: dev
         spec:
           containers:
           - name: nginx
             image: nginx:1.17.1
             ports:
             - containerPort: 80
             volumeMounts:
             - name: logs-volume
               mountPath: /var/log/nginx
           - name: busybox
             image: busybox:1.30
             command: ["/bin/sh","-c","tail -f /logs/access.log"]
             volumeMounts:
             - name: logs-volume
               mountPath: /logs
           volumes:
           - name: logs-volume
             hostPath:               # ⭐
               path: /root/logs      # 指定 node 节点上的目录挂载点
               type: DirectoryOrCreate  # 目录存在就使用，不存在就先创建后使用
        ```
        
        - 关于type的值的一点说明：
            
            - DirectoryOrCreate 目录存在就使用，不存在就先创建后使用
            
            - Directory 目录必须存在
            
            - FileOrCreate 文件存在就使用，不存在就先创建后使用
            
            - File 文件必须存在
            
            - Socket unix套接字必须存在
            
            - CharDevice 字符设备必须存在
            
            - BlockDevice 块设备必须存在
            
        
    
    ```Plain
     # 创建Pod
     [root@k8s-master01 ~]# kubectl create -f volume-hostpath.yaml
     pod/volume-hostpath created
     
     # 查看Pod
     [root@k8s-master01 ~]# kubectl get pods volume-hostpath -n dev -o wide
     NAME                  READY   STATUS    RESTARTS   AGE   IP             NODE   ......
     pod-volume-hostpath   2/2     Running   0          16s   10.42.2.10     node1  ......
     
     \#访问nginx
     [root@k8s-master01 ~]# curl 10.42.2.10
     
     [root@k8s-master01 ~]# kubectl logs -f volume-emptydir -n dev -c busybox
     
     # 接下来就可以去host的/root/logs目录下查看存储的文件了
     ###  注意: 下面的操作需要到Pod所在的节点运行（案例中是node1）
     [root@node1 ~]# ls /root/logs/
     access.log  error.log
     
     # 同样的道理，如果在此目录下创建一个文件，到容器中也是可以看到的
     
     # 即使将 pod 删除，该文件也还存在
     [root@k8s-master01 ~]# kubectl delete -f volume-hostpath.yaml
     
     [root@node1 ~]# ls /root/logs/
     access.log  error.log
    ```
    
# 3. **NFS**
1. 存在的问题：
    
    1. HostPath可以解决数据持久化的问题
    
    1. 但是==**一旦Node节点故障了，Pod如果转移到了别的节点，就会读不到存在之前节点上的数据**==
    
1. 此时需要**准备单独的网络存储系统**
    
    1. 比较常用的用NFS、CIFS
    
    1. NFS是一个网络文件存储系统，可以搭建一台NFS服务器，然后将Pod中的存储直接连接到NFS系统上
    
1. 这样的话，无论Pod在节点上怎么转移，只要Node跟NFS的对接没问题，数据就可以成功访问
1. 示例：
    
    1. 首先要准备nfs的服务器，**新建一个 nfs 机器**
        
        ```Plain
         # 在nfs上安装nfs服务
         [root@nfs ~]# yum install nfs-utils -y
         
         # 准备一个共享目录
         [root@nfs ~]# mkdir /root/data/nfs -pv
         
         # ⭐将共享目录以读写权限暴露给192.168.5.0/24网段中的所有主机
         [root@nfs ~]# vim /etc/exports
         [root@nfs ~]# more /etc/exports
         /root/data/nfs     192.168.5.0/24(rw,no_root_squash)
         
         # 启动nfs服务
         [root@nfs ~]# systemctl start nfs
        ```
        
    
    1. 接下来，要在的每个node节点上都安装下nfs，这样的目的是为了node节点可以驱动nfs设备
        
        ```Plain
         # 在node上安装nfs服务，⭐注意只需要安装，不需要启动，否则也会变为 nfs 服务器
         [root@master ~]# yum install nfs-utils -y
         [root@node01 ~]# yum install nfs-utils -y
         [root@node02 ~]# yum install nfs-utils -y
        ```
        
    
    1. 接下来，就可以编写pod的配置文件了，创建volume-nfs.yaml
        
        ```Plain
         apiVersion: v1
         kind: Pod
         metadata:
           name: volume-nfs
           namespace: dev
         spec:
           containers:
           - name: nginx
             image: nginx:1.17.1
             ports:
             - containerPort: 80
             volumeMounts:
             - name: logs-volume
               mountPath: /var/log/nginx
           - name: busybox
             image: busybox:1.30
             command: ["/bin/sh","-c","tail -f /logs/access.log"]
             volumeMounts:
             - name: logs-volume
               mountPath: /logs
           volumes:
           - name: logs-volume
             nfs:                    # ⭐使用 nfs
               server: 192.168.5.6   # nfs服务器地址
               path: /root/data/nfs  # 共享文件路径
        ```
        
        - 这样**容器写入本容器的数据就会发送给：192.168.5.6上的/root/data/nfs目录中**
        
    
    1. 最后，运行下pod，观察结果
        
        ```Plain
         # 创建pod
         [root@k8s-master01 ~]# kubectl create -f volume-nfs.yaml
         pod/volume-nfs created
         
         # 查看pod
         [root@k8s-master01 ~]# kubectl get pods volume-nfs -n dev
         NAME                  READY   STATUS    RESTARTS   AGE
         volume-nfs        2/2     Running   0          2m9s
         
         # 访问nginx
         [root@k8s-master01 ~]# curl 10.42.2.10
         # 输出日志
         [root@k8s-master01 ~]# kubectl logs -f volume-emptydir -n dev -c busybox
         
         # 查看nfs服务器上的共享目录，发现已经有文件了
         [root@nfs ~]# ls /root/data/nfs
         access.log  error.log
        ```