---
title: "1. 数据库定时备份"
tags:
  - "云原生"
  - "云原生/Shell"
  - "云原生/Shell/Shell 编程案例"
  - "数据库定时备份"
  - "Shell 编程案例"
  - "Shell"
updated: 2026-04-16
---
# 1. 数据库定时备份
1. 需求分析：
    1. 每天凌晨 2:10 备份 数据库 atguiguDB 到 /data/backup/db
    2. 备份开始和备份结束能够给出相应的提示信息
    3. 备份后的文件要求以备份时间为文件名，并打包成 .tar.gz 的形式，比如： 2018-03-12_230201.tar.gz
    4. 在备份的同时，检查是否有10天前备份的数据库文件，如果有就将其删除
2. 准备数据库：

    ```Bash
     [root@hadoop001 home]# mysql -u root -p
     Enter password:
     Welcome to the MySQL monitor.  Commands end with ; or \g.
     Your MySQL connection id is 4
     Server version: 5.7.26 MySQL Community Server (GPL)
     Copyright (c) 2000, 2019, Oracle and/or its affiliates. All rights reserved.
     Oracle is a registered trademark of Oracle Corporation and/or its
     affiliates. Other names may be trademarks of their respective
     owners.
     Type 'help;' or '\h' for help. Type '\c' to clear the current input statement.
     mysql> create database qhj;
     Query OK, 1 row affected (0.01 sec)
     mysql> use qhj
     Database changed
     mysql> create table myorder(id int, name varchar(32));
     Query OK, 0 rows affected (0.12 sec)
     mysql> insert into myorder values(100, 'dog');
     Query OK, 1 row affected (0.01 sec)
     mysql> select * from myorder
         -> ;
     +------+------+
     | id   | name |
     +------+------+
     |  100 | dog  |
     +------+------+
     1 row in set (0.00 sec)
    ```
3. 编写脚本

    ```Bash
     #!/bin/bash
     \#备份目录
     BACKUP=/data/backup/db
     \#当前时间
     DATETIME=`date +%Y-%m-%d_%H%M%S`
     echo "DATETIME=$DATETIME"
     \#数据库的地址
     HOST=localhost
     \#数据库用户名
     DB_USER=root
     \#数据库密码
     DB_PW=hspedu100
     \#备份的数据库名
     DATABASE=hspedu
     \#创建备份目录，如果不存在，就创建
     [ ! -d "$BACKUP/$DATETIME" ] && mkdir -p "$BACKUP/$DATETIME"
     \#备份数据库
     mysqldump -u$DB_USER -p$DB_PW --host=$HOST -q -R --databases $DATABASE | gzip > $BACKUP/$DATETIME/$DATETIME.sql.gz
     \#将文件夹db下的时间文件打包成 tar.gz
     cd $BACKUP
     tar -zcvf $DATETIME.tar.gz $DATETIME
     \#删除对应的备份目录
     rm -rf $BACKUP/$DATETIME
     \#删除10天前的备份文件
     find $BACKUP -atime +10 -name "*.tar.gz" -exec rm -rf {} \;
     echo "备份数据库完成"
    ```
4. 测试

    ```Bash
     [root@hadoop001 home]# vim databackup.sh
     [root@hadoop001 home]# sh databackup.sh
     DATETIME=2021-07-09_225709
     mysqldump: [Warning] Using a password on the command line interface can be insecure.
     mysqldump: Got error: 1049: Unknown database 'hspedu' when selecting the database
     2021-07-09_225709/
     2021-07-09_225709/2021-07-09_225709.sql.gz
     备份数据库完成
     [root@hadoop001 home]# cd /
     [root@hadoop001 /]# ls
     bbb  bin  boot  data  dev  etc  home  lib  lib64  lost+found  media  mnt  newdisk  opt  proc  root  run  sbin  srv  sys  tmp  usr  var
     [root@hadoop001 /]# cd data
     [root@hadoop001 data]# ls
     backup
     [root@hadoop001 data]# cd backup
     [root@hadoop001 backup]# ls
     db
     [root@hadoop001 backup]# cd db
     [root@hadoop001 db]# ls
     2021-07-09_225709.tar.gz
    ```
5. 写入定时调度

    ```Bash
     [root@hadoop001 home]# crontab -e
     [root@hadoop001 home]# crontab -l
     30 2 * * * /home/musql_db_backup.sh
    ```
