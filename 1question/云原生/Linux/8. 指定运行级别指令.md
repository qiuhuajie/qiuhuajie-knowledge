# 8. 指定运行级别指令
1. 运行级别说明：
    
    - 0 ：关机
    
    - 1 ：单用户【找回丢失密码】
    
    - 2：多用户状态没有网络服务（无用）
    
    - 3：**多用户状态有网络服务**（用得最多）
    
    - 4：系统未使用保留给用户
    
    - 5：图形界面（一般默认）
    
    - 6：系统重启
    
1. 切换级别的指令：
    
    ```Plain
     [root@hadoop1 ~]# init 3
    
     [root@hadoop1 ~]# init 5
    ```
    
1. 常用运行级别是3和5 ，如果要修改默认的运行级别，可改文件设置
    
    1. 在centOS7之前，修改/ect/inittab文件中的配置，centOS7后进行了简化：
        

        `multi-user.target`：analogous to runlevel 3

        

        `graphical.target`：analogous to runlevel 5

        
    
    1. 使用命令查看当前默认运行级别：
        
        ```Plain
         [root@hadoop1 home]# systemctl get-default
         graphical.target
        ```
        
    
    1. 使用命令设置运行级别：
        
        ```Plain
         [root@hadoop1 home]# systemctl set-default multi-user.target
         Removed symlink /etc/systemd/system/default.target.
         Created symlink from /etc/systemd/system/default.target to /usr/lib/systemd/system/multi-user.target.
         
         [root@hadoop1 home]# systemctl get-default
         multi-user.target
        ```
        

        此时再重启会自动进入多用户级别：3

        

        ![[IMG-20260404032001053.png|Untitled 116.png]]