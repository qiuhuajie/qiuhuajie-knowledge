# 5. Vi 和 Vim 编辑器
1. 所有的 Linux 系统都会内建 vi 文本编辑器
1. Vim 具有程序编辑的能力，可以看做是Vi的增强版本，可以主动的以字体颜色辨别语法的正确性，方便程序设计。代码补完、编译及错误跳转等方便编程的功能特别丰富，在程序员中被广泛使用
1. vi和vim常用的三种模式
    
    1. 正常模式：以 vim 打开一个档案就直接进入一般模式了(这是默认的模式)。在这个模式中， 可以使用『上下左右』 按键来移动光标，可以使用『删除字符』 或『删除整行』 来处理档案内容，也可以使用『复制、贴上』 来处理你的文件数据
    
    1. 插入模式：按下i, I, o, O, a, A, r, R等任何一个字母之后才会进入编辑模式, 一般来说按i即可
    
    1. 命令行模式：在这个模式当中， 可以提供你相关指令，完成读取、存盘、替换、离开 vim 显示行号等的动作则是在此模式中达成的
    
1. 示例：
    
    1. 使用vim打开并创建一个文件
    
    1. 键入 i 进入插入模式
    
    1. esc退出插入模式，键入 wq 写入并退出
    
1. vi和vim的各个模式之间的互相转换：
    

    ![[IMG-20260404032001020.png|Untitled 115.png]]

    
1. ==**vi 和 vim的快捷键**==
    
    1. 拷贝当前行 `yy` , 拷贝当前行向下的5行 `5yy`，并粘贴（输入p）
    
    1. 删除当前行 `dd` , 删除当前行向下的5行 `5dd`
    
    1. 在文件中**查找**某个单词 [命令行下`/关键字`， 回车查找 , 输入`n`就是查找下一个 ]
    
    1. 设置文件的行号，取消文件的行号.[命令行下 `:set nu` 和 `:set nonu`]
    
    1. 编辑 /etc/profile文件，在一般模式下，使用快捷键到底文档的**最末行**[`G`]和最首行[`gg`]
    
    1. 编辑 /etc/profile 文件，在一般模式下，将**光标移动到 20行** `目标行数 + shift + g`
    
    1. 在一个文件中输入 "hello" ，然后在一般模式下，撤销这个动作 `u`
    
    1. 更多的看整理的文档