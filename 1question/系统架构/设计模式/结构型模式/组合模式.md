- [[#1. 介绍]]
- [[#2. 登场角色]]
    - [[#2.1 Composite 模式中有以下登场角色]]
    - [[#2.2 Composite 模式的类图]]
- [[#3. 代码示例]]
    - [[#3.1 需求描述]]
    - [[#3.2 传统方案解决学校院系]]
    - [[#3.3 Composite 模式解决学校院系]]
# 1. 介绍
![[IMG-20260315213656277.png|Untitled 262.png]]
1. **介绍：**
    
    1. 组合模式（Composite Pattern），又叫部分整体模式，是用于把一组相似的对象当作一个单一的对象
    
    1. 组合模式依据**树形结构**来组合对象，用来表示部分以及整体层次
    
    1. 这种模式创建了一个包含自己对象组的类。该类提供了修改相同对象组的方式
    
1. ==**意图：**==**==将对象组合成树形结构以表示"部分-整体"的层次结构。组合模式使得用户对单个对象和组合对象的使用具有一致性==**
1. ==**主要解决：**==**==它在我们树型结构的问题中，模糊了简单元素和复杂元素的概念，客户程序可以像处理简单元素一样来处理复杂元素，从而使得客户程序与复杂元素的内部结构解耦==**
1. **何时使用：**
    
    1. 您想表示对象的部分-整体层次结构（**树形结构**）
    
    1. 您希望用户**忽略组合对象与单个对象的不同，用户将统一地使用组合结构中的所有对象**
    
1. **如何解决：**树枝和叶子实现统一接口，树枝内部组合该接口
1. **关键代码：**树枝内部组合该接口，并且含有内部属性 List，里面放 Component
1. **应用实例：**
    
    1. 算术表达式包括操作数、操作符和另一个操作数，其中，另一个操作数也可以是操作数、操作符和另一个操作数
    
    1. 在 JAVA AWT 和 SWING 中，对于 Button 和 Checkbox 是树叶，Container 是树枝
    
1. **优点：**
    
    1. 客户端操作：客户端只需要面对一致的对象而不用考虑整体部分或者节点叶子的问题
    
    1. 具有较强的扩展性：当我们要更改组合对象时，我们只需要调整内部的层次关系，客户端不用做出任何改动
    
1. **缺点：**在使用组合模式时，其叶子和树枝的声明都是实现类，而不是接口，违反了依赖倒置原则
1. ==**使用场景：**==**==部分、整体场景，如树形菜单，文件、文件夹的管理==**
1. **注意事项：**
    
    1. 需要遍历组织机构，或者处理的对象具有树形结构时, 非常适合使用组合模式
    
    1. 但要求较高的抽象性， 如果节点和叶子有很多差异性的话，比如很多方法和属性都不一样， 不适合使用组合模式
    
# 2. 登场角色
## 2.1 Composite 模式中有以下登场角色
1. **Leaf(树叶)**
    
    1. 在组合中表示叶子节点，叶子节点没有子节点
    
    1. 是表示“内容”的角色
    
    1. 在该角色中不能放入其他对象
    
1. **Composite(复合物)**
    
    1. 非叶子节点，用于存储子部件，在 Component 接口中实现子部件的相关操作，比如增加（add），删除（remove）等
    
    1. 表示容器的角色
    
    1. 可以在其中放入 Leaf 角色和 Composite 角色
    
1. ⭐**Component**
    
    1. 这是组合中对象声明接口，在适当情况下，实现所有类共有的接口默认行为，**用于访问和管理 Component 子部件**
    
    1. Component 可以是抽象类或者接口
    
    1. 使 Leaf 角色和 Composite 角色具有一致性的角色
    
    1. Composite 角色是 Leaf 角色和 Composite 角色的父类
    
1. **Client**
    
    使用Composite模式的角色
    
## 2.2 Composite 模式的类图
![[IMG-20260315213656475.png|Untitled 1 192.png]]
# 3. 代码示例
## 3.1 需求描述
1. 编写程序展示一个学校院系结构：需求是这样，要在一个页面中展示出学校的院系组成，一个学校有多个学院，一个学院有多个系
1. 如图：
![[IMG-20260315213657541.png|Untitled 2 161.png]]
## 3.2 传统方案解决学校院系
1. 将学院看做是学校的子类，系是学院的子类，这样实际上是站在组织大小来进行分层次的
1. 存在的问题：在一个页面中展示出学校的院系组成，一个学校有多个学院，一个学院有多个系， 因此这种方案，不能很好实现的管理的操作，比如对学院、系的添加，删除，遍历等
1. 解决方案：使用**组合模式** ⬇️，**把学校、院、系都看做是组织结构，他们之间没有继承的关系，而是一个树形结构**，可以更好的实现管理操作
## 3.3 Composite 模式解决学校院系
![[IMG-20260315213658219.png|Untitled 3 122.png]]
1. `OrganizationComponent` ➡️ **Component**
    
    ```Java
    @Data
    @NoArgsConstructor
    @AllArgsConstructor
    public abstract class OrganizationComponent {
        private String name;
        private String des;
    
        protected  void add(OrganizationComponent organizationComponent) {
            //默认实现
            throw new UnsupportedOperationException();
        }
    
        protected  void remove(OrganizationComponent organizationComponent) {
            //默认实现
            throw new UnsupportedOperationException();
        }
    
        //方法print, 做成抽象的, 子类都需要实现
        protected abstract void print();
    }
    ```
    
1. `University` ➡️ **Composite(复合物)**
    
    ```Java
    @Data
    @NoArgsConstructor
    public class University extends OrganizationComponent{
    
        List<OrganizationComponent> organizationComponents = new ArrayList<OrganizationComponent>();   // Composite 中聚合的是 Component 类型的集合
    
        // 构造器
        public University(String name, String des) {
            super(name, des);
        }
    
        // 重写add
        @Override
        protected void add(OrganizationComponent organizationComponent) {
            organizationComponents.add(organizationComponent);
        }
    
        // 重写remove
        @Override
        protected void remove(OrganizationComponent organizationComponent) {
            organizationComponents.remove(organizationComponent);
        }
    
        // print方法，就是输出University 包含的学院
        @Override
        protected void print() {
            System.out.println("--------------" + getName() + "--------------");
            //遍历 organizationComponents
            for (OrganizationComponent organizationComponent : organizationComponents) {
                organizationComponent.print();
            }
        }
    }
    ```
    
1. `College` ➡️ **Composite(复合物)**
    
    ```Java
    @Data
    @NoArgsConstructor
    public class College extends OrganizationComponent{
        //List 中 存放的Department
        List<OrganizationComponent> organizationComponents = new ArrayList<OrganizationComponent>();
    
        // 构造器
        public College(String name, String des) {
            super(name, des);
        }
    
        // 重写add
        @Override
        protected void add(OrganizationComponent organizationComponent) {
            //  将来实际业务中，Colleage 的 add 和  University add 不一定完全一样
            organizationComponents.add(organizationComponent);
        }
    
        // 重写remove
        @Override
        protected void remove(OrganizationComponent organizationComponent) {
            organizationComponents.remove(organizationComponent);
        }
    
        // print方法，就是输出University 包含的学院
        @Override
        protected void print() {
            System.out.println("--------------" + getName() + "--------------");
            //遍历 organizationComponents
            for (OrganizationComponent organizationComponent : organizationComponents) {
                organizationComponent.print();
            }
        }
    }
    ```
    
1. `Department` ➡️ **Leaf(树叶)**
    
    ```Java
    @Data
    @NoArgsConstructor
    public class Department extends OrganizationComponent{
        //没有集合
        // 构造器
        public Department(String name, String des) {
            super(name, des);
        }
    
        //add , remove 就不用写了，因为他是叶子节点
    
        @Override
        protected void print() {
            System.out.println(getName());
        }
    }
    ```
    
1. `CompositeDemo` ➡️ **Client**
    
    ```Java
    public class CompositeDemo {
        public static void main(String[] args) {
            //从大到小创建对象 学校
            OrganizationComponent university = new University("清华大学", " 中国顶级大学 ");
    
            //创建 学院
            OrganizationComponent computerCollege = new College("计算机学院", " 计算机学院 ");
            OrganizationComponent infoEngineercollege = new College("信息工程学院", " 信息工程学院 ");
    
    
            //创建计算机学院下面的系(专业)
            computerCollege.add(new Department("软件工程", " 软件工程不错 "));
            computerCollege.add(new Department("网络工程", " 网络工程不错 "));
            computerCollege.add(new Department("计算机科学与技术", " 计算机科学与技术是老牌的专业 "));
    
            //创建计算机学院下面的系(专业)
            infoEngineercollege.add(new Department("通信工程", " 通信工程不好学 "));
            infoEngineercollege.add(new Department("信息工程", " 信息工程好学 "));
    
            //将学院加入到 学校
            university.add(computerCollege);
            university.add(infoEngineercollege);
    
            university.print();
            System.out.println("=====================================");
            computerCollege.print();
            System.out.println("=====================================");
            infoEngineercollege.print();
        }
    }
    ```
    
1. 数据结果
    
    ```Java
    --------------清华大学--------------
    --------------计算机学院--------------
    软件工程
    网络工程
    计算机科学与技术
    --------------信息工程学院--------------
    通信工程
    信息工程
    =====================================
    --------------计算机学院--------------
    软件工程
    网络工程
    计算机科学与技术
    =====================================
    --------------信息工程学院--------------
    通信工程
    信息工程
    ```
    
1. 类图
    
    ![[IMG-20260315213658702.png|Untitled 4 99.png]]