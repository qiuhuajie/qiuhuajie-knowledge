---
title: "外观模式"
tags:
  - "系统架构"
  - "系统架构/设计模式"
  - "系统架构/设计模式/结构型模式"
  - "外观模式"
  - "结构型模式"
  - "设计模式"
updated: 2026-04-16
---
# 一、介绍

![[IMG-20260315213656267.png|Untitled 263.png]]

1. **介绍：**
    1. 程序这东西总是会变得越来越大。随着时间的推移，程序中的类会越来越多，而且它们之间相互关联，这会导致程序结构也变得越来越复杂。我们在使用这些类之前，必须先弄清楚它们之间的关系，注意正确的调用顺序
    1. 特别是在调用大型程序进行处理时，我们需要格外注意那些数量庞大的类之错综复杂的关系。不过与其这么做，不如为这个大型程序准备一个“窗口”。这样，我们就不必单独地关注每个类了，只需简单地对“窗口”提出请求即可
    1. 这个“窗口”就是我们在本章中将要学习的 Facade 模式。Facade是一个源自法语 Facade的单词，它的意思是“建筑物的正面”
1. ==**意图：**==
    1. **可以为互相关联在一起的错综复杂的类整理出高层接口(API)**。其中的Facade角色可以让系统对外只有一个简单的接口(API)
    1. 而且，**Facade 角色还会考虑到系统内部各个类之间的责任关系和依赖关系，按照正确的顺序调用各个类**
    1. ==**这种模式涉及到一个单一的类，该类提供了客户端请求的简化方法和对现有系统类方法的委托调用**==
1. ==**主要解决：**==**==降低访问复杂系统的内部子系统时的复杂度，简化客户端之间的接口==**
1. **何时使用：**
    1. 客户端不需要知道系统内部的复杂联系，整个系统只需提供一个"接待员"即可
    1. 定义系统的入口
1. **如何解决：**客户端不与系统耦合，外观类与系统耦合
1. **关键代码：**在客户端和复杂系统之间再加一层，这一层将调用顺序、依赖关系等处理好
1. **应用实例：**
    1. 医院看病，可能要去挂号、门诊、划价、取药，让患者或患者家属觉得很复杂，如果有提供接待人员，只让接待人员来处理，就很方便
    1. **JAVA 的三层开发模式：⭐conroller 调用 service，甩锅式敲代码：conroller 不管具体实现**
1. **优点：**
    1. 减少系统相互依赖
    1. 提高灵活性
    1. 提高了安全性
1. **缺点：**不符合开闭原则，如果要改东西很麻烦，继承重写都不合适
1. **使用场景：**
    1. 为复杂的模块或子系统提供外界访问的模块
    1. 子系统相对独立
    1. 预防低水平人员带来的风险
1. **注意事项：**在层次化结构中，可以使用外观模式定义系统中每一层的入口
# 二、登场角色
## 1. Facade 模式中有以下登场角色
1. **Facade(窗口)**
    1. Facade角色是代表构成系统的许多其他角色的“简单窗口”
    1. Facade 角色向系统外部提供高层接口(API)
1. **构成系统的许多其他角色(子模块)**
    1. 这些角色各自完成自己的工作，是功能的实际执行者，它们并不知道 Facade 角色
    1. Facade角色调用其他角色进行工作，但是其他角色不会调用Facade角色
1. **Client(请求者)**

    Client 角色负责调用 Facade 角色

## 2. Facade 模式的类图

![[IMG-20260315213656470.png|Untitled 1 193.png]]

# 三、代码示例
## 1. 需求描述

组建一个家庭影院：DVD播放器、投影仪、自动屏幕、环绕立体声、爆米花机,要求完成使用家庭影院的功能，其过程为：

- 放下屏幕
- 开投影仪
- 开音响
- 开DVD，选dvd
- 调暗灯光
- 播放
- 观影结束后，关闭各种设备
## 2. Facade 模式解决
- 定义一个高层接口，给子系统中的一组接口提供一个一致的界面(比如在高层接口提供四个方法 `ready`, `play`, `pause`, `end` )，用来访问子系统中的一群接口
![[IMG-20260315213657478.png|Untitled 2 162.png]]
1. `DVDPlayer` ➡️ **构成系统的许多其他角色(子模块)**

    ```Java
    public class DVDPlayer {
        //使用单例模式, 使用饿汉式
        private static DVDPlayer instance = new DVDPlayer();
        public static DVDPlayer getInstance() {
            return instance;
        }
        public void on() {
            System.out.println(" dvd on ");
        }
        public void off() {
            System.out.println(" dvd off ");
        }
        public void play() {
            System.out.println(" dvd is playing ");
        }
        public void pause() {
            System.out.println(" dvd is paused ");
        }
    }
    ```
1. `Projector` ➡️ **构成系统的许多其他角色(子模块)**

    ```Java
    public class Projector {
        private static Projector instance = new Projector();
        public static Projector getInstance() {
            return instance;
        }
        public void on() {
            System.out.println(" Projector on ");
        }
        public void off() {
            System.out.println(" Projector off ");
        }
        public void focus() {
            System.out.println(" Projector is Projector  ");
        }
    }
    ```
1. `Screen` ➡️ **构成系统的许多其他角色(子模块)**

    ```Java
    public class Screen {
        private static Screen instance = new Screen();
        public static Screen getInstance() {
            return instance;
        }
        public void up() {
            System.out.println(" Screen up ");
        }
        public void down() {
            System.out.println(" Screen down ");
        }
    }
    ```
1. `Stereo` ➡️ **构成系统的许多其他角色(子模块)**

    ```Java
    public class Stereo {
        private static Stereo instance = new Stereo();
        public static Stereo getInstance() {
            return instance;
        }
        public void on() {
            System.out.println(" Stereo on ");
        }
        public void off() {
            System.out.println(" Screen off ");
        }
        public void up() {
            System.out.println(" Screen up.. ");
        }
    }
    ```
1. `HomeTheaterFacade` ➡️ **Facade(窗口)**

    ```Java
    public class HomeTheaterFacade {
        //定义各个子系统对象
        private Stereo stereo;
        private Projector projector;
        private Screen screen;
        private DVDPlayer dVDPlayer;
        //构造器
        public HomeTheaterFacade() {
            super();
            this.stereo = Stereo.getInstance();
            this.projector = Projector.getInstance();
            this.screen = Screen.getInstance();
            this.dVDPlayer = DVDPlayer.getInstance();
        }
        //⭐
        public void ready() {
            screen.down();
            projector.on();
            stereo.on();
            dVDPlayer.on();
        }
        public void play() {
            dVDPlayer.play();
        }
        public void pause() {
            dVDPlayer.pause();
        }
        public void end() {
            screen.up();
            projector.off();
            stereo.off();
            dVDPlayer.off();
        }
    }
    ```
1. `FacadeDemo` ➡️ **Client(请求者)**

    ```Java
    public class FacadeDemo {
        public static void main(String[] args) {
            HomeTheaterFacade homeTheaterFacade = new HomeTheaterFacade();
            homeTheaterFacade.ready();
            System.out.println("====================");
            homeTheaterFacade.play();
            System.out.println("====================");
            homeTheaterFacade.pause();
            System.out.println("====================");
            homeTheaterFacade.end();
        }
    }
    ```
1. 运行结果

    ```Java
    Screen down
    Projector on
    Stereo on
    dvd on
    ====================
    dvd is playing
    ====================
    dvd is paused
    ====================
    Screen up
    Projector off
    Screen off
    dvd off
    ```
# 四、外观模式在MyBatis框架应用的源码分析
    MyBatis 中的`Configuration`去创建`MetaObject`对象使用到外观模式
    1. `Configuration` 类中有一个 `newMetaObject` 方法

        ![[IMG-20260315213658160.png|Untitled 3 123.png]]
    1. **这个方法是由** `MetaObject` **类提供，这个**`MetaObject` **类就可以看作**==**Facade(窗口)**==**，它提供了一个面向调用者的简单的用来新建** `MetaObject`**对象的方法**

        ![[IMG-20260315213658635.png|Untitled 4 100.png]]
