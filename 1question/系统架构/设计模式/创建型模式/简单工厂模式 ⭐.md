---
title: "简单工厂模式 ⭐"
tags:
  - "系统架构"
  - "系统架构/设计模式"
  - "系统架构/设计模式/创建型模式"
  - "简单工厂模式"
  - "创建型模式"
  - "设计模式"
updated: 2026-04-16
---
# 一、介绍

![[IMG-20260315213656183.png|Untitled 255.png]]

1. **介绍：**
    1. （简单）工厂模式（Factory Pattern）是 Java 中最常用的设计模式之一。这种类型的设计模式属于创建型模式，它提供了一种创建对象的最佳方式
    1. 在工厂模式中，我们在创建对象时不会对客户端暴露创建逻辑，并且是通过使用一个共同的接口来指向新创建的对象
1. ==**意图：**==**==定义一个创建对象的接口，让其子类自己决定实例化哪一个工厂类，工厂模式使其创建过程延迟到子类进行==**
1. ==**主要解决：**==**==主要解决接口选择的问题==**
1. **何时使用：**我们明确地计划不同条件下创建不同实例时
1. **如何解决：**让其子类实现工厂接口，返回的也是一个抽象的产品
1. **关键代码：**创建过程在其子类执行
1. **应用实例：**
    1. 您需要一辆汽车，可以直接从工厂里面提货，而不用去管这辆汽车是怎么做出来的，以及这个汽车里面的具体实现
    1. Hibernate 换数据库只需换方言和驱动就可以
1. **优点：**
    1. 一个调用者想创建一个对象，只要知道其名称就可以了
    1. 扩展性高，如果想增加一个产品，只要扩展一个工厂类就可以
    1. 屏蔽产品的具体实现，调用者只关心产品的接口
1. **缺点：**每次增加一个产品时，都需要增加一个具体类和对象实现工厂，使得系统中类的个数成倍增加，在一定程度上增加了系统的复杂度，同时也增加了系统具体类的依赖。这并不是什么好事。
1. ==**使用场景：**==
    1. 日志记录器：记录可能记录到本地硬盘、系统事件、远程服务器等，用户可以选择记录日志到什么地方
    1. 数据库访问，当用户不知道最后系统采用哪一类数据库，以及数据库可能有变化时
    1. 设计一个连接服务器的框架，需要三个协议，"POP3"、"IMAP"、"HTTP"，可以把这三个作为产品类，共同实现一个接口
1. **注意事项：**
    1. 作为一种创建类模式，在任何需要生成复杂对象的地方，都可以使用工厂方法模式
    1. 有一点需要注意的地方就是**复杂对象适合使用工厂模式**，而简单对象，特别是只需要通过 new 就可以完成创建的对象，无需使用工厂模式。如果使用工厂模式，就需要引入一个工厂类，会增加系统的复杂度。

# 二、登场角色
## 1. Factory 模式中有以下登场角色
1. **Product(产品)**
    1. Product角色属于框架这一方，是一个抽象类
    1. 它定义了在 Factory Method模式中生成的那些实例所持有的接口(API)，但具体的处理则由子类 ConcreteProduct角色决定
1. **Creator(创建者 )**
    1. Creator角色属于框架这一方，它是负责生成 Product 角色的抽象类，但具体的处理则由子类ConcreteCreator角色决定
    1. Creator角色对于实际负责生成实例的ConcreteCreator角色一无所知，它唯一知道的就是，只要调用Product角色和生成实例的方法，就可以生成Productde 的实例
    1. 不用new关键字来生成实例，而是调用生成实例的专用方法来生成实例，这样就可以防止父类与其他具体类耦合
1. **ConcreteProduct(具体的产品)**
    1. ConcreteProduct 角色属于具体加工这一方，它决定了具体的产品
1. **ConcreteCreator(具体的创建者 )**
    1. ConcreteCreator角色属于具体加工这一方，它负责生成具体的产品

## 2. Factory 模式的类图

![[IMG-20260315213656424.png|Untitled 1 185.png]]

# 三、代码示例
## 1. 披萨的项目功能需求
1. 要便于披萨种类的扩展，要便于维护
1. 披萨的种类很多，比如 SausagePizz、 SausagePizz 等
1. 披萨的制作有 bake
1. 完成披萨店订购功能
## 2. 使用传统的方式来完成
1. Pizza 抽象类

    ```Java
    public abstract class Pizza {
        protected String name;
        public abstract void bake();
        public void setName(String name) {
            this.name = name;
        }
    }
    ```
1. SausagePizz、SausagePizz 继承 Pizza

    ```Java
    public class CheesePizza extends Pizza {
        @Override
        public void bake() {
            System.out.println(name + " is baked");
        }
    }
    ```
    ```Java
    public class SausagePizza extends Pizza{
        @Override
        public void bake() {
            System.out.println(name + " is baked");
        }
    }
    ```
1. 订单服务 A，在订单服务中创建 Pizza 实例

    ```Java
    public class PizzaOrderA {
        public PizzaOrderA(String orderType) {
    				Pizza pizza = null;
    				if (orderType.equals("sausage")) {
    					pizza = new SausagePizza();
    					pizza.setName("香肠披萨");
    				} else if (orderType.equals("cheese")) {
    					pizza = new CheesePizza();
    					pizza.setName("奶酪披萨");
    				}
    				// 制作
    				pizza.bake();
    		}
    }
    ```
1. 订单服务 B，代码与订单服务 A 完全一样，也在订单服务中创建 Pizza 实例

    ```Java
    public class PizzaOrderB {
        public PizzaOrderA(String orderType) {
    				Pizza pizza = null;
    				if (orderType.equals("sausage")) {
    					pizza = new SausagePizza();
    					pizza.setName("香肠披萨");
    				} else if (orderType.equals("cheese")) {
    					pizza = new CheesePizza();
    					pizza.setName("奶酪披萨");
    				}
    				// 制作
    				pizza.bake();
    		}
    }
    ```
1. 分析：
    1. 类图：

        ![[IMG-20260315213657017.png|Untitled 2 155.png]]

    1. 优点：是比较好理解，简单易操作
    1. **缺点：**是违反了设计模式的 ocp 原则，如我们这时要新增加一个Pizza的种类（Pepper披萨）
        1. 需要新增一个 PepperPizza 类
        1. 同时修改 PizzaOrderB 和 PizzaOrderA 中的创建 Pizza 实例的逻辑代码，**但如果有 100 个服务中都有创建 Pizza 实例的逻辑代码，那么岂不是都需要修改**

            ![[IMG-20260315213657730.png|Untitled 3 118.png]]

    1. **使用 简单工厂模式进行优化 ⬇️**

## 3. 使用简单工厂模式
1. Pizza 抽象类 ➡️ **Product(产品)**

    ```Java
    public abstract class Pizza {
        protected String name;
        public abstract void bake();
        public void setName(String name) {
            this.name = name;
        }
    }
    ```
1. SausagePizz、SausagePizz 继承 Pizza ➡️ **ConcreteProduct(具体的产品)**

    ```Java
    public class CheesePizza extends Pizza {
        @Override
        public void bake() {
            System.out.println(name + " is baked");
        }
    }
    ```
    ```Java
    public class SausagePizza extends Pizza{
        @Override
        public void bake() {
            System.out.println(name + " is baked");
        }
    }
    ```
1. ⭐`PizzaFactory` 类：原先订单服务 A、B，在订单服务中创建 Pizza 实例的代码都写在这里 ➡️ **Creator(创建者 )**

    ```Java
    public class PizzaFactory {
        public Pizza createPizza(String type) {
            Pizza pizza = null;
            if (type.equals("sausage")) {
                pizza = new SausagePizza();
                pizza.setName("香肠披萨");
            } else if (type.equals("cheese")) {
                pizza = new CheesePizza();
                pizza.setName("奶酪披萨");
            }
            return pizza;
        }
    }
    ```
1. 订单服务 A、B ➡️ **ConcreteCreator(具体的创建者 )**

    ```Java
    public class PizzaOrderA {
    		PizzaFactory pizzaFactory = null;
    		Pizza pizza = null;
    		public PizzaOrderA(PizzaFactory pizzaFactory, String orderType) {
    				// 使用统一的工厂来创建 Pizza 实例
    				this.pizzaFactory = pizzaFactory;
    				pizza = pizzaFactory.createPizza(orderType);
    				// 制作
    				this.pizza.bake();
    		}
    }
    ```
    ```Java
    public class PizzaOrderB {
        PizzaFactory pizzaFactory = null;
        Pizza pizza = null;
    		public PizzaOrderB(PizzaFactory pizzaFactory, String orderType) {
    				// 使用统一的工厂来创建 Pizza 实例
    				this.pizzaFactory = pizzaFactory;
    				pizza = pizzaFactory.createPizza(orderType);
    				// 制作
    				this.pizza.bake();
    		}
    }
    ```
1. 分析：
    1. 类图：

        ![[IMG-20260315213658324.png|Untitled 4 95.png]]

    1. **定义了一个创建对象的类 ：`PizzaFactory` 类，由这个类来封装实例化对象的行为**
    1. 在软件开发中，当我们会用到大量的创建某种、某类或者某批对象时，就会使用到工厂模式

# 四、简单工厂模式在 JDK 中的使用

![[IMG-20260315213658816.png|Untitled 5 80.png]]

![[IMG-20260315213659295.png|Untitled 6 69.png]]

![[IMG-20260315213659641.png|Untitled 7 59.png]]
