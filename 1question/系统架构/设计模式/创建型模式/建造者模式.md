# 1. 介绍

![[IMG-20260315213656175.png|Untitled 258.png]]

1. **介绍：**
    
    1. 建造者模式（Builder Pattern）**使用多个简单的对象一步一步构建成一个复杂的对象**。这种类型的设计模式属于创建型模式，它提供了一种创建对象的最佳方式
    
    1. 一个 Builder 类会一步一步构造最终的对象。该 Builder 类是独立于其他对象的
    
1. ==**意图：**==**==将一个复杂的构建与其表示相分离，使得同样的构建过程可以创建不同的表示==**
1. ==**主要解决：**==
    
    1. 主要解决在软件系统中，有时候面临着==**"一个复杂对象"的创建工作**==，其通常由各个部分的子对象用一定的算法构成
    
    1. 且由于需求的变化，**==这个复杂对象的各个部分经常面临着剧烈的变化==**，但是将它们组合在一起的算法却相对稳定
    
1. **何时使用：**一些基本部件不会变，而其组合经常变化的时候
1. **如何解决：**将变与不变分离开
1. **关键代码：**建造者：创建和提供实例，导演：管理建造出来的实例的依赖关系
1. **应用实例：** 
    
    1. 去肯德基，汉堡、可乐、薯条、炸鸡翅等是不变的，而其组合是经常变化的，生成出所谓的**"套餐"**
    
    1. JAVA 中的 StringBuilder
    
1. **优点：** 
    
    1. 建造者独立，易扩展
    
    1. 便于控制细节风险
    
1. **缺点：**
    
    1. 产品必须有共同点，范围有限制
    
    1. 如内部变化复杂，会有很多的建造类
    
1. ==**使用场景：**==
    
    1. 需要生成的对象具有复杂的内部结构
    
    1. 需要生成的对象内部属性本身相互依赖
    
1. **注意事项：**==**与工厂模式的区别是：建造者模式更加关注⭐零件装配的过程**==
# 2. 登场角色
## 2.1 Builder 模式中有以下登场角色
1. **Builder(建造者)**
    
    1. Builder角色负责定义用于生成实例的接口(API)
    
    1. Builder 角色中准备了用于生成实例的方法
    
1. **ConcreteBuilder(具体的建造者)**
    
    1. ConcreteBuilder角色是负责实现Builder角色的接口的类(API)。这里定义了在生成实例时实际被调用的方法
    
    1. 此外，在ConcreteBuilder 角色中还定义了获取最终生成结果的方法
    
1. **Director(监工)**
    
    1. Director角色负责使用 Builder角色的接口(API)来生成实例
    
    1. 它并不依赖于ConcreteBuilder角色
    
    1. 为了确保不论ConcreteBuilder 角色是如何被定义的，Director角色都能正常工作，它只调用在Builder角色中被定义的方法
    
1. **Client(使用者)**
    
    1. 该角色使用了 Builder模式
    
    1. 它主要有两个作用，一是：隔离了客户与对象的生产过程，二是：负责控制产品对象的生产过程
    
## 2.2 Builder 模式的类图

![[IMG-20260315213656418.png|Untitled 1 188.png]]

# 3. 代码示例
1. 需求
    
    1. 需要建房子：这一过程为打桩、 砌墙、封顶
    
    1. 房子有各种各样的，比如普通房，高楼，别墅，各种房子的过程虽然一样，但是要求是不相同的
    
1. `House` 实体类
    
    ```Java
    @Data
    public class House {
        private String name;
    }
    ```
    
1. `HouseBuilder` **Builder(建造者)**
    
    ```Java
    public abstract class HouseBuilder {
    
        House house = new House();
    
        //打地基
        public abstract void buildBasic();
        //砌墙
        public abstract void buildWalls();
        //封顶
        public abstract void roofed();
    
        public House build() {
            return house;
        }
    }
    ```
    
1. **ConcreteBuilder(具体的建造者)**：`CommonHouseBuilder` `HighBuildingBuilder`
    
    > [!important] **解耦：**
    > 
    > 将产品（即：房子） 和 创建产品的过程（即：建房子流程）分别放在 `HouseBuilder` 和 `具体的HouseBuilder` 中
    
    ```Java
    public class CommonHouseBuilder extends HouseBuilder {
        @Override
        public void buildBasic() {
            System.out.println(" 普通房子打地基 ");
        }
    
        @Override
        public void buildWalls() {
            System.out.println(" 普通房子砌墙 ");
        }
    
        @Override
        public void roofed() {
            System.out.println(" 普通房子封顶 ");
        }
    }
    ```
    
    ```Java
    public class HighBuildingBuilder extends HouseBuilder {
        @Override
        public void buildBasic() {
            System.out.println(" 高楼打地基 ");
        }
    
        @Override
        public void buildWalls() {
            System.out.println(" 高楼打地基 ");
        }
    
        @Override
        public void roofed() {
            System.out.println(" 高楼打地基 ");
        }
    }
    ```
    
1. `HouseDirector` **Director(监工)**
    
    > [!important] Director类的构造函数 或
    > 
    > `setxxx()`的参数是 Builder类型的
    > 
    > - 但是实际上我们并不会将 Builder类的实例作为参数传递给 Director类。这是因为 Builder类是抽象类，是无法生成其实例的
    > 
    > - 实际上传递给Director类的是**Builder类的子类的实例**。而正是这些Builder类的子类决定了造出的房子的形式
    
    ```Java
    public class HouseDirector {
        HouseBuilder houseBuilder = null;
    
        public void setHouseBuilder(HouseBuilder houseBuilder) {
            this.houseBuilder = houseBuilder;
        }
    
        public House constructHouse() {
            houseBuilder.buildBasic();
            houseBuilder.buildWalls();
            houseBuilder.roofed();
            return houseBuilder.build();
        }
    }
    ```
    
1. `BuilderDemo` **Client(使用者)**
    
    ```Java
    public class BuilderDemo {
        public static void main(String[] args) {
    
            HouseDirector houseDirector = new HouseDirector();
    
            CommonHouseBuilder commonHouseBuilder = new CommonHouseBuilder();
            houseDirector.setHouseBuilder(commonHouseBuilder);
            House commonHouse = houseDirector.constructHouse();
    
            HighBuildingBuilder highBuildingBuilder = new HighBuildingBuilder();
            houseDirector.setHouseBuilder(highBuildingBuilder);
            House highBuilding = houseDirector.constructHouse();
        }
    }
    ```
    
1. 运行结果
    
    ```Java
    普通房子打地基 
    普通房子砌墙 
    普通房子封顶 
    高楼打地基 
    高楼打地基 
    高楼打地基
    ```
    
1. 类图分析：
    

    ![[IMG-20260315213656970.png|Untitled 2 157.png]]