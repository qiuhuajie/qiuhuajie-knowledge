- [[#1. 介绍]]
- [[#2. 登场角色]]
    - [[#2.1 Observer 模式中有以下登场角色]]
    - [[#2.2 Observer 模式的类图]]
- [[#3. 代码示例]]
    - [[#3.1 需求描述]]
    - [[#3.2 普通方案]]
    - [[#3.3 使用观察者模式]]
- [[#4. 观察者模式在 JDK 应用的源码分析]]
# 1. 介绍
![[IMG-20260315213656211.png|Untitled 270.png]]
**发送状态变化通知**
1. **介绍：**
    
    1. 当对象间存在一对多关系时，则使用观察者模式（Observer Pattern）
    
    1. 比如，当一个对象被修改时，则会自动通知依赖它的对象。观察者模式属于行为型模式。
    
1. **意图：**定义对象间的一种一对多的依赖关系，当一个对象的状态发生改变时，所有依赖于它的对象都得到通知并被自动更新
1. **主要解决：**一个对象状态改变给其他对象通知的问题，而且要考虑到易用和低耦合，保证高度的协作
1. **何时使用：**一个对象（目标对象）的状态发生改变，所有的依赖对象（观察者对象）都将得到通知，进行广播通知
1. **如何解决：**使用面向对象技术，可以将这种依赖关系弱化
1. **关键代码：**在抽象类里有一个 ArrayList 存放观察者们
1. **应用实例：** 
    
    1. 拍卖的时候，拍卖师观察最高标价，然后通知给其他竞价者竞价
    
    1. 西游记里面悟空请求菩萨降服红孩儿，菩萨洒了一地水招来一个老乌龟，这个乌龟就是观察者，他观察菩萨洒水这个动作
    
1. **优点：**
    
    1. 观察者和被观察者是抽象耦合的
    
    1. 建立一套触发机制
    
1. **缺点：**
    
    1. 如果一个被观察者对象有很多的直接和间接的观察者的话，将所有的观察者都通知到会花费很多时间
    
    1. 如果在观察者和观察目标之间有循环依赖的话，观察目标会触发它们之间进行循环调用，可能导致系统崩溃
    
    1. 观察者模式没有相应的机制让观察者知道所观察的目标对象是怎么发生变化的，而仅仅只是知道观察目标发生了变化
    
1. **使用场景：**
    
    - 一个抽象模型有两个方面，其中一个方面依赖于另一个方面。将这些方面封装在独立的对象中使它们可以各自独立地改变和复用。
    
    - 一个对象的改变将导致其他一个或多个对象也发生改变，而不知道具体有多少对象将发生改变，可以降低对象之间的耦合度。
    
    - 一个对象必须通知其他对象，而并不知道这些对象是谁。
    
    - 需要在系统中创建一个触发链，A对象的行为将影响B对象，B对象的行为将影响C对象……，可以使用观察者模式创建一种链式触发机制。
    
1. **注意事项：**
    
    1. JAVA 中已经有了对观察者模式的支持类
    
    1. 避免循环引用
    
    1. 如果顺序执行，某一观察者错误会导致系统卡壳，一般采用异步方式。
    
# 2. 登场角色
## 2.1 Observer 模式中有以下登场角色
1. **Subject(被观察对象)**
    
    1. Subject角色表示被观察对象
    
    1. Subject角色定义了注册观察者和删除观察者的方法。此外，它还声明了“获取现在的状态”的方法
    
1. **ConcreteSubiect( 具体的被观察对象)**
    
    1. ConcreteSubject角色表示具体的被观察对象
    
    1. 当自身状态发生变化后，它会通知所有已经注册的Observer 角色
    
1. **Observer(观察者)**
    
    Observer 角色负责接收来自 Subject 角色的状态变化的通知。为此，它声明了**`update`** 方法
    
1. **ConcreteObserver( 具体的观察者)**
    
    1. Concretebserver 角色表示具体的 Observer
    
    1. 当它的 `update` 方法被调用后，会去获取被观察对象的最新状态
    
## 2.2 Observer 模式的类图
![[IMG-20260315213656447.png|Untitled 1 200.png]]
# 3. 代码示例
## 3.1 需求描述
1. 气象站需要将最新测量到的天气数据**==推送==**给不同的新闻网站
1. 新闻网站有 `WechatNews` 、`BaiduNews`
## 3.2 普通方案
1. `WeatherData`
    
    ```Java
    @Data
    public class WeatherData {
        private float temperatrue;
        private float pressure;
        private float humidity;
    
        //加入第三方
        private WechatNews wechatNews;
    
        public WeatherData(WechatNews currentConditions) {
            this.wechatNews = currentConditions;
        }
    
        public void dataChange() {
            //⭐调用 接入方的 update
            wechatNews.update(getTemperatrue(), getPressure(), getHumidity());
        }
    
        //当数据有更新时，就调用 setData
        public void setData(float temperature, float pressure, float humidity) {
            this.temperatrue = temperature;
            this.pressure = pressure;
            this.humidity = humidity;
            //调用dataChange， 将最新的信息 推送给 接入方 currentConditions
            dataChange();
        }
    }
    ```
    
1. `WechatNews`
    
    ```Java
    public class WechatNews {
        // 温度，气压，湿度
        private float temperature;
        private float pressure;
        private float humidity;
    
        //更新 天气情况，是由 WeatherData 来调用
        public void update(float temperature, float pressure, float humidity) {
            this.temperature = temperature;
            this.pressure = pressure;
            this.humidity = humidity;
            display();
        }
    
        //显示
        public void display() {
            System.out.println("Today mTemperature: " + temperature);
            System.out.println("Today mPressure: " + pressure);
            System.out.println("Today mHumidity: " + humidity);
        }
    }
    ```
    
1. `ObserverDemo`
    
    ```Java
    public class ObserverDemo {
        public static void main(String[] args) {
            //创建接入方 wechat
            WechatNews wechatNews = new WechatNews();
            //创建 WeatherData 并将 接入方 wechat 传递到 WeatherData中
            WeatherData weatherData = new WeatherData(wechatNews);
    
            //更新天气情况
            weatherData.setData(30, 150, 40);
    
            //天气情况变化
            System.out.println("============天气情况变化=============");
            weatherData.setData(40, 160, 20);
        }
    }
    ```
    
1. 运行结果
    
    ```Java
    Temperature For WechatNews: 30.0
    Pressure For WechatNews: 150.0
    Humidity For WechatNews: 40.0
    ============天气情况变化=============
    Temperature For WechatNews: 40.0
    Pressure For WechatNews: 160.0
    Humidity For WechatNews: 20.0
    ```
    
1. **存在的问题：**
    
    1. 无法在运行时动态的添加其它第三方新闻网站
    
    1. 违反`ocp`原则
        
        在WeatherData中， 当增加一个第三方，都需要创建一个对应的第三方的公告板对象，并加入到dataChange，不利于维护，也不是动态加入
        
        ```Java
        @Data
        public class WeatherData {
        
            private WechatNews wechatNews;
        		// 新的新闻网站需要手动添加，即需要修改 WeatherData 的代码
        ```
        
    
## 3.3 使用观察者模式
1. `Subject` ➡️ **Subject(被观察对象)**
    
    ```Java
    public interface Subject {
        void registerObserver(Observer o);
        void removeObserver(Observer o);
        void notifyObservers();
    }
    ```
    
1. `WeatherData` ➡️ **ConcreteSubiect( 具体的被观察对象)**
    
    ```Java
    @Data
    public class WeatherData implements Subject{
        private float temperature;
        private float pressure;
        private float humidity;
    
        //观察者集合
        private ArrayList<Observer> observers;
    
        public WeatherData() {
            observers = new ArrayList<Observer>();
        }
    
        //当数据有更新时，就调用 setData
        public void setData(float temperature, float pressure, float humidity) {
            this.temperature = temperature;
            this.pressure = pressure;
            this.humidity = humidity;
            // 将新值推送给观察者
            notifyObservers();
        }
    
        @Override
        public void registerObserver(Observer o) {
            observers.add(o);
        }
    
        @Override
        public void removeObserver(Observer o) {
            if(observers.contains(o)) {
                observers.remove(o);
            }
        }
    
        @Override
        public void notifyObservers() {
            for(int i = 0; i < observers.size(); i++) {
                // 给每个观察者推送更新
                observers.get(i).update(this.temperature, this.pressure, this.humidity);
            }
        }
    }
    ```
    
1. `Observer` ➡️ **Observer(观察者)**
    
    ```Java
    public interface Observer {
        void update(float temperature, float pressure, float humidity);
    }
    ```
    
1. `BaiduNews` ➡️ **ConcreteObserver( 具体的观察者)**
    
    ```Java
    public class BaiduNews implements Observer{
        // 温度，气压，湿度
        private float temperature;
        private float pressure;
        private float humidity;
    
        //更新 天气情况，是由 WeatherData 来调用
        public void update(float temperature, float pressure, float humidity) {
            this.temperature = temperature;
            this.pressure = pressure;
            this.humidity = humidity;
            display();
        }
    
        //显示
        public void display() {
            System.out.println("Temperature For BaiduNews: " + temperature);
            System.out.println("Pressure For BaiduNews: " + pressure);
            System.out.println("Humidity For BaiduNews: " + humidity);
        }
    }
    ```
    
1. `WechatNews` ➡️ **ConcreteObserver( 具体的观察者)**
    
    ```Java
    public class WechatNews implements Observer{
        // 温度，气压，湿度
        private float temperature;
        private float pressure;
        private float humidity;
    
        //更新 天气情况，是由 WeatherData 来调用
        public void update(float temperature, float pressure, float humidity) {
            this.temperature = temperature;
            this.pressure = pressure;
            this.humidity = humidity;
            display();
        }
    
        //显示
        public void display() {
            System.out.println("Temperature For WechatNews: " + temperature);
            System.out.println("Pressure For WechatNews: " + pressure);
            System.out.println("Humidity For WechatNews: " + humidity);
        }
    }
    ```
    
1. `ObserverDemo`
    
    ```Java
    public class ObserverDemo {
        public static void main(String[] args) {
    
            WeatherData weatherData = new WeatherData();
            weatherData.registerObserver(new WechatNews());
            weatherData.registerObserver(new BaiduNews());
    
            //更新天气情况，当数据有更新时，就调用 setData
            weatherData.setData(30, 150, 40);
    
            //天气情况变化
            System.out.println("============天气情况变化=============");
            weatherData.setData(40, 160, 20);
        }
    }
    ```
    
1. 运行结果
    
    ```Java
    Temperature For WechatNews: 30.0
    Pressure For WechatNews: 150.0
    Humidity For WechatNews: 40.0
    Temperature For BaiduNews: 30.0
    Pressure For BaiduNews: 150.0
    Humidity For BaiduNews: 40.0
    ============天气情况变化=============
    Temperature For WechatNews: 40.0
    Pressure For WechatNews: 160.0
    Humidity For WechatNews: 20.0
    Temperature For BaiduNews: 40.0
    Pressure For BaiduNews: 160.0
    Humidity For BaiduNews: 20.0
    ```
    
1. 类图
    
    ![[IMG-20260315213657219.png|Untitled 2 169.png]]
    
# 4. 观察者模式在 JDK 应用的源码分析
1. `Observable` 类便是一个被观察者的类，其中
    
    ![[IMG-20260315213657914.png|Untitled 3 129.png]]
    
    - `Vector<Observer> obs` ：聚合了保存观察者的数组
    
    - `addObserver` ：动态地添加一个观察者
    
    - `deleteObserver`：动态地删除一个观察者
    
    - `notifyObservers`：通知所有观察者
        
        ![[IMG-20260315213658449.png|Untitled 4 106.png]]
        
    
    1. `Observer` 接口便是观察者，其中定义了 `update()`
        
        ![[IMG-20260315213658934.png|Untitled 5 86.png]]