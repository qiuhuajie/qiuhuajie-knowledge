---
title: "Actor-Critic"
tags:
  - "人工智能"
  - "人工智能/强化学习"
  - "人工智能/强化学习/Policy-Based"
  - "Actor-Critic"
  - "Policy-Based"
  - "强化学习"
updated: 2026-04-16
---
- [[#1. Actor-Critic]]
- [[#2. A3C]]
# 1. **Actor-Critic**
1. 由于蒙特卡罗策略梯度 reinforce 算法需要完整的状态序列，同时单独对策略函数进行迭代更新，不太容易收敛
2. 引出 Actor-Critic算法：是一种策略（Policy Based）和价值（Value Based）相结合的方法
3. **Actor-Critic 从名字上看包括两部分，演员（Actor）和评价者（Critic）**
    1. **其中 Actor 使用策略函数，负责生成动作（Action）并和环境交互**
    2. **而 Critic 使用价值函数，负责评估 Actor 的表现，并指导 Actor 下一阶段的动作**
4. Policy Gradient 中
    1. 策略函数就是 Actor
    2. 但是那里是没有Critic的，我们当时使用了蒙特卡罗法来计算每一步的价值部分替代了Critic的功能，但是场景比较受限
5. 因此现在我们**使用类似DQN中用的价值函数来替代蒙特卡罗法，作为一个比较通用的 Critic**
6. 也就是说在Actor-Critic算法中，我们需要做两组近似
    1. 第一组是策略函数的近似：

        ![[IMG-20260404032005842.png|800]]
    2. 第二组是价值函数的近似，对于状态价值和动作价值函数分别是：

        ![[IMG-20260404032005896.png|800]]
7. 对于我们上一节讲到的蒙特卡罗策略梯度 reinforce 算法，我们需要进行改造才能变成Actor-Critic算法
    1. 首先，在蒙特卡罗策略梯度reinforce算法中，我们的策略的参数更新公式是

        ![[IMG-20260404032005950.png|800]]
        1. 梯度更新部分中，$∇θlogπθ(s_t,a_t)$ 是我们的分值函数，不用动
        2. 要变成 Actor 的话改动的是 $v_t$ ，这块不能再使用蒙特卡罗法来得到，而应该从 Critic 得到
    2. 而对于Critic来说，这块是新的，不过我们完全可以参考之前DQN的做法，即用一个Q网络来做为Critic, 这个Q网络的输入可以是状态，而输出是每个动作的价值或者最优动作的价值
8. ⭐总结：
    1. **就是 Critic 通过 Q 网络计算状态的最优价值 $v_t$**
    2. **而 Actor 利用 $v_t$ 这个最优价值迭代更新策略函数的参数 $\theta$ ，进而选择动作，并得到反馈和新的状态**
    3. **Critic 使用反馈和新的状态更新 Q 网络参数 $w$，然后 Critic 会使用新的网络参数来帮 Actor 计算状态的最优价值 $v_t$**
9. 有一个可以注意的点就是
    1. 上面我们对于 Critic 评估的点选择是和策略梯度 Policy Gradient 一样的状态价值 $v_t$ ，实际上，我们还可以选择很多其他的指标来做为 Critic 的评估点
    2. 而**目前可以使用的 Actor-Critic 评估点主要有**

        ![[IMG-20260404032006003.png|800]]
10. ==**Actor-Critic 算法流程⭐**==

    ![[IMG-20260404032006090.png|800]]
[](https://www.notion.soundefined)
# 2. A3C
1. **上面普通的 Actor-Critic 算法的代码，其实很难收敛**，无论怎么调参，最后的 CartPole 都很难稳定在200分，这是Actor-Critic算法的问题
2. 但是我们还是有办法去有优化这个难以收敛的问题的，而Asynchronous Advantage Actor-critic(以下简称A3C)就是其中比较好的优化算法
    1. 之前的DQN算法，为了方便收敛使用了经验回放的技巧
    2. 不过 **A3C 使用**==**经验回放**==**的技巧，还克服了一些经验回放的问题**
    3. 经验回放有什么问题呢？
        1. 回放池经验数据相关性太强，用于训练的时候效果很可能不佳
        2. 举个例子，我们学习下棋，总是和同一个人下，期望能提高棋艺。这当然没有问题，但是到一定程度就再难提高了，此时最好的方法是另寻高手切磋
    4. A3C 的思路也是如此，它**利用**==**多线程**==**的方法**
        1. 同时在多个线程里面分别和环境进行交互学习，每个线程都把学习的成果汇总起来，整理保存在一个公共的地方
        2. 并且，定期从公共的地方把大家的齐心学习的成果拿回来，指导自己和环境后面的学习交互
    5. 通过这种方法，**A3C 避免了经验回放相关性过强的问题，同时做到了异步并发的学习模型**
3. 相比 Actor-Critic，A3C的优化主要有3点，分别是异步训练框架，网络结构优化，Critic评估点的优化。其中异步训练框架是最大的优化
    1. ==**异步训练框架**==

        ![[IMG-20260404032006166.png|800]]
        1. 图中上面的Global Network就是上一节说的共享的公共部分，主要是一个公共的神经网络模型，这个神经网络包括Actor网络和Critic网络两部分的功能。下面有n个worker线程，每个线程里有和公共的神经网络一样的网络结构，每个线程会独立的和环境进行交互得到经验数据，这些线程之间互不干扰，独立运行
        2. **每个线程和环境交互到一定量的数据后，就计算在自己线程里的神经网络损失函数的梯度，但是这些梯度却并不更新自己线程里的神经网络，而是去更新公共的神经网络**。也就是n个线程会独立的使用累积的梯度分别更新公共部分的神经网络模型参数。**每隔一段时间，线程会将自己的神经网络的参数更新为公共神经网络的参数**，进而指导后面的环境交互
        3. 可见，公共部分的网络模型就是我们要学习的模型，而线程里的网络模型主要是用于和环境交互使用的，这些线程里的模型可以帮助线程更好的和环境交互，拿到高质量的数据帮助模型更快收敛
    2. ==**网络结构的优化**==
        1. 之前在 Actor-Critic 算法 使用了两个不同的网络 Actor 和 Critic
        2. 在A3C这里，我们把两个网络放到了一起
    3. ==**Critic 评估点的优化**==

        ![[IMG-20260405035358136.png|800]]
4. ==**A3C 算法流程⭐**==

    ![[IMG-20260405035358620.png|800]]